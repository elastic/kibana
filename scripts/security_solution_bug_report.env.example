# SecuritySolution Bug Report - Environment Configuration Template
# 
# Copy this file to ~/.kibana_security_report_env and fill in your values
# Then run: chmod 600 ~/.kibana_security_report_env (to protect credentials)
#
# Usage: source ~/.kibana_security_report_env

# ============================================================
# GitHub Configuration
# ============================================================

# Required: GitHub Personal Access Token
# Get from: https://github.com/settings/tokens
# Required scopes: repo (for private repos) or public_repo (for public only)
export GITHUB_TOKEN="ghp_your_token_here"

# Optional: Repository to query (defaults to elastic/kibana)
# export GITHUB_OWNER="elastic"
# export GITHUB_REPO="kibana"

# ============================================================
# SMTP Email Configuration
# ============================================================

# Required: SMTP server hostname
# Examples:
#   Gmail: smtp.gmail.com
#   Outlook: smtp-mail.outlook.com
#   Yahoo: smtp.mail.yahoo.com
export SMTP_HOST="smtp.gmail.com"

# Required: SMTP server port
# Common ports:
#   587 - TLS (recommended)
#   465 - SSL
#   25  - Unencrypted (not recommended)
export SMTP_PORT="587"

# Required: Email address to send from
export SMTP_USER="your.email@gmail.com"

# Required: Email password or app-specific password
# For Gmail: Use App Password (Google Account → Security → 2-Step → App passwords)
# For other providers: Check if app-specific passwords are required
export SMTP_PASS="your_app_password_here"

# Required: Recipient email addresses (comma-separated for multiple)
# Examples:
#   Single: "team@example.com"
#   Multiple: "team@example.com,manager@example.com,lead@example.com"
export REPORT_EMAIL_TO="recipient1@example.com,recipient2@example.com"

# ============================================================
# Optional: Additional Configuration
# ============================================================

# Uncomment to enable debug output
# export DEBUG="*"

# Node.js memory limit (optional, for large reports)
# export NODE_OPTIONS="--max-old-space-size=4096"
