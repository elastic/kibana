#!/bin/bash
set -e

export NVM_DIR="${NVM_DIR:-$HOME/.nvm}"
[ -s "$NVM_DIR/nvm.sh" ] && . "$NVM_DIR/nvm.sh"
nvm use 2>/dev/null || true

echo "Starting Kibana dev server on port 5601..."
exec yarn start --no-base-path
