# Expose those targets as they are required as part of
# other packages builds and need to be included as inputs
exports_files(
  [
    "package.json",
    "tsconfig.base.json",
    "tsconfig.json",
  ],
  visibility = ["//visibility:public"]
)


filegroup(
  name = "build_ts_refs",
  srcs = [
      "//src/core:build_ts_refs"
  ],
)
