$schema: https://moonrepo.dev/schemas/tasks.json

tasks:
  jest:
    command: node
    args:
      - scripts/jest.js
      - '--runInBand'
      - '--passWithNoTests'
    options:
      mergeArgs: append
      runFromWorkspaceRoot: true
