$schema: https://moonrepo.dev/schemas/tasks.json

fileGroups:
  "globalTsConfigs":
    - "/tsconfig.json"
    - "/tsconfig.base.json"
    - "/tsconfig.browser.json"

tasks:
  noop:
    command: echo "This is a no-op task"
    description: A task that does nothing
    options:
      runInCI: affected
