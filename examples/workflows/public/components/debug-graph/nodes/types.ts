export type NodeType = 'if' | 'merge' | 'parallel' | 'action' | 'foreach' | 'atomic' | 'trigger';

export const flowNodeTypes = [
  'if',
  'merge',
  'parallel',
  'foreach',
  'enter-foreach',
  'exit-foreach',
  'atomic',
  'merge',
  'trigger',
];
