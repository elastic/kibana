describe('esArchiver: createGenerateIndexRecordsStream()', () => {
  it('consumes index names and queries for the mapping of each');
  it('filter index metadata from settings');
  it('reports the archive');
  it('produces one index record for each index name it receives');
});
