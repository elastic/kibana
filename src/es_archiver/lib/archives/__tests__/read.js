describe('esArchiver createReadArchiveStreams()', () => {
  context('gzipped file', () => {
    it('returns an array of streams');
    it('reads and parses the file at the path');
  });

  context('plain text file', () => {
    it('returns an array of streams');
    it('reads and parses the file at the path');
  });
});
