.modal-header
  %button.close{"aria-hidden" => "true", "data-dismiss" => "modal", :type => "button"} ×
  %h3 {{panel.title}} Editor
.modal-body
  %h4
    General
    %small panel settings
  %div{"ng-include" => "", :src => "'partials/panelgeneral.html'"}
  %h4{:style => "text-transform: capitalize;"}
    {{panel.type}}
    %small
      panel settings.
      %strong{"ng-show" => "!_.isUndefined(panel.status)"} ({{panel.status}})
  %div{"ng-include" => "", :src => "edit_path(panel.type)"} No additional settings are available for this type of panel.
.modal-footer
  / close_edit() is provided here to allow for a scope to perform action on dismiss
  %button.btn.btn-success{"ng-click" => "close_edit();dismiss()", :type => "button"} Close
