load("@build_bazel_rules_nodejs//:index.bzl", "js_library")

SRCS = glob(
  [
    "server/kbn_server.d.ts",
    "server/kbn_server.js"
  ],
)

js_library(
    name = "legacy",
    srcs = SRCS,
    deps = [],
    visibility = ["//visibility:public"],
)
