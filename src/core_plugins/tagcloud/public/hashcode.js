/**
 * Converts string to 32-bit integer.
 * Return is a hash. Different strings can produce the same hashcode.
 * Hashes should be evenly distributed from 0 to 2^32. (no unit test for that)
 * @return {number}
 */
export function hashCode() {
  return -1;
}
