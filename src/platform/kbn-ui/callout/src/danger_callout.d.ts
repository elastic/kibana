import React from 'react';
import { type KbnCalloutProps } from './types';
export type KbnDangerCalloutProps = KbnCalloutProps;
/**
 * Danger callout — Use for errors, destructive operations, or blocking
 * issues that require immediate attention.
 */
export declare const KbnDangerCallout: (props: KbnDangerCalloutProps) => React.JSX.Element;
