/**
 * Updates the tab indices for a given set of elements.
 *
 * @param elements - the elements to update the tab indices for.
 */
export declare const updateTabIndices: (elements: HTMLElement[]) => void;
