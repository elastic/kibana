export declare const BROWSER_POPOVER_VERTICAL_OFFSET = 20;
/** Fallback index pattern when the query has no source (e.g. empty or only processing commands). */
export declare const DEFAULT_FIELDS_BROWSER_INDEX = "*";
export declare const SUPPORTED_COMMANDS: string[];
