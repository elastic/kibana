export const BROWSER_POPOVER_VERTICAL_OFFSET = 20; // 20px to account for the line height

// Commands that should have a badge
// Only FROM and TS commands support sources
export const SUPPORTED_COMMANDS = ['from', 'ts'];