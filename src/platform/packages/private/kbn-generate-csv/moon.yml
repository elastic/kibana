# This file is generated by the @kbn/moon package. Any manual edits will be erased!
#  To extend this, write your extensions/overrides to 'moon.extend.yml'
#  then regenerate this file with: 'node scripts/regenerate_moon_projects --update --filter @kbn/generate-csv'

$schema: https://moonrepo.dev/schemas/project.json
id: '@kbn/generate-csv'
type: unknown
owners:
  defaultOwner: '@elastic/response-ops'
toolchain:
  default: node
language: typescript
project:
  name: '@kbn/generate-csv'
  description: Moon project for @kbn/generate-csv
  channel: ''
  owner: '@elastic/response-ops'
  metadata:
    sourceRoot: src/platform/packages/private/kbn-generate-csv
tags:
  - platform
  - shared-server
  - prod
  - jest-unit-tests
fileGroups:
  src:
    - '**/*.ts'
    - '**/*.tsx'
    - '**/*.d.ts'
    - '!target/**/*'
tasks:
  jest:
    args:
      - '--config'
      - $projectRoot/jest.config.js
    inputs:
      - '@group(src)'
  jestCI:
    args:
      - '--config'
      - $projectRoot/jest.config.js
    inputs:
      - '@group(src)'
