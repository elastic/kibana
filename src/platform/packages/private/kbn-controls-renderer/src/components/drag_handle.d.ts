interface DragHandleProps {
    isEditable: boolean;
    controlTitle?: string;
    [key: string]: any;
}
export declare const DragHandle: ({ isEditable, controlTitle, children, ...rest }: DragHandleProps) => any;
export {};
