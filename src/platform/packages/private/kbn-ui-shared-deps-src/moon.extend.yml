tags:
  - is-webpack-built
dependsOn:
  - '@kbn/ui-shared-deps-npm'
tasks:
  build-webpack:
    command: yarn
    args:
      - build
    deps:
      - '^:build-webpack'
    outputs:
      - /target/build/src/platform/packages/private/kbn-ui-shared-deps-src
  watch-webpack:
    type: run
    options:
      persistent: true
    command: yarn
    args:
      - build
      - --watch
    deps:
      - '^:build-webpack'
    outputs:
      - /target/build/src/platform/packages/private/kbn-ui-shared-deps-src
