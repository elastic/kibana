# This file is generated by the @kbn/moon package. Any manual edits will be erased!
#  To extend this, write your extensions/overrides to 'moon.extend.yml'
#  then regenerate this file with: 'node scripts/regenerate_moon_projects.js --update --filter @kbn/vega-sandbox'

$schema: https://moonrepo.dev/schemas/project.json
id: '@kbn/vega-sandbox'
layer: unknown
owners:
  defaultOwner: '@elastic/kibana-visualizations'
toolchains:
  default: node
language: typescript
project:
  title: '@kbn/vega-sandbox'
  description: Moon project for @kbn/vega-sandbox
  channel: ''
  owner: '@elastic/kibana-visualizations'
  sourceRoot: src/platform/packages/private/kbn-vega-sandbox
dependsOn:
  - '@kbn/repo-info'
tags:
  - shared-common
  - package
  - prod
  - group-platform
  - private
  - jest-unit-tests
  - is-webpack-built
fileGroups:
  src:
    - '**/*.ts'
    - '**/*.js'
    - '!**/*.config.js'
    - '!target/**/*'
    - '!target_vega_sandbox/**/*'
  jest-config:
    - jest.config.js
tasks:
  build-webpack:
    command: pnpm
    args:
      - build
    deps:
      - ^:build-webpack
    checks:
      - check: fingerprint
        script: cd "${MOON_WORKSPACE_ROOT:-$(git rev-parse --show-toplevel)}" && node scripts/shared_deps_fingerprint.js --moon-project src/platform/packages/private/kbn-vega-sandbox --webpack-config src/platform/packages/private/kbn-vega-sandbox/webpack.config.js
        hash: stdout
    inputs:
      - '@group(src)'
      - package.json
      - webpack.config.js
    outputs:
      - /target/build/src/platform/packages/private/kbn-vega-sandbox
  watch-webpack:
    type: run
    options:
      persistent: true
    command: pnpm
    args:
      - build
      - '--watch'
    deps:
      - ^:build-webpack
    inputs:
      - '@group(src)'
      - package.json
      - webpack.config.js
    outputs:
      - /target/build/src/platform/packages/private/kbn-vega-sandbox
