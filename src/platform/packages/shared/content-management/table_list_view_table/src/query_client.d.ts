import React from 'react';
/**
 * Attempts to reuse existing query client otherwise fallbacks to a default one.
 */
export declare function MaybeQueryClientProvider({ children }: React.PropsWithChildren): React.JSX.Element;
