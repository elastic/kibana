# This file is generated by the @kbn/moon package. Any manual edits will be erased!
#  To extend this, write your extensions/overrides to 'moon.extend.yml'
#  then regenerate this file with: 'node scripts/regenerate_moon_projects.js --update --filter @kbn/doc-links'

$schema: https://moonrepo.dev/schemas/project.json
id: '@kbn/doc-links'
layer: unknown
owners:
  defaultOwner: '@elastic/docs'
toolchains:
  default: node
language: typescript
project:
  title: '@kbn/doc-links'
  description: Moon project for @kbn/doc-links
  channel: ''
  owner: '@elastic/docs'
  sourceRoot: src/platform/packages/shared/kbn-doc-links
dependsOn:
  - '@kbn/std'
tags:
  - shared-common
  - package
  - prod
  - group-platform
  - shared
  - jest-unit-tests
fileGroups:
  src:
    - '**/*.ts'
    - '!target/**/*'
tasks:
  jest:
    command: node
    args:
      - '--no-experimental-require-module'
      - $workspaceRoot/scripts/jest
      - '--config'
      - $projectRoot/jest.config.js
    options:
      runFromWorkspaceRoot: true
    inputs:
      - '@group(src)'
  jestCI:
    command: node
    args:
      - '--no-experimental-require-module'
      - $workspaceRoot/scripts/jest
      - '--config'
      - $projectRoot/jest.config.js
    options:
      runFromWorkspaceRoot: true
    inputs:
      - '@group(src)'
