# This file is generated by the @kbn/moon package. Any manual edits will be erased!
#  To extend this, write your extensions/overrides to 'moon.extend.yml'
#  then regenerate this file with: 'node scripts/regenerate_moon_projects.js --update --filter @kbn/mcp-dev-server'

$schema: https://moonrepo.dev/schemas/project.json
id: '@kbn/mcp-dev-server'
layer: unknown
owners:
  defaultOwner: '@elastic/appex-ai-infra'
toolchains:
  default: node
language: typescript
project:
  title: '@kbn/mcp-dev-server'
  description: Moon project for @kbn/mcp-dev-server
  channel: ''
  owner: '@elastic/appex-ai-infra'
  sourceRoot: src/platform/packages/shared/kbn-mcp-dev-server
dependsOn:
  - '@kbn/dev-cli-runner'
  - '@kbn/zod'
  - '@kbn/repo-info'
  - '@kbn/repo-packages'
  - '@kbn/std'
tags:
  - shared-server
  - package
  - dev
  - group-platform
  - shared
  - jest-unit-tests
fileGroups:
  src:
    - '**/*.ts'
    - '!target/**/*'
tasks:
  jest:
    command: node
    args:
      - '--no-experimental-require-module'
      - $workspaceRoot/scripts/jest
      - '--config'
      - $projectRoot/jest.config.js
    options:
      runFromWorkspaceRoot: true
    inputs:
      - '@group(src)'
  jestCI:
    command: node
    args:
      - '--no-experimental-require-module'
      - $workspaceRoot/scripts/jest
      - '--config'
      - $projectRoot/jest.config.js
    options:
      runFromWorkspaceRoot: true
    inputs:
      - '@group(src)'
