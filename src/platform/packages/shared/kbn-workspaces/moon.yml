# This file is generated by the @kbn/moon package. Any manual edits will be erased!
#  To extend this, write your extensions/overrides to 'moon.extend.yml'
#  then regenerate this file with: 'node scripts/regenerate_moon_projects.js --update --filter @kbn/workspaces'

$schema: https://moonrepo.dev/schemas/project.json
id: '@kbn/workspaces'
layer: unknown
owners:
  defaultOwner: '@elastic/observability-ui'
toolchains:
  default: node
language: typescript
project:
  title: '@kbn/workspaces'
  description: Moon project for @kbn/workspaces
  channel: ''
  owner: '@elastic/observability-ui'
  sourceRoot: src/platform/packages/shared/kbn-workspaces
dependsOn:
  - '@kbn/repo-info'
  - '@kbn/tooling-log'
tags:
  - shared-server
  - package
  - dev
  - group-platform
  - shared
  - jest-unit-tests
fileGroups:
  src:
    - '**/*.ts'
    - '!target/**/*'
tasks:
  jest:
    command: node
    args:
      - '--no-experimental-require-module'
      - $workspaceRoot/scripts/jest
      - '--config'
      - $projectRoot/jest.config.js
    options:
      runFromWorkspaceRoot: true
    inputs:
      - '@group(src)'
  jestCI:
    command: node
    args:
      - '--no-experimental-require-module'
      - $workspaceRoot/scripts/jest
      - '--config'
      - $projectRoot/jest.config.js
    options:
      runFromWorkspaceRoot: true
    inputs:
      - '@group(src)'
