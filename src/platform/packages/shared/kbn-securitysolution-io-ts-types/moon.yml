$schema: https://moonrepo.dev/schemas/project.json
id: '@kbn/securitysolution-io-ts-types'
type: unknown
owners:
  defaultOwner: '@elastic/security-detection-engine'
toolchain:
  default: node
language: typescript
project:
  name: '@kbn/securitysolution-io-ts-types'
  description: Moon project for @kbn/securitysolution-io-ts-types
  channel: ''
  owner: '@elastic/security-detection-engine'
  metadata:
    sourceRoot: src/platform/packages/shared/kbn-securitysolution-io-ts-types
dependsOn:
  - '@kbn/securitysolution-io-ts-utils'
tags:
  - platform
  - shared-common
  - prod
fileGroups:
  src:
    - '**/*.ts'
    - '!target/**/*'
tasks:
  jest:
    args:
      - '--config'
      - $projectRoot/jest.config.js
    inputs:
      - '@group(src)'
  jestCI:
    args:
      - '--config'
      - $projectRoot/jest.config.js
    inputs:
      - '@group(src)'
