import * as t from 'io-ts';
/**
 * Types the DefaultBooleanFalse as:
 *   - If null or undefined, then a default false will be set
 */
export declare const DefaultBooleanFalse: t.Type<boolean, boolean | undefined, unknown>;
