$schema: https://moonrepo.dev/schemas/project.json
id: '@kbn/securitysolution-es-utils'
type: unknown
owners:
  defaultOwner: '@elastic/security-detection-engine'
toolchain:
  default: node
language: typescript
project:
  name: '@kbn/securitysolution-es-utils'
  description: Moon project for @kbn/securitysolution-es-utils
  channel: ''
  owner: '@elastic/security-detection-engine'
  metadata:
    sourceRoot: src/platform/packages/shared/kbn-securitysolution-es-utils
dependsOn:
  - '@kbn/zod-helpers'
  - '@kbn/zod'
tags:
  - platform
  - shared-common
  - prod
fileGroups:
  src:
    - '**/*.ts'
    - '!target/**/*'
tasks:
  jest:
    args:
      - '--config'
      - $projectRoot/jest.config.js
    inputs:
      - '@group(src)'
  jestCI:
    args:
      - '--config'
      - $projectRoot/jest.config.js
    inputs:
      - '@group(src)'
