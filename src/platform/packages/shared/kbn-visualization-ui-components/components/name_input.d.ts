import React from 'react';
export declare const NameInput: ({ value, onChange, defaultValue, }: {
    value: string;
    onChange: (value: string) => void;
    defaultValue?: string;
}) => React.JSX.Element;
