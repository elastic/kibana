$schema: https://moonrepo.dev/schemas/project.json
id: '@kbn/unified-data-table'
type: unknown
owners:
  defaultOwner: '@elastic/kibana-data-discovery'
toolchain:
  default: node
language: typescript
project:
  name: '@kbn/unified-data-table'
  description: Moon project for @kbn/unified-data-table
  channel: ''
  owner: '@elastic/kibana-data-discovery'
  metadata:
    sourceRoot: src/platform/packages/shared/kbn-unified-data-table
tags:
  - platform
  - shared-browser
  - prod
fileGroups:
  src:
    - '*.ts'
    - '**/*.tsx'
    - src/**/*
    - __mocks__/**/*.ts
    - '!target/**/*'
tasks:
  jest:
    args:
      - '--config'
      - $projectRoot/jest.config.js
    inputs:
      - '@group(src)'
  jestCI:
    args:
      - '--config'
      - $projectRoot/jest.config.js
    inputs:
      - '@group(src)'
