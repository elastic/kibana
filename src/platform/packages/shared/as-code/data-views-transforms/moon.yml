# This file is generated by the @kbn/moon package. Any manual edits will be erased!
#  To extend this, write your extensions/overrides to 'moon.extend.yml'
#  then regenerate this file with: 'node scripts/regenerate_moon_projects.js --update --filter @kbn/as-code-data-views-transforms'

$schema: https://moonrepo.dev/schemas/project.json
id: '@kbn/as-code-data-views-transforms'
layer: unknown
owners:
  defaultOwner: '@elastic/kibana-data-discovery'
toolchains:
  default: node
language: typescript
project:
  title: '@kbn/as-code-data-views-transforms'
  description: Moon project for @kbn/as-code-data-views-transforms
  channel: ''
  owner: '@elastic/kibana-data-discovery'
  sourceRoot: src/platform/packages/shared/as-code/data-views-transforms
dependsOn: []
tags:
  - shared-common
  - package
  - prod
  - group-platform
  - shared
  - jest-unit-tests
fileGroups:
  src:
    - '**/*.ts'
    - '!target/**/*'
tasks:
  jest:
    command: node
    args:
      - '--no-experimental-require-module'
      - $workspaceRoot/scripts/jest
      - '--config'
      - $projectRoot/jest.config.js
    options:
      runFromWorkspaceRoot: true
    inputs:
      - '@group(src)'
  jestCI:
    command: node
    args:
      - '--no-experimental-require-module'
      - $workspaceRoot/scripts/jest
      - '--config'
      - $projectRoot/jest.config.js
    options:
      runFromWorkspaceRoot: true
    inputs:
      - '@group(src)'
