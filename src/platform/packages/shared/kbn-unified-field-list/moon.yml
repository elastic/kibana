$schema: https://moonrepo.dev/schemas/project.json
id: '@kbn/unified-field-list'
type: unknown
owners:
  defaultOwner: '@elastic/kibana-data-discovery'
toolchain:
  default: node
language: typescript
project:
  name: '@kbn/unified-field-list'
  description: Moon project for @kbn/unified-field-list
  channel: ''
  owner: '@elastic/kibana-data-discovery'
  metadata:
    sourceRoot: src/platform/packages/shared/kbn-unified-field-list
tags:
  - platform
  - shared-browser
  - prod
fileGroups:
  src:
    - '*.ts'
    - src/**/*
    - __mocks__/**/*.ts
    - '!target/**/*'
tasks:
  jest:
    args:
      - '--config'
      - $projectRoot/jest.config.js
    inputs:
      - '@group(src)'
  jestCI:
    args:
      - '--config'
      - $projectRoot/jest.config.js
    inputs:
      - '@group(src)'
