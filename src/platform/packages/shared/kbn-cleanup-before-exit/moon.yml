# This file is generated by the @kbn/moon package. Any manual edits will be erased!
#  To extend this, write your extensions/overrides to 'moon.extend.yml'
#  then regenerate this file with: 'node scripts/regenerate_moon_projects.js --update --filter @kbn/cleanup-before-exit'

$schema: https://moonrepo.dev/schemas/project.json
id: '@kbn/cleanup-before-exit'
layer: unknown
owners:
  defaultOwner: '@elastic/observability-ui'
toolchains:
  default: node
language: typescript
project:
  title: '@kbn/cleanup-before-exit'
  description: Moon project for @kbn/cleanup-before-exit
  channel: ''
  owner: '@elastic/observability-ui'
  sourceRoot: src/platform/packages/shared/kbn-cleanup-before-exit
dependsOn: []
tags:
  - shared-server
  - package
  - prod
  - group-platform
  - shared
  - jest-unit-tests
fileGroups:
  src:
    - '**/*.ts'
    - '!target/**/*'
tasks:
  jest:
    command: node
    args:
      - '--no-experimental-require-module'
      - $workspaceRoot/scripts/jest
      - '--config'
      - $projectRoot/jest.config.js
    options:
      runFromWorkspaceRoot: true
    inputs:
      - '@group(src)'
  jestCI:
    command: node
    args:
      - '--no-experimental-require-module'
      - $workspaceRoot/scripts/jest
      - '--config'
      - $projectRoot/jest.config.js
    options:
      runFromWorkspaceRoot: true
    inputs:
      - '@group(src)'
