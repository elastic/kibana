# @kbn/unified-tabs

Tabs bar components.

## Storybook 

Run the following command:
`pnpm storybook unified_tabs`.

## Example plugin

Start Kibana with:
`pnpm start --run-examples`.

Then navigate to the Unified Tabs example plugin `http://localhost:5601/app/unifiedTabsExamples`.
