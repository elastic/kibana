# @kbn/superdatepicker

This package is a wrapper around `EuiSuperDatePicker`.

## Entire time range

To enable this funcionality you need to provide a callback function `getEntireTimeRange` that will return an object containing `start` and `end` values, which are valid dates.
