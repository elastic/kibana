# This file is generated by the @kbn/moon package. Any manual edits will be erased!
#  To extend this, write your extensions/overrides to 'moon.extend.yml'
#  then regenerate this file with: 'node scripts/regenerate_moon_projects.js --update --filter @kbn/monaco'

$schema: https://moonrepo.dev/schemas/project.json
id: '@kbn/monaco'
layer: unknown
owners:
  defaultOwner: '@elastic/appex-sharedux'
toolchains:
  default: node
language: typescript
project:
  title: '@kbn/monaco'
  description: Moon project for @kbn/monaco
  channel: ''
  owner: '@elastic/appex-sharedux'
  sourceRoot: src/platform/packages/shared/kbn-monaco
dependsOn:
  - '@kbn/i18n'
  - '@kbn/repo-info'
  - '@kbn/esql-language'
  - '@kbn/esql-types'
  - '@kbn/std'
tags:
  - shared-common
  - package
  - prod
  - group-platform
  - shared
  - jest-unit-tests
  - is-webpack-built
fileGroups:
  src:
    - src/**/*
    - '**/*.ts'
    - src/languages/painless/autocomplete_definitions/*.json
    - '!target/**/*'
  jest-config:
    - jest.config.js
tasks:
  build-webpack:
    command: yarn
    args:
      - build
    checks:
      - check: fingerprint
        script: cd "${MOON_WORKSPACE_ROOT:-$(git rev-parse --show-toplevel)}" && node scripts/shared_deps_fingerprint.js --moon-project src/platform/packages/shared/kbn-monaco --webpack-config src/platform/packages/shared/kbn-monaco/webpack.config.js
        hash: stdout
    inputs:
      - '@group(src)'
      - package.json
      - webpack.config.js
      - /src/platform/packages/shared/kbn-esql-language/**/*.{js,ts,tsx}
      - /src/platform/packages/shared/kbn-esql-types/**/*.{js,ts,tsx}
      - /src/platform/packages/shared/kbn-esql-language/**/*.{js,ts,tsx}
      - /src/platform/packages/shared/kbn-i18n/**/*.{js,ts,tsx}
      - /src/platform/packages/shared/kbn-repo-info/**/*.{js,ts,tsx}
    outputs:
      - /target/build/src/platform/packages/shared/kbn-monaco
  watch-webpack:
    type: run
    options:
      persistent: true
    command: yarn
    args:
      - build
      - '--watch'
    inputs:
      - '@group(src)'
      - package.json
      - webpack.config.js
    outputs:
      - /target/build/src/platform/packages/shared/kbn-monaco
