# This file is generated by the @kbn/moon package. Any manual edits will be erased!
#  To extend this, write your extensions/overrides to 'moon.extend.yml'
#  then regenerate this file with: 'node scripts/regenerate_moon_projects.js --update --filter @kbn/xstate-utils'

$schema: https://moonrepo.dev/schemas/project.json
id: '@kbn/xstate-utils'
type: unknown
owners:
  defaultOwner: '@elastic/obs-onboarding-team'
toolchain:
  default: node
language: typescript
project:
  name: '@kbn/xstate-utils'
  description: Moon project for @kbn/xstate-utils
  channel: ''
  owner: '@elastic/obs-onboarding-team'
  metadata:
    sourceRoot: src/platform/packages/shared/kbn-xstate-utils
dependsOn: []
tags:
  - shared-browser
  - package
  - prod
  - group-platform
  - shared
  - jest-unit-tests
fileGroups:
  src:
    - '**/*.ts'
    - '!target/**/*'
tasks:
  jest:
    args:
      - '--config'
      - $projectRoot/jest.config.js
    inputs:
      - '@group(src)'
  jestCI:
    args:
      - '--config'
      - $projectRoot/jest.config.js
    inputs:
      - '@group(src)'
