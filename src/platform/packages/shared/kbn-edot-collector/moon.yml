# This file is generated by the @kbn/moon package. Any manual edits will be erased!
#  To extend this, write your extensions/overrides to 'moon.extend.yml'
#  then regenerate this file with: 'node scripts/regenerate_moon_projects.js --update --filter @kbn/edot-collector'

$schema: https://moonrepo.dev/schemas/project.json
id: '@kbn/edot-collector'
type: unknown
owners:
  defaultOwner: '@elastic/obs-ai-team'
toolchain:
  default: node
language: typescript
project:
  name: '@kbn/edot-collector'
  description: Moon project for @kbn/edot-collector
  channel: ''
  owner: '@elastic/obs-ai-team'
  metadata:
    sourceRoot: src/platform/packages/shared/kbn-edot-collector
dependsOn:
  - '@kbn/tooling-log'
  - '@kbn/dev-cli-runner'
  - '@kbn/repo-info'
tags:
  - shared-server
  - package
  - dev
  - group-platform
  - shared
  - jest-unit-tests
fileGroups:
  src:
    - '**/*.ts'
    - '!target/**/*'
tasks:
  jest:
    args:
      - '--config'
      - $projectRoot/jest.config.js
    inputs:
      - '@group(src)'
  jestCI:
    args:
      - '--config'
      - $projectRoot/jest.config.js
    inputs:
      - '@group(src)'
