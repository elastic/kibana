# This file is generated by the @kbn/moon package. Any manual edits will be erased!
#  To extend this, write your extensions/overrides to 'moon.extend.yml'
#  then regenerate this file with: 'node scripts/regenerate_moon_projects.js --update --filter @kbn/connector-specs'

$schema: https://moonrepo.dev/schemas/project.json
id: '@kbn/connector-specs'
type: unknown
owners:
  defaultOwner: '@elastic/response-ops'
toolchain:
  default: node
language: typescript
project:
  name: '@kbn/connector-specs'
  description: Moon project for @kbn/connector-specs
  channel: ''
  owner: '@elastic/response-ops'
  metadata:
    sourceRoot: src/platform/packages/shared/kbn-connector-specs
dependsOn:
  - '@kbn/zod'
  - '@kbn/logging'
  - '@kbn/licensing-types'
  - '@kbn/actions-utils'
  - '@kbn/i18n'
tags:
  - shared-common
  - package
  - prod
  - group-platform
  - shared
  - jest-unit-tests
fileGroups:
  src:
    - '**/*.ts'
    - '**/*.tsx'
    - '!target/**/*'
tasks:
  jest:
    args:
      - '--config'
      - $projectRoot/jest.config.js
    inputs:
      - '@group(src)'
  jestCI:
    args:
      - '--config'
      - $projectRoot/jest.config.js
    inputs:
      - '@group(src)'
