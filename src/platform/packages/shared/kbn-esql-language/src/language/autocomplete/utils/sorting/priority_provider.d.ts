import type { ISuggestionItem } from '../../../../commands/registry/types';
import type { SortingContext } from './types';
export declare function calculatePriority(item: ISuggestionItem, context: SortingContext): number;
