import type { SuggestForExpressionParams, SuggestForExpressionResult } from './types';
/** Coordinates position detection, handler selection, and range attachment */
export declare function suggestForExpression(params: SuggestForExpressionParams): Promise<SuggestForExpressionResult>;
