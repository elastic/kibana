# This file is generated by the @kbn/moon package. Any manual edits will be erased!
#  To extend this, write your extensions/overrides to 'moon.extend.yml'
#  then regenerate this file with: 'node scripts/regenerate_moon_projects.js --update --filter @kbn/crypto'

$schema: https://moonrepo.dev/schemas/project.json
id: '@kbn/crypto'
layer: unknown
owners:
  defaultOwner: '@elastic/kibana-security'
toolchains:
  default: node
language: typescript
project:
  title: '@kbn/crypto'
  description: Moon project for @kbn/crypto
  channel: ''
  owner: '@elastic/kibana-security'
  sourceRoot: src/platform/packages/shared/kbn-crypto
dependsOn:
  - '@kbn/dev-utils'
tags:
  - shared-common
  - package
  - prod
  - group-platform
  - shared
  - jest-unit-tests
fileGroups:
  src:
    - '**/*.ts'
    - '!target/**/*'
tasks:
  jest:
    command: node
    args:
      - '--no-experimental-require-module'
      - $workspaceRoot/scripts/jest
      - '--config'
      - $projectRoot/jest.config.js
    options:
      runFromWorkspaceRoot: true
    inputs:
      - '@group(src)'
  jestCI:
    command: node
    args:
      - '--no-experimental-require-module'
      - $workspaceRoot/scripts/jest
      - '--config'
      - $projectRoot/jest.config.js
    options:
      runFromWorkspaceRoot: true
    inputs:
      - '@group(src)'
