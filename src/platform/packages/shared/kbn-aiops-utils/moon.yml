# This file is generated by the @kbn/moon package. Any manual edits will be erased!
#  To extend this, write your extensions/overrides to 'moon.extend.yml'
#  then regenerate this file with: 'node scripts/regenerate_moon_projects.js --update --filter @kbn/aiops-utils'

$schema: https://moonrepo.dev/schemas/project.json
id: '@kbn/aiops-utils'
type: unknown
owners:
  defaultOwner: '@elastic/ml-ui'
toolchain:
  default: node
language: typescript
project:
  name: '@kbn/aiops-utils'
  description: Moon project for @kbn/aiops-utils
  channel: ''
  owner: '@elastic/ml-ui'
  metadata:
    sourceRoot: src/platform/packages/shared/kbn-aiops-utils
dependsOn:
  - '@kbn/data-views-plugin'
  - '@kbn/field-types'
tags:
  - shared-common
  - package
  - prod
  - group-platform
  - shared
  - jest-unit-tests
fileGroups:
  src:
    - '**/*.ts'
    - '**/*.tsx'
    - '!target/**/*'
tasks:
  jest:
    args:
      - '--config'
      - $projectRoot/jest.config.js
    inputs:
      - '@group(src)'
  jestCI:
    args:
      - '--config'
      - $projectRoot/jest.config.js
    inputs:
      - '@group(src)'
