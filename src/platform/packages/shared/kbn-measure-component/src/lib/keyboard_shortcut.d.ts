/**
 * Check if Escape key was pressed.
 */
export declare const isEscapeKey: (event: KeyboardEvent) => boolean;
/**
 * Check if the keyboard event corresponds to the measure shortcut (Meta/Ctrl + .).
 */
export declare const isMeasureShortcut: (event: KeyboardEvent) => boolean;
