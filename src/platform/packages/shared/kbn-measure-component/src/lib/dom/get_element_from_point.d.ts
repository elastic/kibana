/**
 * Get the deepest element at the given mouse event's coordinates.
 * Returns the most specific DOM element for precise measurements.
 */
export declare const getElementFromPoint: (event: MouseEvent) => HTMLElement | null;
