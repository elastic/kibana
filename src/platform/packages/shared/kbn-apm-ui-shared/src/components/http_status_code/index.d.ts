import React from 'react';
interface HttpStatusCodeProps {
    code: number;
    showTooltip?: boolean;
}
export declare function HttpStatusCode({ code, showTooltip }: HttpStatusCodeProps): React.JSX.Element;
export {};
