import React from 'react';
interface Props {
    inputTokens?: number;
    outputTokens?: number;
}
export declare function TokenUsageBadges({ inputTokens, outputTokens }: Props): React.JSX.Element | null;
export {};
