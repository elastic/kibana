{
  "type": "object",
  "properties": {
    "index": {
      "anyOf": [
        {
          "$ref": "#/$defs/Id"
        },
        {
          "type": "array",
          "items": {
            "$ref": "#/$defs/Id"
          }
        }
      ],
      "description": "Data stream or index to check for rollup capabilities. Wildcard (`*`) expressions are supported.",
      "$ref": "#/$defs/Ids",
      "x-found-in": "path"
    }
  },
  "required": [
    "index"
  ],
  "$defs": {
    "Id": {
      "type": "string"
    },
    "Ids": {
      "anyOf": [
        {
          "$ref": "#/$defs/Id"
        },
        {
          "type": "array",
          "items": {
            "$ref": "#/$defs/Id"
          }
        }
      ]
    }
  }
}
