{
  "type": "object",
  "properties": {
    "index": {
      "type": "string",
      "description": "The name of the indices (comma-separated) whose failed lifecycle step is to be retry",
      "$ref": "#/$defs/IndexName",
      "x-found-in": "path"
    }
  },
  "required": [
    "index"
  ],
  "$defs": {
    "IndexName": {
      "type": "string"
    }
  }
}
