{
  "type": "object",
  "properties": {
    "id": {
      "description": "Index, indices or index-pattern to return rollup capabilities for. `_all` may be used to fetch rollup capabilities from all jobs.",
      "$ref": "#/$defs/Id",
      "x-found-in": "path"
    }
  },
  "$defs": {
    "Id": {
      "type": "string"
    }
  }
}
