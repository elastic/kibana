{
  "type": "object",
  "properties": {
    "namespace": {
      "description": "The name of the namespace. Omit this parameter to retrieve information about all service accounts. If you omit this parameter, you must also omit the `service` parameter.",
      "$ref": "#/$defs/Namespace",
      "x-found-in": "path"
    },
    "service": {
      "description": "The service name. Omit this parameter to retrieve information about all service accounts that belong to the specified `namespace`.",
      "$ref": "#/$defs/Service",
      "x-found-in": "path"
    }
  },
  "$defs": {
    "Namespace": {
      "type": "string"
    },
    "Service": {
      "type": "string"
    }
  }
}
