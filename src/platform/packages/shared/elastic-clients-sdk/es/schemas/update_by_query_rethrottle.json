{
  "type": "object",
  "properties": {
    "task_id": {
      "type": "string",
      "description": "The ID for the task.",
      "$ref": "#/$defs/Id",
      "x-found-in": "path"
    },
    "requests_per_second": {
      "type": "number",
      "description": "The throttle for this request in sub-requests per second. To turn off throttling, set it to `-1`.",
      "$ref": "#/$defs/float",
      "x-found-in": "query"
    }
  },
  "required": [
    "task_id",
    "requests_per_second"
  ],
  "$defs": {
    "Id": {
      "type": "string"
    },
    "float": {
      "type": "number"
    }
  }
}
