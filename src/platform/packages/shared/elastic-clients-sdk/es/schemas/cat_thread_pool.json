{
  "type": "object",
  "properties": {
    "thread_pool_patterns": {
      "description": "A comma-separated list of thread pool names used to limit the request. Accepts wildcard expressions.",
      "$ref": "#/$defs/Names",
      "x-found-in": "path"
    },
    "h": {
      "description": "List of columns to appear in the response. Supports simple wildcards.",
      "$ref": "#/$defs/CatCatThreadPoolColumns",
      "x-found-in": "query"
    },
    "s": {
      "description": "A comma-separated list of column names or aliases that determines the sort order. Sorting defaults to ascending and can be changed by setting `:asc` or `:desc` as a suffix to the column name.",
      "$ref": "#/$defs/Names",
      "x-found-in": "query"
    },
    "local": {
      "type": "boolean",
      "description": "If `true`, the request computes the list of selected nodes from the local cluster state. If `false` the list of selected nodes are computed from the cluster state of the master node. In both cases the coordinating node will send requests for further information to each selected node.",
      "x-found-in": "query"
    },
    "master_timeout": {
      "description": "The period to wait for a connection to the master node.",
      "$ref": "#/$defs/Duration",
      "x-found-in": "query"
    }
  },
  "$defs": {
    "Name": {
      "type": "string"
    },
    "Names": {
      "anyOf": [
        {
          "$ref": "#/$defs/Name"
        },
        {
          "type": "array",
          "items": {
            "$ref": "#/$defs/Name"
          }
        }
      ]
    },
    "CatCatThreadPoolColumn": {
      "anyOf": [
        {
          "type": "string",
          "enum": [
            "active",
            "a",
            "completed",
            "c",
            "core",
            "cr",
            "ephemeral_id",
            "eid",
            "host",
            "h",
            "ip",
            "i",
            "keep_alive",
            "k",
            "largest",
            "l",
            "max",
            "mx",
            "name",
            "node_id",
            "id",
            "node_name",
            "pid",
            "p",
            "pool_size",
            "psz",
            "port",
            "po",
            "queue",
            "q",
            "queue_size",
            "qs",
            "rejected",
            "r",
            "size",
            "sz",
            "type",
            "t"
          ]
        },
        {
          "type": "string"
        }
      ]
    },
    "CatCatThreadPoolColumns": {
      "anyOf": [
        {
          "$ref": "#/$defs/CatCatThreadPoolColumn"
        },
        {
          "type": "array",
          "items": {
            "$ref": "#/$defs/CatCatThreadPoolColumn"
          }
        }
      ]
    },
    "Duration": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "number",
          "const": -1
        },
        {
          "type": "number",
          "const": 0
        }
      ]
    }
  }
}
