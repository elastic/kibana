{
  "type": "object",
  "properties": {
    "username": {
      "description": "The user whose password you want to change. If you do not specify this parameter, the password is changed for the current user.",
      "$ref": "#/$defs/Username",
      "x-found-in": "path"
    },
    "refresh": {
      "description": "If `true` (the default) then refresh the affected shards to make this operation visible to search, if `wait_for` then wait for a refresh to make this operation visible to search, if `false` then do nothing with refreshes.",
      "$ref": "#/$defs/Refresh",
      "x-found-in": "query"
    },
    "password": {
      "description": "The new password value. Passwords must be at least 6 characters long.",
      "$ref": "#/$defs/Password",
      "x-found-in": "body"
    },
    "password_hash": {
      "type": "string",
      "description": "A hash of the new password value. This must be produced using the same hashing algorithm as has been configured for password storage. For more details, see the explanation of the `xpack.security.authc.password_hashing.algorithm` setting.",
      "x-found-in": "body"
    }
  },
  "$defs": {
    "Username": {
      "type": "string"
    },
    "Refresh": {
      "anyOf": [
        {
          "type": "boolean"
        },
        {
          "type": "string",
          "enum": [
            "true",
            "false",
            "wait_for"
          ]
        }
      ]
    },
    "Password": {
      "type": "string"
    }
  }
}
