{
  "type": "object",
  "properties": {
    "repository": {
      "type": "string",
      "description": "The name of the repository to delete a snapshot from.",
      "$ref": "#/$defs/Name",
      "x-found-in": "path"
    },
    "snapshot": {
      "anyOf": [
        {
          "$ref": "#/$defs/Name"
        },
        {
          "type": "array",
          "items": {
            "$ref": "#/$defs/Name"
          }
        }
      ],
      "description": "A comma-separated list of snapshot names to delete. It also accepts wildcards (`*`).",
      "$ref": "#/$defs/Names",
      "x-found-in": "path"
    },
    "master_timeout": {
      "description": "The period to wait for the master node. If the master node is not available before the timeout expires, the request fails and returns an error. To indicate that the request should never timeout, set it to `-1`.",
      "$ref": "#/$defs/Duration",
      "x-found-in": "query"
    },
    "wait_for_completion": {
      "type": "boolean",
      "description": "If `true`, the request returns a response when the matching snapshots are all deleted. If `false`, the request returns a response as soon as the deletes are scheduled.",
      "x-found-in": "query"
    }
  },
  "required": [
    "repository",
    "snapshot"
  ],
  "$defs": {
    "Name": {
      "type": "string"
    },
    "Names": {
      "anyOf": [
        {
          "$ref": "#/$defs/Name"
        },
        {
          "type": "array",
          "items": {
            "$ref": "#/$defs/Name"
          }
        }
      ]
    },
    "Duration": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "number",
          "const": -1
        },
        {
          "type": "number",
          "const": 0
        }
      ]
    }
  }
}
