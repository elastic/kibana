{
  "type": "object",
  "properties": {
    "node_id": {
      "description": "A comma-separated list of node identifiers or names used to limit the returned information.",
      "$ref": "#/$defs/NodeIds",
      "x-found-in": "path"
    },
    "h": {
      "description": "A comma-separated list of columns names to display. It supports simple wildcards.",
      "$ref": "#/$defs/CatCatAllocationColumns",
      "x-found-in": "query"
    },
    "s": {
      "description": "List of columns that determine how the table should be sorted. Sorting defaults to ascending and can be changed by setting `:asc` or `:desc` as a suffix to the column name.",
      "$ref": "#/$defs/Names",
      "x-found-in": "query"
    },
    "local": {
      "type": "boolean",
      "description": "If `true`, the request computes the list of selected nodes from the local cluster state. If `false` the list of selected nodes are computed from the cluster state of the master node. In both cases the coordinating node will send requests for further information to each selected node.",
      "x-found-in": "query"
    },
    "master_timeout": {
      "description": "Period to wait for a connection to the master node.",
      "$ref": "#/$defs/Duration",
      "x-found-in": "query"
    }
  },
  "$defs": {
    "NodeId": {
      "type": "string"
    },
    "NodeIds": {
      "anyOf": [
        {
          "$ref": "#/$defs/NodeId"
        },
        {
          "type": "array",
          "items": {
            "$ref": "#/$defs/NodeId"
          }
        }
      ]
    },
    "CatCatAllocationColumn": {
      "anyOf": [
        {
          "type": "string",
          "enum": [
            "shards",
            "s",
            "shards.undesired",
            "write_load.forecast",
            "wlf",
            "writeLoadForecast",
            "disk.indices.forecast",
            "dif",
            "diskIndicesForecast",
            "disk.indices",
            "di",
            "diskIndices",
            "disk.used",
            "du",
            "diskUsed",
            "disk.avail",
            "da",
            "diskAvail",
            "disk.total",
            "dt",
            "diskTotal",
            "disk.percent",
            "dp",
            "diskPercent",
            "host",
            "h",
            "ip",
            "node",
            "n",
            "node.role",
            "r",
            "role",
            "nodeRole"
          ]
        },
        {
          "type": "string"
        }
      ]
    },
    "CatCatAllocationColumns": {
      "anyOf": [
        {
          "$ref": "#/$defs/CatCatAllocationColumn"
        },
        {
          "type": "array",
          "items": {
            "$ref": "#/$defs/CatCatAllocationColumn"
          }
        }
      ]
    },
    "Name": {
      "type": "string"
    },
    "Names": {
      "anyOf": [
        {
          "$ref": "#/$defs/Name"
        },
        {
          "type": "array",
          "items": {
            "$ref": "#/$defs/Name"
          }
        }
      ]
    },
    "Duration": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "number",
          "const": -1
        },
        {
          "type": "number",
          "const": 0
        }
      ]
    }
  }
}
