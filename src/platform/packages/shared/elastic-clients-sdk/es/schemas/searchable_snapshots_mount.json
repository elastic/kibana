{
  "type": "object",
  "properties": {
    "repository": {
      "type": "string",
      "description": "The name of the repository containing the snapshot of the index to mount.",
      "$ref": "#/$defs/Name",
      "x-found-in": "path"
    },
    "snapshot": {
      "type": "string",
      "description": "The name of the snapshot of the index to mount.",
      "$ref": "#/$defs/Name",
      "x-found-in": "path"
    },
    "master_timeout": {
      "description": "The period to wait for the master node. If the master node is not available before the timeout expires, the request fails and returns an error. To indicate that the request should never timeout, set it to `-1`.",
      "$ref": "#/$defs/Duration",
      "x-found-in": "query"
    },
    "wait_for_completion": {
      "type": "boolean",
      "description": "If true, the request blocks until the operation is complete.",
      "x-found-in": "query"
    },
    "storage": {
      "description": "The mount option for the searchable snapshot index. For further information on mount options, refer to: [Mount options](https://www.elastic.co/docs/deploy-manage/tools/snapshot-and-restore/searchable-snapshots#searchable-snapshot-mount-storage-options)",
      "$ref": "#/$defs/SearchableSnapshotsMountStorageOption",
      "x-found-in": "query"
    },
    "index": {
      "type": "string",
      "description": "The name of the index contained in the snapshot whose data is to be mounted. If no `renamed_index` is specified, this name will also be used to create the new index.",
      "$ref": "#/$defs/IndexName",
      "x-found-in": "body"
    },
    "renamed_index": {
      "description": "The name of the index that will be created.",
      "$ref": "#/$defs/IndexName",
      "x-found-in": "body"
    },
    "index_settings": {
      "type": "object",
      "propertyNames": {
        "type": "string"
      },
      "additionalProperties": {},
      "description": "The settings that should be added to the index when it is mounted.",
      "x-found-in": "body"
    },
    "ignore_index_settings": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "The names of settings that should be removed from the index when it is mounted.",
      "x-found-in": "body"
    }
  },
  "required": [
    "repository",
    "snapshot",
    "index"
  ],
  "$defs": {
    "Name": {
      "type": "string"
    },
    "Duration": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "number",
          "const": -1
        },
        {
          "type": "number",
          "const": 0
        }
      ]
    },
    "SearchableSnapshotsMountStorageOption": {
      "type": "string",
      "enum": [
        "full_copy",
        "shared_cache"
      ]
    },
    "IndexName": {
      "type": "string"
    }
  }
}
