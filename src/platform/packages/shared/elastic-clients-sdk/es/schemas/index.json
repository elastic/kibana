{
  "type": "object",
  "properties": {
    "id": {
      "description": "A unique identifier for the document. To automatically generate a document ID, use the `POST /<target>/_doc/` request format and omit this parameter.",
      "$ref": "#/$defs/Id",
      "x-found-in": "path"
    },
    "index": {
      "type": "string",
      "description": "The name of the data stream or index to target. If the target doesn't exist and matches the name or wildcard (`*`) pattern of an index template with a `data_stream` definition, this request creates the data stream. If the target doesn't exist and doesn't match a data stream template, this request creates the index. You can check for existing targets with the resolve index API.",
      "$ref": "#/$defs/IndexName",
      "x-found-in": "path"
    },
    "if_primary_term": {
      "description": "Only perform the operation if the document has this primary term.",
      "$ref": "#/$defs/long",
      "x-found-in": "query"
    },
    "if_seq_no": {
      "type": "number",
      "description": "Only perform the operation if the document has this sequence number.",
      "$ref": "#/$defs/SequenceNumber",
      "x-found-in": "query"
    },
    "include_source_on_error": {
      "type": "boolean",
      "description": "True or false if to include the document source in the error message in case of parsing errors.",
      "x-found-in": "query"
    },
    "op_type": {
      "description": "Set to `create` to only index the document if it does not already exist (put if absent). If a document with the specified `_id` already exists, the indexing operation will fail. The behavior is the same as using the `<index>/_create` endpoint. If a document ID is specified, this paramater defaults to `index`. Otherwise, it defaults to `create`. If the request targets a data stream, an `op_type` of `create` is required.",
      "$ref": "#/$defs/OpType",
      "x-found-in": "query"
    },
    "pipeline": {
      "type": "string",
      "description": "The ID of the pipeline to use to preprocess incoming documents. If the index has a default ingest pipeline specified, then setting the value to `_none` disables the default ingest pipeline for this request. If a final pipeline is configured it will always run, regardless of the value of this parameter.",
      "x-found-in": "query"
    },
    "refresh": {
      "description": "If `true`, Elasticsearch refreshes the affected shards to make this operation visible to search. If `wait_for`, it waits for a refresh to make this operation visible to search. If `false`, it does nothing with refreshes.",
      "$ref": "#/$defs/Refresh",
      "x-found-in": "query"
    },
    "routing": {
      "description": "A custom value that is used to route operations to a specific shard.",
      "$ref": "#/$defs/Routing",
      "x-found-in": "query"
    },
    "timeout": {
      "description": "The period the request waits for the following operations: automatic index creation, dynamic mapping updates, waiting for active shards. This parameter is useful for situations where the primary shard assigned to perform the operation might not be available when the operation runs. Some reasons for this might be that the primary shard is currently recovering from a gateway or undergoing relocation. By default, the operation will wait on the primary shard to become available for at least 1 minute before failing and responding with an error. The actual wait time could be longer, particularly when multiple waits occur.",
      "$ref": "#/$defs/Duration",
      "x-found-in": "query"
    },
    "version": {
      "type": "number",
      "description": "An explicit version number for concurrency control. It must be a non-negative long number.",
      "$ref": "#/$defs/VersionNumber",
      "x-found-in": "query"
    },
    "version_type": {
      "description": "The version type.",
      "$ref": "#/$defs/VersionType",
      "x-found-in": "query"
    },
    "wait_for_active_shards": {
      "description": "The number of shard copies that must be active before proceeding with the operation. You can set it to `all` or any positive integer up to the total number of shards in the index (`number_of_replicas+1`). The default value of `1` means it waits for each primary shard to be active.",
      "$ref": "#/$defs/WaitForActiveShards",
      "x-found-in": "query"
    },
    "require_alias": {
      "type": "boolean",
      "description": "If `true`, the destination must be an index alias.",
      "x-found-in": "query"
    },
    "require_data_stream": {
      "type": "boolean",
      "description": "If `true`, the request's actions must target a data stream (existing or to be created).",
      "x-found-in": "query"
    },
    "document": {
      "x-found-in": "body"
    }
  },
  "required": [
    "index",
    "document"
  ],
  "$defs": {
    "Id": {
      "type": "string"
    },
    "IndexName": {
      "type": "string"
    },
    "long": {
      "type": "number"
    },
    "SequenceNumber": {
      "$ref": "#/$defs/long"
    },
    "OpType": {
      "type": "string",
      "enum": [
        "index",
        "create"
      ]
    },
    "Refresh": {
      "anyOf": [
        {
          "type": "boolean"
        },
        {
          "type": "string",
          "enum": [
            "true",
            "false",
            "wait_for"
          ]
        }
      ]
    },
    "Routing": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "array",
          "items": {
            "type": "string"
          }
        }
      ]
    },
    "Duration": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "number",
          "const": -1
        },
        {
          "type": "number",
          "const": 0
        }
      ]
    },
    "VersionNumber": {
      "$ref": "#/$defs/long"
    },
    "VersionType": {
      "type": "string",
      "enum": [
        "internal",
        "external",
        "external_gte"
      ]
    },
    "integer": {
      "type": "number"
    },
    "WaitForActiveShardOptions": {
      "type": "string",
      "enum": [
        "all",
        "index-setting"
      ]
    },
    "WaitForActiveShards": {
      "anyOf": [
        {
          "$ref": "#/$defs/integer"
        },
        {
          "$ref": "#/$defs/WaitForActiveShardOptions"
        }
      ]
    }
  }
}
