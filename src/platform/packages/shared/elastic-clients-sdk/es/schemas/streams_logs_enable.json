{
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "enum": [
        "logs",
        "logs.otel",
        "logs.ecs"
      ],
      "description": "The stream type to enable.",
      "$ref": "#/$defs/StreamsStreamType",
      "x-found-in": "path"
    },
    "master_timeout": {
      "description": "The period to wait for a connection to the master node. If no response is received before the timeout expires, the request fails and returns an error.",
      "$ref": "#/$defs/Duration",
      "x-found-in": "query"
    },
    "timeout": {
      "description": "The period to wait for a response. If no response is received before the timeout expires, the request fails and returns an error.",
      "$ref": "#/$defs/Duration",
      "x-found-in": "query"
    }
  },
  "required": [
    "name"
  ],
  "$defs": {
    "StreamsStreamType": {
      "type": "string",
      "enum": [
        "logs",
        "logs.otel",
        "logs.ecs"
      ]
    },
    "Duration": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "number",
          "const": -1
        },
        {
          "type": "number",
          "const": 0
        }
      ]
    }
  }
}
