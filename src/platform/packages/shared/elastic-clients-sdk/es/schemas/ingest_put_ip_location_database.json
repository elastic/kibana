{
  "type": "object",
  "properties": {
    "id": {
      "type": "string",
      "description": "The database configuration identifier.",
      "$ref": "#/$defs/Id",
      "x-found-in": "path"
    },
    "master_timeout": {
      "description": "The period to wait for a connection to the master node. If no response is received before the timeout expires, the request fails and returns an error. A value of `-1` indicates that the request should never time out.",
      "$ref": "#/$defs/Duration",
      "x-found-in": "query"
    },
    "timeout": {
      "description": "The period to wait for a response from all relevant nodes in the cluster after updating the cluster metadata. If no response is received before the timeout expires, the cluster metadata update still applies but the response indicates that it was not completely acknowledged. A value of `-1` indicates that the request should never time out.",
      "$ref": "#/$defs/Duration",
      "x-found-in": "query"
    },
    "configuration": {
      "allOf": [
        {
          "type": "object",
          "properties": {
            "name": {
              "description": "The provider-assigned name of the IP geolocation database to download.",
              "$ref": "#/$defs/Name"
            }
          },
          "required": [
            "name"
          ]
        },
        {
          "anyOf": [
            {
              "type": "object",
              "properties": {
                "maxmind": {
                  "$ref": "#/$defs/IngestMaxmind"
                }
              },
              "required": [
                "maxmind"
              ]
            },
            {
              "type": "object",
              "properties": {
                "ipinfo": {
                  "$ref": "#/$defs/IngestIpinfo"
                }
              },
              "required": [
                "ipinfo"
              ]
            }
          ]
        }
      ],
      "$ref": "#/$defs/IngestDatabaseConfiguration",
      "x-found-in": "body"
    }
  },
  "required": [
    "id",
    "configuration"
  ],
  "$defs": {
    "Id": {
      "type": "string"
    },
    "Duration": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "number",
          "const": -1
        },
        {
          "type": "number",
          "const": 0
        }
      ]
    },
    "Name": {
      "type": "string"
    },
    "IngestMaxmind": {
      "type": "object",
      "properties": {
        "account_id": {
          "$ref": "#/$defs/Id"
        }
      },
      "required": [
        "account_id"
      ]
    },
    "IngestIpinfo": {
      "type": "object",
      "properties": {}
    },
    "IngestDatabaseConfiguration": {
      "allOf": [
        {
          "type": "object",
          "properties": {
            "name": {
              "description": "The provider-assigned name of the IP geolocation database to download.",
              "$ref": "#/$defs/Name"
            }
          },
          "required": [
            "name"
          ]
        },
        {
          "anyOf": [
            {
              "type": "object",
              "properties": {
                "maxmind": {
                  "$ref": "#/$defs/IngestMaxmind"
                }
              },
              "required": [
                "maxmind"
              ]
            },
            {
              "type": "object",
              "properties": {
                "ipinfo": {
                  "$ref": "#/$defs/IngestIpinfo"
                }
              },
              "required": [
                "ipinfo"
              ]
            }
          ]
        }
      ]
    }
  }
}
