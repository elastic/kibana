# This file is generated by the @kbn/moon package. Any manual edits will be erased!
#  To extend this, write your extensions/overrides to 'moon.extend.yml'
#  then regenerate this file with: 'node scripts/regenerate_moon_projects.js --update --filter @kbn/osquery-io-ts-types'

$schema: https://moonrepo.dev/schemas/project.json
id: '@kbn/osquery-io-ts-types'
layer: unknown
owners:
  defaultOwner: '@elastic/security-defend-workflows'
toolchains:
  default: node
language: typescript
project:
  title: '@kbn/osquery-io-ts-types'
  description: Moon project for @kbn/osquery-io-ts-types
  channel: ''
  owner: '@elastic/security-defend-workflows'
  sourceRoot: src/platform/packages/shared/kbn-osquery-io-ts-types
dependsOn:
  - '@kbn/io-ts-utils'
tags:
  - shared-common
  - package
  - prod
  - group-platform
  - shared
  - jest-unit-tests
fileGroups:
  src:
    - '**/*.ts'
    - '!target/**/*'
tasks:
  jest:
    command: node
    args:
      - '--no-experimental-require-module'
      - $workspaceRoot/scripts/jest
      - '--config'
      - $projectRoot/jest.config.js
    options:
      runFromWorkspaceRoot: true
    inputs:
      - '@group(src)'
  jestCI:
    command: node
    args:
      - '--no-experimental-require-module'
      - $workspaceRoot/scripts/jest
      - '--config'
      - $projectRoot/jest.config.js
    options:
      runFromWorkspaceRoot: true
    inputs:
      - '@group(src)'
