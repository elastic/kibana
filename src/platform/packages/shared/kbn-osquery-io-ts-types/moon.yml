$schema: https://moonrepo.dev/schemas/project.json
id: '@kbn/osquery-io-ts-types'
type: unknown
owners:
  defaultOwner: '@elastic/security-asset-management'
toolchain:
  default: node
language: typescript
project:
  name: '@kbn/osquery-io-ts-types'
  description: Moon project for @kbn/osquery-io-ts-types
  channel: ''
  owner: '@elastic/security-asset-management'
  metadata:
    sourceRoot: src/platform/packages/shared/kbn-osquery-io-ts-types
dependsOn:
  - '@kbn/io-ts-utils'
tags:
  - platform
  - shared-common
  - prod
fileGroups:
  src:
    - '**/*.ts'
    - '!target/**/*'
tasks:
  jest:
    args:
      - '--config'
      - $projectRoot/jest.config.js
    inputs:
      - '@group(src)'
  jestCI:
    args:
      - '--config'
      - $projectRoot/jest.config.js
    inputs:
      - '@group(src)'
