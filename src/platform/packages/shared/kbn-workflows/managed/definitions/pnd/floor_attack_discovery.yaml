version: "1"
name: Attack Discovery
description: Watch Floor Attack Discovery worker stub.
enabled: false
tags:
  - watch
  - watch-floor
triggers:
  - type: manual
consts:
  worker_settings:
    settingsVersion: __WORKER_SETTINGS_VERSION__
    autonomy: "__WORKER_AUTONOMY_LEVEL__"
steps:
  - name: stub
    type: console
    with:
      message: "Attack Discovery worker stub"
