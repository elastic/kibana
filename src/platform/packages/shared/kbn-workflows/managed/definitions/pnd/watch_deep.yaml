version: "1"
name: Deep Watch
description: >
  Deep Watch skeleton. Specialist, on-demand depth — forensics,
  hunts, and draft-only conclusions under human review.
enabled: false
tags:
  - watch
  - watch-deep
triggers:
  - type: manual
consts:
  watch_policy:
    settingsVersion: __WATCH_SETTINGS_VERSION__
    # Persisted plumbing only; runtime autonomy semantics are defined separately.
    autonomy: "__WATCH_AUTONOMY_LEVEL__"
    mandate: Deep investigation & hunts
    ui:
      color: "#8b5cf6"
      order: 40
steps:
  - name: specialist_stub
    type: console
    with:
      message: "Deep Watch skeleton — add ai.agent / skill steps (see Floor)"
