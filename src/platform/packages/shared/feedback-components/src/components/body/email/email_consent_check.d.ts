import React from 'react';
interface Props {
    allowEmailContact: boolean;
    handleChangeAllowEmailContact: (allow: boolean) => void;
}
export declare const EmailConsentCheck: ({ allowEmailContact, handleChangeAllowEmailContact }: Props) => React.JSX.Element;
export {};
