# This file is generated by the @kbn/moon package. Any manual edits will be erased!
#  To extend this, write your extensions/overrides to 'moon.extend.yml'
#  then regenerate this file with: 'node scripts/regenerate_moon_projects.js --update --filter @kbn/unsaved-changes-prompt'

$schema: https://moonrepo.dev/schemas/project.json
id: '@kbn/unsaved-changes-prompt'
type: unknown
owners:
  defaultOwner: '@elastic/kibana-management'
toolchain:
  default: node
language: typescript
project:
  name: '@kbn/unsaved-changes-prompt'
  description: Moon project for @kbn/unsaved-changes-prompt
  channel: ''
  owner: '@elastic/kibana-management'
  metadata:
    sourceRoot: src/platform/packages/shared/kbn-unsaved-changes-prompt
dependsOn:
  - '@kbn/i18n'
  - '@kbn/core'
tags:
  - shared-browser
  - package
  - prod
  - group-platform
  - shared
  - jest-unit-tests
fileGroups:
  src:
    - '*.ts'
    - src/**/*
    - '!target/**/*'
tasks:
  jest:
    args:
      - '--config'
      - $projectRoot/jest.config.js
    inputs:
      - '@group(src)'
  jestCI:
    args:
      - '--config'
      - $projectRoot/jest.config.js
    inputs:
      - '@group(src)'
