# @kbn/unified-shortcuts

Shared shortcut UI primitives for Kibana.

This package currently exports `LeaderKeyShortcuts`, a small overlay component for leader-key
shortcut flows, along with its `LeaderKeyShortcut` type.
