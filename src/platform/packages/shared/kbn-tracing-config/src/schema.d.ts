import type { Type } from '@kbn/config-schema';
import type { TracingConfig } from './types';
/**
 * The tracing config schema that is exposed by the Telemetry plugin.
 */
export declare const tracingConfigSchema: Type<TracingConfig>;
