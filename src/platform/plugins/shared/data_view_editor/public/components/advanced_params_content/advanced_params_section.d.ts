import React from 'react';
interface Props {
    children: React.ReactNode;
    defaultVisible: boolean;
}
export declare const AdvancedParamsSection: ({ children, defaultVisible }: Props) => React.JSX.Element;
export {};
