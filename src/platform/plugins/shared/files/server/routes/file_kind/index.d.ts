import type { FileKind } from '../../../common/types';
import type { FilesRouter } from '../types';
/**
 * Register a single file kind's routes
 */
export declare function registerFileKindRoutes(router: FilesRouter, fileKind: FileKind): void;
