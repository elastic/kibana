import React, { type ReactNode } from 'react';
interface Props {
    imgElement: ReactNode;
    closeModal: () => void;
}
export declare const ImagePreviewModal: ({ imgElement, closeModal }: Props) => React.JSX.Element;
export {};
