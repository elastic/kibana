import React from 'react';
interface Props {
    imgHTML: string;
    closeModal: () => void;
}
export declare const ImagePreviewModal: ({ imgHTML, closeModal }: Props) => React.JSX.Element;
export {};
