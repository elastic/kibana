# This file is generated by the @kbn/moon package. Any manual edits will be erased!
#  To extend this, write your extensions/overrides to 'moon.extend.yml'
#  then regenerate this file with: 'node scripts/regenerate_moon_projects --update --filter @kbn/ui-actions-enhanced-plugin'

$schema: https://moonrepo.dev/schemas/project.json
id: '@kbn/ui-actions-enhanced-plugin'
type: unknown
owners:
  defaultOwner: '@elastic/appex-sharedux'
toolchain:
  default: node
language: typescript
project:
  name: '@kbn/ui-actions-enhanced-plugin'
  description: Moon project for @kbn/ui-actions-enhanced-plugin
  channel: ''
  owner: '@elastic/appex-sharedux'
  metadata:
    sourceRoot: src/platform/plugins/shared/ui_actions_enhanced
tags:
  - platform
  - plugin
  - prod
  - jest-unit-tests
fileGroups:
  src:
    - public/**/*
    - server/**/*
    - common/**/*
    - '!target/**/*'
tasks:
  jest:
    args:
      - '--config'
      - $projectRoot/jest.config.js
    inputs:
      - '@group(src)'
  jestCI:
    args:
      - '--config'
      - $projectRoot/jest.config.js
    inputs:
      - '@group(src)'
