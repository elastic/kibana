import { ProfileStateRegistry } from './profile_state';
/** Creates the Discover profile state registry with all supported definitions registered. */
export declare const createProfileStateRegistry: () => ProfileStateRegistry;
