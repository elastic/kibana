/**
 * Take text from the model and present it to the user as a string
 * @param text model value
 * @returns {string}
 */
export declare function toUser(text: {
    [key: string]: any;
} | string | number): string;
