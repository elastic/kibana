import type { IKbnUrlStateStorage } from '@kbn/kibana-utils-plugin/public';
export declare const getDashboardListItemLink: (kbnUrlStateStorage: IKbnUrlStateStorage, id: string, timeRestore: boolean) => string;
