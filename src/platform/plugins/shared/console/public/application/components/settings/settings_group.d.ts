import React from 'react';
export interface DevToolsSettingsModalProps {
    title: string;
    description?: string;
}
export declare const SettingsGroup: ({ title, description }: DevToolsSettingsModalProps) => React.JSX.Element;
