import React from 'react';
interface ImportConfirmModalProps {
    onClose: () => void;
    fileContent: string;
}
export declare const ImportConfirmModal: ({ onClose, fileContent }: ImportConfirmModalProps) => React.JSX.Element;
export {};
