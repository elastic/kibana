import React from 'react';
interface MainProps {
    currentTabProp?: string;
    isEmbeddable?: boolean;
}
export declare function Main({ currentTabProp, isEmbeddable }: MainProps): React.JSX.Element | null;
export {};
