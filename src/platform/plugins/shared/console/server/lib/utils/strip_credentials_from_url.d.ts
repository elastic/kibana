/**
 * Strips username and password credentials from a URL string.
 * Returns the original string if it cannot be parsed as a URL.
 */
export declare function stripCredentialsFromUrl(urlString: string): string;
