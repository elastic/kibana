## Dashboard panel placement registry

This registry is used to register how dashboard panels are placed when created.

It is exported from the `presentationUtil` plugin instead of the `dashboard` plugin to reduce circular
dependency issues.
