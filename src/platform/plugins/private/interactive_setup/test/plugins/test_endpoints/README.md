# Interactive Setup Test Endpoints

Preboot plugin that exposes test-only HTTP endpoints used by the `interactive_setup` Scout specs (e.g. retrieving the Kibana verification code). Loaded only when running tests — never shipped in production builds.
