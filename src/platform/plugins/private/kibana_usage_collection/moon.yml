$schema: https://moonrepo.dev/schemas/project.json
id: '@kbn/kibana-usage-collection-plugin'
type: unknown
owners:
  defaultOwner: '@elastic/kibana-core'
toolchain:
  default: node
language: typescript
project:
  name: '@kbn/kibana-usage-collection-plugin'
  description: Moon project for @kbn/kibana-usage-collection-plugin
  channel: ''
  owner: '@elastic/kibana-core'
  metadata:
    sourceRoot: src/platform/plugins/private/kibana_usage_collection
tags:
  - platform
  - plugin
  - prod
fileGroups:
  src:
    - common/*
    - public/**/*
    - server/**/*
    - '!target/**/*'
tasks:
  jest:
    args:
      - '--config'
      - $projectRoot/jest.config.js
    inputs:
      - '@group(src)'
  jestCI:
    args:
      - '--config'
      - $projectRoot/jest.config.js
    inputs:
      - '@group(src)'
