# This file is generated by the @kbn/moon package. Any manual edits will be erased!
#  To extend this, write your extensions/overrides to 'moon.extend.yml'
#  then regenerate this file with: 'node scripts/regenerate_moon_projects.js --update --filter @kbn/node-debugging-routes-plugin'

$schema: https://moonrepo.dev/schemas/project.json
id: '@kbn/node-debugging-routes-plugin'
layer: unknown
owners:
  defaultOwner: '@elastic/kibana-core'
toolchains:
  default: node
language: typescript
project:
  title: '@kbn/node-debugging-routes-plugin'
  description: Moon project for @kbn/node-debugging-routes-plugin
  channel: ''
  owner: '@elastic/kibana-core'
  sourceRoot: src/platform/plugins/private/node_debugging_routes
dependsOn:
  - '@kbn/core'
  - '@kbn/config-schema'
tags:
  - plugin
  - prod
  - group-platform
  - private
  - jest-unit-tests
fileGroups:
  src:
    - server/**/*
    - '!target/**/*'
tasks:
  jest:
    command: node
    args:
      - '--no-experimental-require-module'
      - $workspaceRoot/scripts/jest
      - '--config'
      - $projectRoot/jest.config.js
    options:
      runFromWorkspaceRoot: true
    inputs:
      - '@group(src)'
  jestCI:
    command: node
    args:
      - '--no-experimental-require-module'
      - $workspaceRoot/scripts/jest
      - '--config'
      - $projectRoot/jest.config.js
    options:
      runFromWorkspaceRoot: true
    inputs:
      - '@group(src)'
