# @kbn/core-notifications-browser-components

Reusable React components for rendering notification center events.
Consumers typically pair these with hooks from
`@kbn/core-notifications-browser-hooks` to build a notification surface
(e.g. a sidebar or popover) on top of `NotificationsStart#events`.
