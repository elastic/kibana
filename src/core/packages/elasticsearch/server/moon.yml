$schema: https://moonrepo.dev/schemas/project.json
id: '@kbn/core-elasticsearch-server'
type: unknown
owners:
  defaultOwner: '@elastic/kibana-core'
toolchain:
  default: node
language: typescript
project:
  name: '@kbn/core-elasticsearch-server'
  description: Moon project for @kbn/core-elasticsearch-server
  channel: ''
  owner: '@elastic/kibana-core'
  metadata:
    sourceRoot: src/core/packages/elasticsearch/server
dependsOn:
  - '@kbn/utility-types'
  - '@kbn/es-errors'
  - '@kbn/core-http-server'
  - '@kbn/config-schema'
tags:
  - platform
  - shared-server
  - prod
fileGroups:
  src:
    - '**/*.ts'
    - '!target/**/*'
tasks:
  jest:
    args:
      - '--config'
      - $projectRoot/jest.config.js
    inputs:
      - '@group(src)'
  jestCI:
    args:
      - '--config'
      - $projectRoot/jest.config.js
    inputs:
      - '@group(src)'
