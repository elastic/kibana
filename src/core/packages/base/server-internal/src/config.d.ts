/**
 * Constants for Core config paths that are used from multiple service.
 *
 * @internal
 */
export declare const coreConfigPaths: {
    /**
     * Config path of the http service's config.
     */
    http: string;
};
