import type { FunctionComponent } from 'react';
import type { RenderingMetadata } from '../types';
interface Props {
    url: RenderingMetadata['uiPublicUrl'];
    optimizeFontLoading?: boolean;
}
export declare const Fonts: FunctionComponent<Props>;
export {};
