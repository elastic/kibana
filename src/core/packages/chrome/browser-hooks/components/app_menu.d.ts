import type { AppMenuConfig } from '@kbn/core-chrome-app-menu-components';
interface AppMenuProps {
    config?: AppMenuConfig;
}
export declare const RegisterAppMenu: ({ config }: AppMenuProps) => null;
export {};
