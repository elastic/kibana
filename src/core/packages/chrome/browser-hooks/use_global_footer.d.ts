import type { ReactNode } from 'react';
/**
 * Returns the current global footer `ReactNode` registered by the developer
 * toolbar. **Internal** — not intended for plugin use.
 */
export declare function useGlobalFooter(): ReactNode;
