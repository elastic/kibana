import React from 'react';
import type { AppHeaderBadge } from '../types';
export declare const AppBadge: {
    ({ badge }: {
        badge: AppHeaderBadge;
    }): React.JSX.Element;
    displayName: string;
};
