# @kbn/core-spaces-common

Domain-neutral package that owns the `SpaceId` branded type, `asSpaceId` helper, and `DEFAULT_SPACE_ID` constant. Shared by HTTP, saved-objects, authz, task-manager, and other Core packages without introducing cross-dependencies.
