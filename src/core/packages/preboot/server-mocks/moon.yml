$schema: https://moonrepo.dev/schemas/project.json
id: '@kbn/core-preboot-server-mocks'
type: unknown
owners:
  defaultOwner: '@elastic/kibana-core'
toolchain:
  default: node
language: typescript
project:
  name: '@kbn/core-preboot-server-mocks'
  description: Moon project for @kbn/core-preboot-server-mocks
  channel: ''
  owner: '@elastic/kibana-core'
  metadata:
    sourceRoot: src/core/packages/preboot/server-mocks
dependsOn:
  - '@kbn/utility-types'
  - '@kbn/core-preboot-server-internal'
  - '@kbn/core-preboot-server'
tags:
  - platform
  - shared-server
  - dev
fileGroups:
  src:
    - '**/*.ts'
    - '!target/**/*'
tasks:
  jest:
    args:
      - '--config'
      - $projectRoot/jest.config.js
    inputs:
      - '@group(src)'
  jestCI:
    args:
      - '--config'
      - $projectRoot/jest.config.js
    inputs:
      - '@group(src)'
