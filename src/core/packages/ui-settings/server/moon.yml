# This file is generated by the @kbn/moon package. Any manual edits will be erased!
#  To extend this, write your extensions/overrides to 'moon.extend.yml'
#  then regenerate this file with: 'node scripts/regenerate_moon_projects.js --update --filter @kbn/core-ui-settings-server'

$schema: https://moonrepo.dev/schemas/project.json
id: '@kbn/core-ui-settings-server'
type: unknown
owners:
  defaultOwner: '@elastic/appex-sharedux'
toolchain:
  default: node
language: typescript
project:
  name: '@kbn/core-ui-settings-server'
  description: Moon project for @kbn/core-ui-settings-server
  channel: ''
  owner: '@elastic/appex-sharedux'
  metadata:
    sourceRoot: src/core/packages/ui-settings/server
dependsOn:
  - '@kbn/core-saved-objects-api-server'
  - '@kbn/core-ui-settings-common'
tags:
  - shared-server
  - package
  - prod
  - group-platform
  - shared
  - jest-unit-tests
fileGroups:
  src:
    - '**/*.ts'
    - '!target/**/*'
tasks:
  jest:
    args:
      - '--config'
      - $projectRoot/jest.config.js
    inputs:
      - '@group(src)'
  jestCI:
    args:
      - '--config'
      - $projectRoot/jest.config.js
    inputs:
      - '@group(src)'
