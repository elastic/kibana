$schema: https://moonrepo.dev/schemas/project.json
id: '@kbn/core-metrics-server'
type: unknown
owners:
  defaultOwner: '@elastic/kibana-core'
toolchain:
  default: node
language: typescript
project:
  name: '@kbn/core-metrics-server'
  description: Moon project for @kbn/core-metrics-server
  channel: ''
  owner: '@elastic/kibana-core'
  metadata:
    sourceRoot: src/core/packages/metrics/server
dependsOn:
  - '@kbn/utility-types'
tags:
  - platform
  - shared-server
  - prod
fileGroups:
  src:
    - '**/*.ts'
    - '!target/**/*'
tasks:
  jest:
    args:
      - '--config'
      - $projectRoot/jest.config.js
    inputs:
      - '@group(src)'
  jestCI:
    args:
      - '--config'
      - $projectRoot/jest.config.js
    inputs:
      - '@group(src)'
