$schema: https://moonrepo.dev/schemas/project.json
id: '@kbn/core-application-common'
type: unknown
owners:
  defaultOwner: '@elastic/kibana-core'
toolchain:
  default: node
language: typescript
project:
  name: '@kbn/core-application-common'
  description: Moon project for @kbn/core-application-common
  channel: ''
  owner: '@elastic/kibana-core'
  metadata:
    sourceRoot: src/core/packages/application/common
dependsOn:
  - '@kbn/i18n'
tags:
  - platform
  - shared-common
  - prod
fileGroups:
  src:
    - '**/*.ts'
    - '**/*.tsx'
    - '!target/**/*'
tasks:
  jest:
    args:
      - '--config'
      - $projectRoot/jest.config.js
    inputs:
      - '@group(src)'
  jestCI:
    args:
      - '--config'
      - $projectRoot/jest.config.js
    inputs:
      - '@group(src)'
