openapi: 3.0.0
info:
  title: Update External Rule Source API endpoint
  version: '1'
paths:
  /internal/detection_engine/external_rule_sources/_update:
    post:
      x-labels: [ess, serverless]
      x-codegen-enabled: true
      operationId: UpdateExternalRuleSource
      summary: Update External Rule Source
      description: Update an external rule source
      tags:
        - External Rule Sources API
      requestBody:
        content:
          application/json:
            schema:
              allOf:
                - $ref: '../model/external_rule_source.schema.yaml#/components/schemas/ExternalRuleSourceInput'
                - type: object
                  properties:
                    id:
                      description: The unique identifier of the external rule source
                      type: string
                  required:
                    - id
      responses:
        200:
          description: Indicates a successful call
          content:
            application/json:
              schema:
                $ref: '../model/external_rule_source.schema.yaml#/components/schemas/ExternalRuleSourceOutput'
