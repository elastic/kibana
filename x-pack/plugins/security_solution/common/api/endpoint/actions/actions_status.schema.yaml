openapi: 3.0.0
info:
  title: Get Action status schema
  version: '2023-10-31'
paths: { }
components:
  parameters:
    GetActionStatusRequestQueryParameter:
      name: query
      in: query
      required: true
      schema:
        $ref: '../model/schema/common.yaml#/components/schemas/AgentIds'

  schemas:
    SuccessResponse:
      type: object
      properties: {}
      # Define properties for the success response if needed
