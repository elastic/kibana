openapi: 3.0.0
info:
  title: File Download Schema
  version: '2023-10-31'
paths: { }
components:
  parameters:
   FileDownloadRequestParameter:
      name: query
      in: path
      required: true
      schema:
        $ref: '#/components/schemas/FileDownloadRequestParams'
  schemas:
    FileDownloadRequestParams:
      type: object
      required:
        - action_id
        - file_id
      properties:
        action_id:
          type: string
        file_id:
          type: string
    SuccessResponse:
      type: object
      properties: {}
      # Define properties for the success response if needed
