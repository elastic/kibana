openapi: 3.0.0
info:
  title: File Info Schema
  version: '2023-10-31'
paths: { }
components:
  parameters:
   FileInfoRequestParameter:
      name: query
      in: path
      required: true
      schema:
        $ref: '#/components/schemas/FileInfoRequestParams'
  schemas:
    FileInfoRequestParams:
      type: object
      required:
        - action_id
        - file_id
      properties:
        action_id:
          type: string
        file_id:
          type: string
    SuccessResponse:
      type: object
      properties: {}
      # Define properties for the success response if needed
