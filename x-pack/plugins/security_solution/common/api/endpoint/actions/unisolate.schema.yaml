openapi: 3.0.0
info:
  title: Release Schema
  version: '2023-10-31'
paths:
  /api/endpoint/action/unisolate:
    post:
      summary: Release Action
      operationId: EndpointReleaseAction
      description: Release an endpoint
      x-codegen-enabled: false
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ReleaseRouteRequestBody'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '../model/schema/common.schema.yaml#/components/schemas/SuccessResponse'

components:
  schemas:
    ReleaseRouteRequestBody:
      allOf:
        - $ref: '../model/schema/common.schema.yaml#/components/schemas/NoParametersRequestSchema'
