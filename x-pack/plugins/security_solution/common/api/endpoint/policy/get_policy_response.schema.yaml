openapi: 3.0.0
info:
  title: Get Policy Response schema
  version: '2023-10-31'
paths: { }
components:
  parameters:
    PolicyResponseRequestQueryParameter:
      name: query
      in: query
      required: true
      schema:
        type: object
        properties:
          agentId:
            $ref: '../model/schema/common.yaml#/components/schemas/AgentId'

  schemas:
    SuccessResponse:
      type: object
      properties: {}
      # Define properties for the success response if needed
