openapi: 3.0.0
info:
  title: Get Action status schema
  version: '2023-10-31'
paths: { }
components:
  parameters:
    AgentPolicySummaryRequestQueryParameter:
      name: query
      in: query
      required: true
      schema:
        type: object
        properties:
          package_name:
            type: string
          policy_id:
            type: string
            nullable: true

  schemas:
    SuccessResponse:
      type: object
      properties: {}
      # Define properties for the success response if needed
