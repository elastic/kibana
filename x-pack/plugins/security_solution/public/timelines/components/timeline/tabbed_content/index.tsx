/*
 * Copyright Elasticsearch B.V. and/or licensed to Elasticsearch B.V. under one
 * or more contributor license agreements. Licensed under the Elastic License;
 * you may not use this file except in compliance with the Elastic License.
 */

// import { EuiTab } from '@elastic/eui';
// import React, { useCallback } from 'react';
// import styled from 'styled-components';

// const TabbedContentTabComponent = ({ id, name, isDisabled, isSelected, onClick }) => {
//   const handleClick = useCallback(() => onClick(id), [onClick, id]);

//   return (
//     <EuiTab onClick={handleClick} isSelected={isSelected} disabled={isDisabled}>
//       {name}
//     </EuiTab>
//   );
// };

// const TabbedContentTab = React.memo(TabbedContentTabComponent);

// const TabContentWrapper = styled.div<{ $isVisible }>`

// `

// const TabbedContentTabContent = ({ content, isSelected }) =>
