{
    "job_id": "JOB_ID",
    "indices": [
        "INDEX_PATTERN_NAME"
    ],
    "query": {
        "bool": {
            "must": [
                {"exists": {"field": "kubernetes.pod.uid"}},
                {"exists": {"field": "kubernetes.pod.memory"}}
            ]
        }
    }
}