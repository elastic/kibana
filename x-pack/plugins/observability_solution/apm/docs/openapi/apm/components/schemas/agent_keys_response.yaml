type: object
properties:
  agentKey:
    type: object
    required:
      - id
      - name
      - api_key
      - encoded
    properties:
      expiration:
        type: integer
        format: int64
      id:
        type: string
      name:
        type: string
      api_key:
        type: string
      encoded:
        type: string
