in: query
name: owner
description: >
  A filter to limit the response to a specific set of applications. If this
  parameter is omitted, the response contains information about all the cases
  that the user has access to read.
schema:
  oneOf:
    - $ref: '../../entrypoint.yaml#/components/schemas/ownerProperty'
    - type: array
      items:
        $ref: '../../entrypoint.yaml#/components/schemas/ownerProperty'
example: cases