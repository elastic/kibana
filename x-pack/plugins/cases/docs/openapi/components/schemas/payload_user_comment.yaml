type: object
properties:
  comment:
    type: object
    properties:
      comment:
        type: string
      owner:
        $ref: 'owners.yaml'
      type:
        type: string
        enum:
          - user