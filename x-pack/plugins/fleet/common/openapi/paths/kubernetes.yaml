get:
  summary: Get K8s Full Agent Manifest
  tags: []
  responses:
    '200':
      description: OK
      content:
        application/json:
          schema:
            type: object
            properties:
              item:
                type: string
  operationId: get-full-k8s-manifest
  parameters:
  - schema:
      type: boolean
    name: download
    in: query
    required: false
  - schema:
      type: string
    name: fleetServer
    in: query
    required: false
  - schema:
      type: string
    name: enrolToken
    in: query
    required: false
