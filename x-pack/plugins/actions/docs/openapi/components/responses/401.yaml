description: Authorization information is missing or invalid.
content:
  application/json:
    schema:
      type:  object
      title: Unauthorized response
      properties:
        error:
          type: string
          examples:
            - Unauthorized
          enum:
            - Unauthorized
        message:
          type: string 
        statusCode:
          type: integer
          examples:
            - 401
          enum:
            - 401