title: Slack connector parameters
description: Test an action that sends a message when the connector uses a Slack webhook URL.
type: object
required:
  - message
properties:
  message:
    type: string
    description: The Slack message text, which cannot contain Markdown, images, or other advanced formatting.
