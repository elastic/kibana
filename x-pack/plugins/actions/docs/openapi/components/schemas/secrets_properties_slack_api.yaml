title: Connector secrets properties for a Slack API connector
description: Defines secrets for connectors when type is `.slack_api`.
required:
  - token
type: object
properties:
  token:
    type: string
    description: Slack bot user OAuth token.
