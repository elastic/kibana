# This file is generated by the @kbn/moon package. Any manual edits will be erased!
#  To extend this, write your extensions/overrides to 'moon.extend.yml'
#  then regenerate this file with: 'node scripts/regenerate_moon_projects --update --filter @kbn/logstash-plugin'

$schema: https://moonrepo.dev/schemas/project.json
id: '@kbn/logstash-plugin'
type: unknown
owners:
  defaultOwner: '@elastic/logstash'
toolchain:
  default: node
language: typescript
project:
  name: '@kbn/logstash-plugin'
  description: Moon project for @kbn/logstash-plugin
  channel: ''
  owner: '@elastic/logstash'
  metadata:
    sourceRoot: x-pack/platform/plugins/private/logstash
tags:
  - platform
  - plugin
  - prod
  - jest-unit-tests
fileGroups:
  src:
    - common/**/*
    - public/**/*
    - server/**/*
    - '!target/**/*'
tasks:
  jest:
    args:
      - '--config'
      - $projectRoot/jest.config.js
    inputs:
      - '@group(src)'
  jestCI:
    args:
      - '--config'
      - $projectRoot/jest.config.js
    inputs:
      - '@group(src)'
