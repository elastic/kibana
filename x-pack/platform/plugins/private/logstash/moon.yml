$schema: https://moonrepo.dev/schemas/project.json
id: '@kbn/logstash-plugin'
type: unknown
owners:
  defaultOwner: '@elastic/logstash'
toolchain:
  default: node
language: typescript
project:
  name: '@kbn/logstash-plugin'
  description: Moon project for @kbn/logstash-plugin
  channel: ''
  owner: '@elastic/logstash'
  metadata:
    sourceRoot: x-pack/platform/plugins/private/logstash
tags:
  - platform
  - plugin
  - prod
fileGroups:
  src:
    - common/**/*
    - public/**/*
    - server/**/*
    - '!target/**/*'
tasks:
  jest:
    args:
      - '--config'
      - $projectRoot/jest.config.js
    inputs:
      - '@group(src)'
  jestCI:
    args:
      - '--config'
      - $projectRoot/jest.config.js
    inputs:
      - '@group(src)'
