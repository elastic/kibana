import type { Logger } from '@kbn/core/server';
import type { BaseParams } from '@kbn/reporting-common/types';
export declare function checkParamsVersion(jobParams: BaseParams, logger: Logger): string;
