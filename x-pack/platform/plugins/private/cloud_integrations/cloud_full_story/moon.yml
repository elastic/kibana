# This file is generated by the @kbn/moon package. Any manual edits will be erased!
#  To extend this, write your extensions/overrides to 'moon.extend.yml'
#  then regenerate this file with: 'node scripts/regenerate_moon_projects.js --update --filter @kbn/cloud-full-story-plugin'

$schema: https://moonrepo.dev/schemas/project.json
id: '@kbn/cloud-full-story-plugin'
layer: unknown
owners:
  defaultOwner: '@elastic/kibana-core'
toolchains:
  default: node
language: typescript
project:
  title: '@kbn/cloud-full-story-plugin'
  description: Moon project for @kbn/cloud-full-story-plugin
  channel: ''
  owner: '@elastic/kibana-core'
  sourceRoot: x-pack/platform/plugins/private/cloud_integrations/cloud_full_story
dependsOn:
  - '@kbn/core'
  - '@kbn/cloud-plugin'
  - '@kbn/config-schema'
tags:
  - plugin
  - prod
  - group-platform
  - private
  - jest-unit-tests
fileGroups:
  src:
    - .storybook/**/*
    - common/**/*
    - public/**/*
    - server/**/*
    - '!target/**/*'
tasks:
  jest:
    command: node
    args:
      - '--no-experimental-require-module'
      - $workspaceRoot/scripts/jest
      - '--config'
      - $projectRoot/jest.config.js
    options:
      runFromWorkspaceRoot: true
    inputs:
      - '@group(src)'
  jestCI:
    command: node
    args:
      - '--no-experimental-require-module'
      - $workspaceRoot/scripts/jest
      - '--config'
      - $projectRoot/jest.config.js
    options:
      runFromWorkspaceRoot: true
    inputs:
      - '@group(src)'
