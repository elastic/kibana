import React from 'react';
import type { FieldDataRowProps } from '../../types';
interface Props extends FieldDataRowProps {
    showIcon?: boolean;
}
export declare const DistinctValues: ({ showIcon, config }: Props) => React.JSX.Element | null;
export {};
