import React from 'react';
export declare const TotalCountHeader: ({ id, totalCount, approximate, loading, label, }: {
    id?: string;
    totalCount: number;
    label?: React.ReactElement | string;
    loading?: boolean;
    approximate?: boolean;
}) => React.JSX.Element;
