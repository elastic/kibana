/**
 * PATCH /internal/cases/templates/{template_id}
 * Partial update of a template (creates a new version)
 */
export declare const patchTemplateRoute: import("../types").CaseRoute<Readonly<{} & {
    template_id: string;
}>, unknown, unknown>;
