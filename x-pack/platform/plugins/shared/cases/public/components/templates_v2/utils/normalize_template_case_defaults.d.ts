export declare const normalizeTemplateCaseDefaultsForValidation: (definition: unknown) => unknown;
/**
 * Canonicalizes legacy top-level `title` into top-level `name` while preserving the rest of the
 * user's YAML formatting/comments.
 */
export declare const normalizeTemplateCaseDefaultsYaml: (definitionYaml: string) => string;
