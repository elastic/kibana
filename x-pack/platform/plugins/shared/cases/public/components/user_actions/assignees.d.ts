import type { UserActionBuilder } from './types';
export declare const shouldAddComma: (index: number, arrayLength: number) => boolean;
export declare const shouldAddAnd: (index: number, arrayLength: number) => boolean;
export declare const createAssigneesUserActionBuilder: UserActionBuilder;
