export function CustomSelectionTable({ checkboxDisabledCheck, columns, currentPage, filterDefaultFields, filters, items, itemsPerPage, onTableChange, radioDisabledCheck, selectedIds, setCurrentPaginationData, singleSelection, sortableProperties, tableItemId, }: {
    checkboxDisabledCheck: any;
    columns: any;
    currentPage?: number | undefined;
    filterDefaultFields: any;
    filters: any;
    items: any;
    itemsPerPage?: number | undefined;
    onTableChange: any;
    radioDisabledCheck: any;
    selectedIds: any;
    setCurrentPaginationData: any;
    singleSelection: any;
    sortableProperties: any;
    tableItemId?: string | undefined;
}): React.JSX.Element;
export namespace CustomSelectionTable {
    namespace propTypes {
        let checkboxDisabledCheck: any;
        let columns: any;
        let currentPage: any;
        let filterDefaultFields: any;
        let filters: any;
        let items: any;
        let itemsPerPage: any;
        let onTableChange: any;
        let radioDisabledCheck: any;
        let selectedId: any;
        let setCurrentPaginationData: any;
        let singleSelection: any;
        let sortableProperties: any;
        let tableItemId: any;
    }
}
import React from 'react';
