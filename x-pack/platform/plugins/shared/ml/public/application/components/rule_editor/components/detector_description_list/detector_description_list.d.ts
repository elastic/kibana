export function DetectorDescriptionList({ job, detector, anomaly }: {
    job: any;
    detector: any;
    anomaly: any;
}): React.JSX.Element;
export namespace DetectorDescriptionList {
    namespace propTypes {
        let job: PropTypes.Validator<object>;
        let detector: PropTypes.Validator<object>;
        let anomaly: PropTypes.Validator<object>;
    }
}
import React from 'react';
import PropTypes from 'prop-types';
