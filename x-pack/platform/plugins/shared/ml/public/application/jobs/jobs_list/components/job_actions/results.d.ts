export function ResultLinks({ jobs }: {
    jobs: any;
}): React.JSX.Element;
export namespace ResultLinks {
    namespace propTypes {
        let jobs: PropTypes.Validator<any[]>;
    }
}
import React from 'react';
import type PropTypes from 'prop-types';
