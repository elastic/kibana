export function JsonPane({ job }: {
    job: any;
}): React.JSX.Element;
export namespace JsonPane {
    namespace propTypes {
        let job: PropTypes.Validator<object>;
    }
}
import React from 'react';
import PropTypes from 'prop-types';
