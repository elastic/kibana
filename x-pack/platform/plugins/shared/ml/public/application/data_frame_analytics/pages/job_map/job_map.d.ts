import type { FC } from 'react';
interface Props {
    defaultHeight?: number;
    analyticsId?: string;
    modelId?: string;
    forceRefresh?: boolean;
}
export declare const JobMap: FC<Props>;
export {};
