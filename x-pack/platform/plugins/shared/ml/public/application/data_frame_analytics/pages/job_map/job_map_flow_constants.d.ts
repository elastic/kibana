/** Dagre / React Flow node dimensions (must match custom node outer size). */
export declare const JOB_MAP_NODE_WIDTH = 200;
export declare const JOB_MAP_NODE_HEIGHT = 104;
export declare const JOB_MAP_RANK_SEPARATION = 72;
export declare const JOB_MAP_NODE_SEPARATION = 48;
export declare const JOB_MAP_GRAPH_MARGIN = 20;
/** Bottom padding (px) subtracted from the available height when sizing the canvas. */
export declare const JOB_MAP_CANVAS_BOTTOM_PADDING = 20;
/** Edge stroke width in pixels. */
export declare const JOB_MAP_EDGE_STROKE_WIDTH = 1;
/** Arrow marker size (width and height) in pixels. */
export declare const JOB_MAP_EDGE_MARKER_SIZE = 12;
/**
 * Legacy node type for Kibana index patterns.
 * Not included in JOB_MAP_NODE_TYPES in the shared package for back-compat reasons.
 */
export declare const JOB_MAP_INDEX_PATTERN_TYPE: "index-pattern";
