export function ProgressIcon({ state }: {
    state: any;
}): React.JSX.Element | undefined;
export namespace ProgressIcon {
    namespace propTypes {
        let state: PropTypes.Requireable<number>;
    }
}
import React from 'react';
import type PropTypes from 'prop-types';
