responses:
  200:
    description: "Indicates a successful response"
    content:
      application/json:
        examples:
          getSmlRuleByIdResponseExample:
            description: "Example response returning a single SML rule"
            value:
              id: my-rule-id
              name: Summarize logs index
              type: index
              index_pattern: logs-*
              inference_id: my-chat-completion-endpoint
              prompt: "Summarize the following index metadata:\n${variables.mapping}"
              variables:
                mapping:
                  type: index
                  input: _mapping
              created_at: "2026-04-06T12:00:00.000Z"
              updated_at: "2026-04-06T12:00:00.000Z"
x-codeSamples:
- lang: curl
  source: |
    curl \
      -X GET "${KIBANA_URL}/api/agent_builder/sml/rule/{ruleId}" \
      -H "Authorization: ApiKey ${API_KEY}"
- lang: Console
  source: |
    GET kbn:/api/agent_builder/sml/rule/{ruleId}
