import React from 'react';
import type { ToolFormMode } from '../tool_form';
export interface DetailsProps {
    mode: ToolFormMode;
}
export declare const DetailsSection: ({ mode }: DetailsProps) => React.JSX.Element;
