import React from 'react';
import type { ToolFormMode } from '../tool_form';
export interface TypeProps {
    mode: ToolFormMode;
}
export declare const TypeSection: ({ mode }: TypeProps) => React.JSX.Element;
