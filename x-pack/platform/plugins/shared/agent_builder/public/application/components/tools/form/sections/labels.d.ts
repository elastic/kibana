import React from 'react';
import type { ToolFormMode } from '../tool_form';
export interface LabelsProps {
    mode: ToolFormMode;
}
export declare const LabelsSection: ({ mode }: LabelsProps) => React.JSX.Element;
