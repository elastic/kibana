import React from 'react';
export declare function InfoBadge({ title, dataView, index, palette, children, 'data-test-subj-prefix': dataTestSubjPrefix, }: {
    title: string;
    dataView: string;
    index: number;
    palette?: string[];
    children?: React.ReactNode;
    'data-test-subj-prefix': string;
}): React.JSX.Element;
