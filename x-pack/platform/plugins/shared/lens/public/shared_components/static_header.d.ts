import React from 'react';
import type { IconType } from '@elastic/eui';
export declare const StaticHeader: ({ label, icon, indicator, }: {
    label: string;
    icon?: IconType;
    indicator?: React.ReactNode;
}) => React.JSX.Element;
