$schema: https://moonrepo.dev/schemas/project.json
id: '@kbn/aiops-plugin'
type: unknown
owners:
  defaultOwner: '@elastic/ml-ui'
toolchain:
  default: node
language: typescript
project:
  name: '@kbn/aiops-plugin'
  description: Moon project for @kbn/aiops-plugin
  channel: ''
  owner: '@elastic/ml-ui'
  metadata:
    sourceRoot: x-pack/platform/plugins/shared/aiops
tags:
  - platform
  - plugin
  - prod
fileGroups:
  src:
    - public/**/*
    - scripts/**/*
    - server/**/*
    - types/**/*
    - '!target/**/*'
tasks:
  jest:
    args:
      - '--config'
      - $projectRoot/jest.config.js
    inputs:
      - '@group(src)'
  jestCI:
    args:
      - '--config'
      - $projectRoot/jest.config.js
    inputs:
      - '@group(src)'
