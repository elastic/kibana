import React from 'react';
import type { Space } from '../../../common';
interface Props {
    space: Space;
    serverBasePath: string;
}
export declare const SpaceCard: (props: Props) => React.JSX.Element;
export {};
