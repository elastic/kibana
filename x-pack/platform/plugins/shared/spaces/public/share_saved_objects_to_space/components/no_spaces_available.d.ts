import React from 'react';
import type { ApplicationStart } from '@kbn/core/public';
interface Props {
    application: ApplicationStart;
}
export declare const NoSpacesAvailable: (props: Props) => React.JSX.Element | null;
export {};
