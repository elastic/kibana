import type { Space } from '.';
/**
 * Returns whether the given Space is reserved or not.
 *
 * @param space the space
 * @returns boolean
 */
export declare function isReservedSpace(space?: Partial<Space> | null): boolean;
