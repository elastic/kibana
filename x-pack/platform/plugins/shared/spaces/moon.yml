# This file is generated by the @kbn/moon package. Any manual edits will be erased!
#  To extend this, write your extensions/overrides to 'moon.extend.yml'
#  then regenerate this file with: 'node scripts/regenerate_moon_projects --update --filter @kbn/spaces-plugin'

$schema: https://moonrepo.dev/schemas/project.json
id: '@kbn/spaces-plugin'
type: unknown
owners:
  defaultOwner: '@elastic/kibana-security'
toolchain:
  default: node
language: typescript
project:
  name: '@kbn/spaces-plugin'
  description: Moon project for @kbn/spaces-plugin
  channel: ''
  owner: '@elastic/kibana-security'
  metadata:
    sourceRoot: x-pack/platform/plugins/shared/spaces
tags:
  - platform
  - plugin
  - prod
  - jest-unit-tests
fileGroups:
  src:
    - common/**/*
    - public/**/*
    - server/**/*
    - '!target/**/*'
tasks:
  jest:
    args:
      - '--config'
      - $projectRoot/jest.config.js
    inputs:
      - '@group(src)'
  jestCI:
    args:
      - '--config'
      - $projectRoot/jest.config.js
    inputs:
      - '@group(src)'
