import type { RouteDefinitionParams } from '.';
/**
 * Defines routes that are used for encryption key rotation.
 */
export declare function defineKeyRotationRoutes({ encryptionKeyRotationService, router, logger, config, buildFlavor, }: RouteDefinitionParams): void;
