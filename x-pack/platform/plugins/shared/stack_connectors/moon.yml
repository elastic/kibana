# This file is generated by the @kbn/moon package. Any manual edits will be erased!
#  To extend this, write your extensions/overrides to 'moon.extend.yml'
#  then regenerate this file with: 'node scripts/regenerate_moon_projects --update --filter @kbn/stack-connectors-plugin'

$schema: https://moonrepo.dev/schemas/project.json
id: '@kbn/stack-connectors-plugin'
type: unknown
owners:
  defaultOwner: '@elastic/response-ops'
toolchain:
  default: node
language: typescript
project:
  name: '@kbn/stack-connectors-plugin'
  description: Moon project for @kbn/stack-connectors-plugin
  channel: ''
  owner: '@elastic/response-ops'
  metadata:
    sourceRoot: x-pack/platform/plugins/shared/stack_connectors
tags:
  - platform
  - plugin
  - prod
  - jest-unit-tests
fileGroups:
  src:
    - __mocks__/**/*
    - server/**/*
    - server/**/*.json
    - common/**/*
    - public/**/*
    - '!target/**/*'
tasks:
  jest:
    args:
      - '--config'
      - $projectRoot/jest.config.js
    inputs:
      - '@group(src)'
  jestCI:
    args:
      - '--config'
      - $projectRoot/jest.config.js
    inputs:
      - '@group(src)'
