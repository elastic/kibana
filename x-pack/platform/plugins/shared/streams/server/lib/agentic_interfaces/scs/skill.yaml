# Semantic Code Search
# Research how code works using Semantic Code Search (SCS). Finds symbols, traces data flows, and presents concise findings with file paths and line numbers.

id: scs-semantic-code-search
name: Semantic Code Search
description: Research how code works using Semantic Code Search (SCS). Finds symbols, traces data flows, and presents concise findings with file paths and line numbers.

tool_ids:
  - scs.discover_directories
  - scs.map_symbols
  - scs.semantic_search
  - scs.symbol_analysis
  - scs.read_file_from_chunks
