import React from 'react';
import type { MapSettings } from '../../../common/descriptor_types';
export interface Props {
    settings: MapSettings;
}
export declare function RightSideControls({ settings }: Props): React.JSX.Element;
