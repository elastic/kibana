import React from 'react';
interface Props {
    alpha: number;
    onChange: (alpha: number) => void;
}
export declare function AlphaSlider({ alpha, onChange }: Props): React.JSX.Element;
export {};
