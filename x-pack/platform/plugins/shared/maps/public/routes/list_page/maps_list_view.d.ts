import React from 'react';
import type { ScopedHistory } from '@kbn/core/public';
interface Props {
    history: ScopedHistory;
}
export declare const MapsListView: React.MemoExoticComponent<({ history }: Props) => React.JSX.Element>;
export {};
