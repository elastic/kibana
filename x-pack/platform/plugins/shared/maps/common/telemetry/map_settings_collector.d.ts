import type { MapAttributes } from '../../server';
export declare class MapSettingsCollector {
    private _customIconsCount;
    constructor(attributes: MapAttributes);
    getCustomIconsCount(): number;
}
