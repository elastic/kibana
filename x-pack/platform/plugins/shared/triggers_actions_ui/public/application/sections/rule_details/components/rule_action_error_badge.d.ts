import React from 'react';
export interface RuleActionErrorBadge {
    totalErrors: number;
    showIcon?: boolean;
}
export declare const RuleActionErrorBadge: (props: RuleActionErrorBadge) => React.JSX.Element;
