$schema: https://moonrepo.dev/schemas/project.json
id: '@kbn/task-manager-plugin'
type: unknown
owners:
  defaultOwner: '@elastic/response-ops'
toolchain:
  default: node
language: typescript
project:
  name: '@kbn/task-manager-plugin'
  description: Moon project for @kbn/task-manager-plugin
  channel: ''
  owner: '@elastic/response-ops'
  metadata:
    sourceRoot: x-pack/platform/plugins/shared/task_manager
tags:
  - platform
  - plugin
  - prod
fileGroups:
  src:
    - server/**/*
    - server/**/*.json
    - common/**/*
    - '!target/**/*'
tasks:
  jest:
    args:
      - '--config'
      - $projectRoot/jest.config.js
    inputs:
      - '@group(src)'
  jestCI:
    args:
      - '--config'
      - $projectRoot/jest.config.js
    inputs:
      - '@group(src)'
