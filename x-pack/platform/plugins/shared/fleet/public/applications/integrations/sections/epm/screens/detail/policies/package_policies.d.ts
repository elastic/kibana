import React from 'react';
import type { PackageInfo } from '../../../../../types';
export declare const PackagePoliciesPage: ({ packageInfo, embedded, }: {
    packageInfo: PackageInfo;
    embedded?: boolean;
}) => React.JSX.Element;
