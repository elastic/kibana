import React, { type ReactNode } from 'react';
interface ControlsColumnProps {
    controls: ReactNode;
    title: string | undefined;
}
export declare const ControlsColumn: ({ controls, title }: ControlsColumnProps) => React.JSX.Element;
export {};
