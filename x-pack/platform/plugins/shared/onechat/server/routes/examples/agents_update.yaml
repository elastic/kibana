responses:
  200:
    content:
      application/json:
        examples:
          updateAgentResponseExample:
            id: created-agent-id
            type: chat
            name: Search Index Helper
            description: Search for anything in "content-*" indices!
            labels:
            - custom-indices
            - department-search
            - elastic-employees
            avatar_color: "#BFDBFF"
            avatar_symbol: SI
            configuration:
              instructions: You are a custom agent that wants to help searching data using all
                indices starting with prefix "content-".
              tools:
              - tool_ids:
                - platform.core.search
                - platform.core.list_indices
                - platform.core.get_index_mapping
                - platform.core.get_document_by_id