# SUM

The SUM function calculates the total of a numeric expression.

## Syntax

`SUM(number)`

### Parameters

#### number

The numeric expression to be summed. Inline functions can be used within this parameter.

## Examples

Calculates the total value of the `languages` column across all employees.

```esql
FROM employees
| STATS SUM(languages)
```

Calculates the sum of each employee’s maximum salary change, aggregating the results into a `total_salary_changes` value.

```esql
FROM employees
| STATS total_salary_changes = SUM(MV_MAX(salary_change))
```