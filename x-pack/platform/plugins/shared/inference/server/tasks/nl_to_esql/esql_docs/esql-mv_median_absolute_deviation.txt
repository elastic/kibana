Calculates the median absolute deviation and the median for the array `[0, 2, 5, 6]`, storing the results in new columns.

```esql
ROW values = [0, 2, 5, 6]
| EVAL median_absolute_deviation = MV_MEDIAN_ABSOLUTE_DEVIATION(values), median = MV_MEDIAN(values)
```