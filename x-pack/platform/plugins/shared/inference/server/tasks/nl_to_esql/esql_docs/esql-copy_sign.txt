Calculates a new field `cs1` for each employee, combining the magnitude of their `salary` with the sign of the smallest value in their `salary_change`.

```esql
FROM employees
| EVAL cs1 = COPY_SIGN(salary, LEAST(salary_change))
```