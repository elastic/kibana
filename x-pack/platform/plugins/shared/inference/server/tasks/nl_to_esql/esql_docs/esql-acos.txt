Calculate the arccosine of 0.9 and store the result in a new column called `acos`. This query creates a single row with the value 0.9 in column `a`, then computes the arccosine of `a` and saves it in the column `acos`.

```esql
ROW a=.9
| EVAL acos=ACOS(a)
```