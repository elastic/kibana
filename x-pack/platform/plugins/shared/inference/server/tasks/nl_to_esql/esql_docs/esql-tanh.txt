# TANH

Returns the hyperbolic tangent of a number.

**Syntax**
`TANH(number)`

### Parameters

#### `number`

Numeric expression. If `null`, the function returns `null`.

**Supported types**

**Example**
```esql
ROW a=1.8
| EVAL tanh=TANH(a)
```

