Calculates the 50th percentile (median) of the array `[5, 5, 10, 12, 5000]` using `MV_PERCENTILE` and compares it to the result from `MV_MEDIAN`.

```esql
ROW values = [5, 5, 10, 12, 5000]
| EVAL p50 = MV_PERCENTILE(values, 50), median = MV_MEDIAN(values)
```