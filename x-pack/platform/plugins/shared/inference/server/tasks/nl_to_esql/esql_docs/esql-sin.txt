# SIN

Returns the sine of an angle.

**Syntax**
`SIN(angle)`

### Parameters

#### `angle`

An angle, in radians. If `null`, the function returns `null`.

**Supported types**

**Example**
```esql
ROW a=1.8
| EVAL sin=SIN(a)
```

