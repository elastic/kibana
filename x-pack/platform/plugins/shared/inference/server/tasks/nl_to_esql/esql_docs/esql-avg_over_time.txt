Calculates the maximum average network cost over each 1-minute interval, grouped by cluster.

```esql
TS k8s
| STATS max_cost=MAX(AVG_OVER_TIME(network.cost)) BY cluster, time_bucket = TBUCKET(1minute)
```