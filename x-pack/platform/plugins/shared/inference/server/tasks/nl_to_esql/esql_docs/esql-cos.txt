# COS

Returns the cosine of an angle.

**Syntax**
`COS(angle)`

### Parameters

#### `angle`

An angle, in radians. If `null`, the function returns `null`.

**Supported types**

**Example**
```esql
ROW a=1.8
| EVAL cos=COS(a)
```

