# SPACE

Returns a string consisting of a specified number of space characters.

## Syntax

`SPACE(number)`

### Parameters

#### number

The number of spaces to include in the result string.

## Examples

Creates a string "Hello World!" by inserting one space between "Hello" and "World!".

```esql
ROW message = CONCAT("Hello", SPACE(1), "World!");
```