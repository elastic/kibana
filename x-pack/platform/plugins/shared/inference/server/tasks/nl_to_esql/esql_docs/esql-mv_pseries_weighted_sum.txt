Calculates the weighted sum of the array `[70.0, 45.0, 21.0, 21.0, 21.0]` using a P-Series with `p = 1.5` and returns the result in the `sum` column.

```esql
ROW a = [70.0, 45.0, 21.0, 21.0, 21.0]
| EVAL sum = MV_PSERIES_WEIGHTED_SUM(a, 1.5)
| KEEP sum
```