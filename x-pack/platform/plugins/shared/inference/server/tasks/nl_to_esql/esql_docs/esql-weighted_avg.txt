Calculates the weighted average salary for each language group, using employee height as the weighting factor, rounds the result, and displays the average alongside the language.

```esql
FROM employees
| STATS w_avg = WEIGHTED_AVG(salary, height) BY languages
| EVAL w_avg = ROUND(w_avg)
| KEEP w_avg, languages
| SORT languages
```