# ST_ENVELOPE

Determines the minimum bounding box of the supplied geometry.

## Syntax

`ST_ENVELOPE(geometry, point, point, point, point)`

### Parameters

#### geometry

Expression of type `geo_point`, `geo_shape`, `cartesian_point`, or `cartesian_shape`. If `null`, the function returns `null`.

## Examples

The following query calculates the minimum bounding box (envelope) for the city boundary of the airport with abbreviation "CPH", and returns the abbreviation, airport name, and the computed envelope:

```esql
FROM airport_city_boundaries
| WHERE abbrev == "CPH"
| EVAL envelope = ST_ENVELOPE(city_boundary)
| KEEP abbrev, airport, envelope
```

# ST_XMAX

Extracts the maximum value of the `x` coordinates from the supplied geometry. For `geo_point` or `geo_shape`, this is equivalent to extracting the maximum longitude value.

## Syntax

`ST_XMAX(point)`

### Parameters

#### point

Expression of type `geo_point`, `geo_shape`, `cartesian_point`, or `cartesian_shape`. If `null`, the function returns `null`.

## Examples

This query extracts the minimum and maximum x (longitude) and y (latitude) coordinates from the bounding box of the city boundary for the airport with abbreviation "CPH". It returns the abbreviation, airport name, and the xmin, xmax, ymin, and ymax values:

```esql
FROM airport_city_boundaries
| WHERE abbrev == "CPH"
| EVAL envelope = ST_ENVELOPE(city_boundary)
| EVAL xmin = ST_XMIN(envelope), xmax = ST_XMAX(envelope), ymin = ST_YMIN(envelope), ymax = ST_YMAX(envelope)
| KEEP abbrev, airport, xmin, xmax, ymin, ymax
```

# ST_XMIN

Extracts the minimum value of the `x` coordinates from the supplied geometry. For `geo_point` or `geo_shape`, this is equivalent to extracting the minimum longitude value.

## Syntax

`ST_XMIN(point)`

### Parameters

#### point

Expression of type `geo_point`, `geo_shape`, `cartesian_point`, or `cartesian_shape`. If `null`, the function returns `null`.

## Examples

This query extracts the minimum x coordinate (xmin), along with the maximum x (xmax), minimum y (ymin), and maximum y (ymax) coordinates from the bounding box of the city boundary for the airport with abbreviation "CPH". It returns the abbreviation, airport name, and the coordinate values:

```esql
FROM airport_city_boundaries
| WHERE abbrev == "CPH"
| EVAL envelope = ST_ENVELOPE(city_boundary)
| EVAL xmin = ST_XMIN(envelope), xmax = ST_XMAX(envelope), ymin = ST_YMIN(envelope), ymax = ST_YMAX(envelope)
| KEEP abbrev, airport, xmin, xmax, ymin, ymax
```

# ST_YMAX

Extracts the maximum value of the `y` coordinates from the supplied geometry. For `geo_point` or `geo_shape`, this is equivalent to extracting the maximum latitude value.

## Syntax

`ST_YMAX(point)`

### Parameters

#### point

Expression of type `geo_point`, `geo_shape`, `cartesian_point`, or `cartesian_shape`. If `null`, the function returns `null`.

## Examples

This query extracts the maximum y coordinate (ymax), as well as the minimum x (xmin), maximum x (xmax), and minimum y (ymin) coordinates from the bounding box of the city boundary for the airport with abbreviation "CPH". It returns the abbreviation, airport name, and the coordinate values:

```esql
FROM airport_city_boundaries
| WHERE abbrev == "CPH"
| EVAL envelope = ST_ENVELOPE(city_boundary)
| EVAL xmin = ST_XMIN(envelope), xmax = ST_XMAX(envelope), ymin = ST_YMIN(envelope), ymax = ST_YMAX(envelope)
| KEEP abbrev, airport, xmin, xmax, ymin, ymax
```

# ST_YMIN

Extracts the minimum value of the `y` coordinates from the supplied geometry. For `geo_point` or `geo_shape`, this is equivalent to extracting the minimum latitude value.

## Syntax

`ST_YMIN(point)`

### Parameters

#### point

Expression of type `geo_point`, `geo_shape`, `cartesian_point`, or `cartesian_shape`. If `null`, the function returns `null`.

## Examples

This query extracts the minimum y coordinate (ymin), along with the minimum x (xmin), maximum x (xmax), and maximum y (ymax) coordinates from the bounding box of the city boundary for the airport with abbreviation "CPH". It returns the abbreviation, airport name, and the coordinate values:

```esql
FROM airport_city_boundaries
| WHERE abbrev == "CPH"
| EVAL envelope = ST_ENVELOPE(city_boundary)
| EVAL xmin = ST_XMIN(envelope), xmax = ST_XMAX(envelope), ymin = ST_YMIN(envelope), ymax = ST_YMAX(envelope)
| KEEP abbrev, airport, xmin, xmax, ymin, ymax
```
