Calculates the sum of the absolute change in events received between the last two data points, grouped by pod and 10-minute time intervals.

```esql
TS k8s
| STATS events = SUM(IDELTA(events_received)) by pod, time_bucket = TBUCKET(10minute)
```