Compute the SHA1 hash of the `message` column for all rows where the message is not "Connection error", and display both the original message and its hash.

```esql
FROM sample_data
| WHERE message != "Connection error"
| EVAL sha1 = sha1(message)
| KEEP message, sha1
```