Combines the first and last names of employees into a single 'fullname' field, separated by a space.

```esql
FROM employees
| KEEP first_name, last_name
| EVAL fullname = CONCAT(first_name, " ", last_name)
```