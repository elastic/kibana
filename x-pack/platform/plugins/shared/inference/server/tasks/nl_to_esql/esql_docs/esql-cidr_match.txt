Checks the `hosts` data for rows where the `ip1` address falls within either the `127.0.0.2/32` or `127.0.0.3/32` CIDR blocks, and then displays the `card`, `host`, `ip0`, and `ip1` columns.

```esql
FROM hosts
| WHERE CIDR_MATCH(ip1, "127.0.0.2/32", "127.0.0.3/32")
| KEEP card, host, ip0, ip1
```