# TAU

The TAU function returns the ratio of a circle’s circumference to its radius.

## Syntax

`TAU()`

### Parameters

This function does not take any parameters.

## Examples

The following query calculates the mathematical constant tau (τ), which is the ratio of a circle’s circumference to its radius (equal to 2π):

```esql
ROW TAU()
```
