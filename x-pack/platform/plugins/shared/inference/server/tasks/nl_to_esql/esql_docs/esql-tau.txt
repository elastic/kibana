# TAU

The TAU function returns the ratio of a circle’s circumference to its radius.

## Syntax

`TAU()`

### Parameters

This function does not take any parameters.

## Examples

The following query calculates the value of tau (τ), which is the ratio of a circle's circumference to its radius. The result is returned as a single row.

```esql
ROW TAU()
```