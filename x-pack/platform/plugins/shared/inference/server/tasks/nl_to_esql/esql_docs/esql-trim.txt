Removes leading and trailing spaces from the `message` and `color` columns in a single row.

```esql
ROW message = "   some text  ",  color = " red "
| EVAL message = TRIM(message)
| EVAL color = TRIM(color)
```