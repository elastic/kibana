import React from 'react';
interface Props {
    title: string;
    body: string;
    onClose: () => void;
}
export declare const EntityNotFoundFlyout: ({ title, body, onClose }: Props) => React.JSX.Element;
export {};
