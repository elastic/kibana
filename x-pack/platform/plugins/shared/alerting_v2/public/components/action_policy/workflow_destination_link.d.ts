import React from 'react';
interface WorkflowDestinationLinkProps {
    id: string;
    isEnabled?: boolean;
}
export declare const WorkflowDestinationLink: ({ id, isEnabled }: WorkflowDestinationLinkProps) => React.JSX.Element;
export {};
