/** Full `field=value · …` label, used as tooltip content. */
export declare const formatSeriesLabel: (groupHash: string, groupingValues: Record<string, string | null> | null | undefined) => string;
/** Values-only `value · …` label, used as the visible truncated text in the gantt column. */
export declare const formatSeriesLabelValues: (groupHash: string, groupingValues: Record<string, string | null> | null | undefined) => string;
