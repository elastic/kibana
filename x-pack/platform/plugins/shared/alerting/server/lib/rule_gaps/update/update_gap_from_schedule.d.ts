import type { Gap } from '../gap';
import type { ScheduledItem } from './utils';
export declare const updateGapFromSchedule: ({ gap, scheduledItems, }: {
    gap: Gap;
    scheduledItems: ScheduledItem[];
}) => Gap;
