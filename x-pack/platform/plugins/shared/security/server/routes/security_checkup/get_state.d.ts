import type { RouteDefinitionParams } from '..';
/**
 * Defines route that returns the state of the security checkup feature.
 */
export declare function defineSecurityCheckupGetStateRoutes({ router, logger, config$, license, }: RouteDefinitionParams): void;
