import React from 'react';
import type { Role } from '../../../../common';
interface Props {
    role: Role;
}
export declare const ReservedRoleBadge: (props: Props) => React.JSX.Element | null;
export {};
