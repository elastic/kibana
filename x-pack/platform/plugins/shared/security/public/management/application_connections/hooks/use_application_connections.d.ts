import type { ApplicationConnections } from '../constants/types';
export declare const useApplicationConnections: () => {
    applicationConnections: ApplicationConnections[];
    isLoading: boolean;
};
