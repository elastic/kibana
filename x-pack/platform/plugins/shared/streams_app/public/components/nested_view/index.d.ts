import React from 'react';
export declare function NestedView({ children, last, first, isBeingDragged, }: {
    children: React.ReactNode;
    last?: boolean;
    first?: boolean;
    isBeingDragged?: boolean;
}): React.JSX.Element;
