import React from 'react';
export declare const TableTitle: ({ pageIndex, pageSize, total, label, }: {
    pageIndex: number;
    pageSize: number;
    total: number;
    label: string;
}) => React.JSX.Element;
