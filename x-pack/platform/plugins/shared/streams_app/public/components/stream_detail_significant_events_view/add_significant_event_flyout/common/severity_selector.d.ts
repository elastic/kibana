import React from 'react';
export declare function SeveritySelector({ severityScore, onChange, disabled, }: {
    severityScore: number | undefined;
    onChange: (score: number | undefined) => void;
    disabled?: boolean;
}): React.JSX.Element;
