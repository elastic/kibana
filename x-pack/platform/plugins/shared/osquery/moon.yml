# This file is generated by the @kbn/moon package. Any manual edits will be erased!
#  To extend this, write your extensions/overrides to 'moon.extend.yml'
#  then regenerate this file with: 'node scripts/regenerate_moon_projects --update --filter @kbn/osquery-plugin'

$schema: https://moonrepo.dev/schemas/project.json
id: '@kbn/osquery-plugin'
type: unknown
owners:
  defaultOwner: '@elastic/security-defend-workflows'
toolchain:
  default: node
language: typescript
project:
  name: '@kbn/osquery-plugin'
  description: Moon project for @kbn/osquery-plugin
  channel: ''
  owner: '@elastic/security-defend-workflows'
  metadata:
    sourceRoot: x-pack/platform/plugins/shared/osquery
tags:
  - platform
  - plugin
  - prod
  - jest-unit-tests
fileGroups:
  src:
    - common/**/*
    - public/**/*
    - scripts/**/*
    - scripts/**/*.json
    - server/**/*
    - public/common/schemas/**/*.json
    - '!target/**/*'
tasks:
  jest:
    args:
      - '--config'
      - $projectRoot/jest.config.js
    inputs:
      - '@group(src)'
  jestCI:
    args:
      - '--config'
      - $projectRoot/jest.config.js
    inputs:
      - '@group(src)'
