/**
 * checks if the provided manifest version was a version where legacy semantic_text behavior was being used
 */
export declare const isLegacySemanticTextVersion: (manifestVersion: string) => boolean;
