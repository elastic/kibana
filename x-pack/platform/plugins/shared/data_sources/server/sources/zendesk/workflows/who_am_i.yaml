version: '1'
name: 'sources.zendesk.who_am_i'
description: |
  Get the currently authenticated Zendesk user (me). Returns the user record for the API credentials in use.
tags: ['agent-builder-tool']
enabled: true
triggers:
  - type: 'manual'
inputs: []
steps:
  - name: who-am-i
    type: zendesk.whoAmI
    connector-id: <%= zendesk-stack-connector-id %>
    with:
      {}
