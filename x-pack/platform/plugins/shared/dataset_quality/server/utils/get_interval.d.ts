export declare const getFieldIntervalInSeconds: ({ start, end, buckets, minIntervalSeconds, }: {
    start: number;
    end: number;
    buckets?: number;
    minIntervalSeconds?: number;
}) => number;
