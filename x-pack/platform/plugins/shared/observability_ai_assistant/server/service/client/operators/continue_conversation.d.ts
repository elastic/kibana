import type { Logger } from '@kbn/logging';
import type { Observable } from 'rxjs';
import type { AnalyticsServiceStart } from '@kbn/core/server';
import type { AssistantScope } from '@kbn/ai-assistant-common';
import { type InferenceConnector } from '@kbn/inference-common';
import type { Message } from '../../../../common';
import type { MessageOrChatEvent } from '../../../../common/conversation_complete';
import type { Instruction } from '../../../../common/types';
import type { ChatFunctionClient } from '../../chat_function_client';
import type { AutoAbortedChatFunction } from '../../types';
export declare function executeFunctionAndCatchError({ name, args, functionClient, messages, chat, signal, logger, connectorId, simulateFunctionCalling, analytics, connector, scopes, }: {
    name: string;
    args: string | undefined;
    functionClient: ChatFunctionClient;
    messages: Message[];
    chat: AutoAbortedChatFunction;
    signal: AbortSignal;
    logger: Logger;
    connectorId: string;
    simulateFunctionCalling: boolean;
    analytics: AnalyticsServiceStart;
    connector?: InferenceConnector;
    scopes: AssistantScope[];
}): Observable<MessageOrChatEvent>;
export declare function continueConversation({ messages: initialMessages, functionClient, chat, signal, functionCallsLeft, apiUserInstructions, kbUserInstructions, logger, disableFunctions, connectorId, simulateFunctionCalling, analytics, connector, scopes, }: {
    messages: Message[];
    functionClient: ChatFunctionClient;
    chat: AutoAbortedChatFunction;
    signal: AbortSignal;
    functionCallsLeft: number;
    apiUserInstructions: Instruction[];
    kbUserInstructions: Instruction[];
    logger: Logger;
    disableFunctions: boolean;
    connectorId: string;
    simulateFunctionCalling: boolean;
    analytics: AnalyticsServiceStart;
    connector?: InferenceConnector;
    scopes: AssistantScope[];
}): Observable<MessageOrChatEvent>;
