import React from 'react';
interface Props {
    error?: boolean;
    body?: React.ReactNode;
    controls?: React.ReactNode;
}
export declare function MessagePanel(props: Props): React.JSX.Element;
export {};
