# This file is generated by the @kbn/moon package. Any manual edits will be erased!
#  To extend this, write your extensions/overrides to 'moon.extend.yml'
#  then regenerate this file with: 'node scripts/regenerate_moon_projects.js --update --filter @kbn/rollup'

$schema: https://moonrepo.dev/schemas/project.json
id: '@kbn/rollup'
layer: unknown
owners:
  defaultOwner: '@elastic/kibana-management'
toolchains:
  default: node
language: typescript
project:
  title: '@kbn/rollup'
  description: Moon project for @kbn/rollup
  channel: ''
  owner: '@elastic/kibana-management'
  sourceRoot: x-pack/platform/packages/private/rollup
dependsOn:
  - '@kbn/i18n'
tags:
  - shared-common
  - package
  - prod
  - group-platform
  - private
  - jest-unit-tests
fileGroups:
  src:
    - '**/*.ts'
    - '**/*.tsx'
    - '!target/**/*'
tasks:
  jest:
    command: node
    args:
      - '--no-experimental-require-module'
      - $workspaceRoot/scripts/jest
      - '--config'
      - $projectRoot/jest.config.js
    options:
      runFromWorkspaceRoot: true
    inputs:
      - '@group(src)'
  jestCI:
    command: node
    args:
      - '--no-experimental-require-module'
      - $workspaceRoot/scripts/jest
      - '--config'
      - $projectRoot/jest.config.js
    options:
      runFromWorkspaceRoot: true
    inputs:
      - '@group(src)'
