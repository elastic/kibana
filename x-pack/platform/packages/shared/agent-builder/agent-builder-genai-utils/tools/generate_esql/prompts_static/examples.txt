Q: What are the 10 latest errors from the logs?
A:
```esql
FROM logs
| WHERE level == "ERROR"
| SORT @timestamp DESC
| LIMIT 10
```

Q: What are the titles and descriptions of blog articles published last month?
A:
```esql
FROM blogposts
| WHERE published > NOW() - 1 month
| KEEP title, description
| SORT title
| LIMIT 100
```

Q: How many employees are from the Netherlands?
A:
```esql
FROM employees
| WHERE country == "NL"
| STATS COUNT(*)
```

Q: How many orders were placed each month over the last year?
A:
```esql
FROM orders
| WHERE order_date > NOW() - 1 year
| STATS count = COUNT(*) BY date_bucket = BUCKET(order_date, 1 month)
```

Q: Extract the date, message and IP address from the logs using DISSECT
A:
```esql
FROM postgres-logs*
| DISSECT message "%{date} - %{msg} - %{ip}"
| KEEP date, msg, ip
| EVAL date = DATE_PARSE("yyyy-MM-dd'T'HH:mm:ss.SSS'Z'", date)
| LIMIT 100
```

Q: Find contributors whose first name starts with "b", sort by number of commits, return top 5 first and last names.
A:
```esql
FROM commits
| WHERE TO_LOWER(first_name) LIKE "b*"
| STATS doc_count = COUNT(*) by first_name, last_name
| SORT doc_count DESC
| KEEP first_name, last_name
| LIMIT 5
```

Q: What is the average salary of employees hired in 1985 split in 20 buckets?
A:
```esql
FROM employees
| WHERE hire_date >= "1985-01-01T00:00:00Z" AND hire_date < "1986-01-01T00:00:00Z"
| STATS avg_salary = AVG(salary) BY date_bucket = BUCKET(hire_date, 20, "1985-01-01T00:00:00Z", "1986-01-01T00:00:00Z")
| SORT bucket
```

Q: How many employees were hired in 1985 and what is their salary distribution?
A:
```esql
FROM employees
| WHERE hire_date >= "1985-01-01T00:00:00Z" AND hire_date < "1986-01-01T00:00:00Z"
| STATS c = COUNT(1) BY b = BUCKET(salary, 5000.)
| SORT b
```

Q: How many employees were hired after 2023-01-01 and what is the recent hiring rate by country?
A:
```esql
FROM employees
| EVAL is_recent_hire = CASE(hire_date <= "2023-01-01T00:00:00Z", 1, 0)
| STATS total_recent_hires = SUM(is_recent_hire), total_hires = COUNT(*) BY country
| EVAL recent_hiring_rate = total_recent_hires / total_hires
```

Q: Calculate the failure rate per host over the past 24 hours
A:
```esql
FROM logs-*
| WHERE @timestamp <= NOW() - 24 hours
| EVAL is_5xx = CASE(http.response.status_code >= 500, 1, 0)
| STATS total_events = COUNT(*), total_failures = SUM(is_5xx) BY host.hostname, bucket = BUCKET(@timestamp, 1 hour)
| EVAL failure_rate_per_host = total_failures / total_events
| DROP total_events, total_failures
```

Q: How many logs are there for each log level in the last 24 hours?
A:
```esql
FROM logs-*
| WHERE @timestamp <= NOW() - 24 hours
| STATS count = COUNT(*) BY log.level
| SORT count DESC
```

Q: What is the min, max and average value of the numbers in the bag?
A:
```esql
FROM bag_of_numbers
| EVAL min = MV_MIN(numbers), max = MV_MAX(numbers), avg = MV_AVG(numbers)
| KEEP bad_id, min, max, avg
```

Q: What messages not containing the word "error" are in the logs?
A:
```esql
FROM logs
| WHERE message NOT LIKE "*error*"
| KEEP message
| LIMIT 100
```

Q: Find first 10 users with names starting with "A" or "B", sorted by name
A:
```esql
FROM users
| KEEP name
| WHERE name LIKE "A*" OR name LIKE "B*"
| SORT name
| LIMIT 10
```

Q: Get user names and birth dates, sorted by birth date
A:
```esql
FROM personal_info
| EVAL birth=DATE_PARSE("yyyy-MM-dd", birth_date)
| KEEP user_name, birth
| SORT birth
| LIMIT 100
```

Q: How many unique IP addresses are there from France, Germany and Spain?
A:
```esql
FROM users
| WHERE country IN (France, Germany, Spain)
| STATS COUNT_DISTINCT(user_ip)
```

Q: How many warning logs are there from the US?
A:
```esql
FROM logs
| WHERE message LIKE "*warning*" AND location RLIKE ".*us.*"
| STATS COUNT(*)
```

Q: Find books by authors whose names sound like "Frank Herbert"
A:
```esql
FROM library
| WHERE MATCH(author, "Frank Herbert")
| KEEP title, author
| LIMIT 100
```

Q: Find the latest articles from the "hr" category, return their titles and document ids
A:
```esql
FROM articles METADATA _id
| WHERE category == "hr"
| KEEP _id, title
| SORT publish_date DESC
| LIMIT 100
```

Q: Find products with a description matching "wireless headphones" and sort them by relevance
A:
```esql
FROM products METADATA _score
| WHERE MATCH(description, "wireless headphones")
| SORT _score DESC
| KEEP name, description, _score
| LIMIT 100
```

Q: Detect change points in CPU usage over the past week
A:
```esql
FROM metrics-*
| WHERE @timestamp > NOW() - 7 days
| STATS avg_cpu = AVG(system.cpu.total.norm.pct) BY BUCKET(@timestamp, 1 hour)
| CHANGE_POINT avg_cpu ON @timestamp
| WHERE type IS NOT NULL
| KEEP @timestamp, avg_cpu, type, pvalue
```

Q: Find when response times had significant changes in the API logs
A:
```esql
FROM api-logs
| WHERE @timestamp > NOW() - 30 days
| STATS avg_response_time = AVG(response_time_ms) BY BUCKET(@timestamp, 1 day)
| CHANGE_POINT avg_response_time
| WHERE type IS NOT NULL
| SORT @timestamp DESC
| KEEP @timestamp, avg_response_time, type, pvalue
```

Q: Detect spikes and dips in daily sales revenue
A:
```esql
FROM sales
| STATS daily_revenue = SUM(amount) BY date = BUCKET(order_date, 1 day)
| CHANGE_POINT daily_revenue ON date AS change_type, significance
| WHERE change_type IS NOT NULL
| KEEP date, daily_revenue, change_type, significance
| SORT date
```