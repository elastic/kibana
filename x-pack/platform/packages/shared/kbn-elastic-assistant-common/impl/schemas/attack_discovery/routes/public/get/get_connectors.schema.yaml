openapi: 3.0.0
info:
  title: Get attack discovery connectors API endpoint
  version: '2026-01-01'
paths:
  /api/attack_discovery/connectors:
    get:
      x-codegen-enabled: true
      x-labels: [ess, serverless]
      operationId: GetAttackDiscoveryConnectors
      description: Get the list of connectors used to generate attack discoveries. It includes also deleted connectors.
      summary: Get the list of connectors used to generate attack discoveries. It includes also deleted connectors.
      responses:
        200:
          description: Successful response
          content:
            application/json:
              schema:
                type: object
                required:
                  - connectorsNames
                properties:
                  connectorsNames:
                    type: array
                    description: List of connectors names
                    items:
                      type: string
        400:
          description: Bad request
          content:
            application/json:
              schema:
                type: object
                properties:
                  status_code:
                    type: number
                    description: HTTP status code
                    example: 400
                  error:
                    type: string
                    description: Error type
                    example: "Bad Request"
                  message:
                    type: string
                    description: Human-readable error message
                    example: "Invalid size parameter. Must be a positive number."
