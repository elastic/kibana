export declare const AI_IS_CURRENTLY_ANALYZING: (alertsCount: number) => string;
export declare const AI_IS_CURRENTLY_ANALYZING_FROM: ({ alertsCount, from, }: {
    alertsCount: number;
    from: string;
}) => string;
export declare const AI_IS_CURRENTLY_ANALYZING_RANGE: ({ alertsCount, end, start, }: {
    alertsCount: number;
    end: string;
    start: string;
}) => string;
