import { monaco } from '@kbn/monaco';
import type { StepDecoration } from '../types';
export declare const useStepDecorations: (editor: monaco.editor.IStandaloneCodeEditor | null, decorations: StepDecoration[]) => {
    styles: import("@emotion/utils").SerializedStyles;
};
