# This file is generated by the @kbn/moon package. Any manual edits will be erased!
#  To extend this, write your extensions/overrides to 'moon.extend.yml'
#  then regenerate this file with: 'node scripts/regenerate_moon_projects.js --update --filter @kbn/mcp-client'

$schema: https://moonrepo.dev/schemas/project.json
id: '@kbn/mcp-client'
layer: unknown
owners:
  defaultOwner: '@elastic/workchat-eng'
toolchains:
  default: node
language: typescript
project:
  title: '@kbn/mcp-client'
  description: Moon project for @kbn/mcp-client
  channel: ''
  owner: '@elastic/workchat-eng'
  sourceRoot: x-pack/platform/packages/shared/kbn-mcp-client
dependsOn:
  - '@kbn/core'
  - '@kbn/logging-mocks'
tags:
  - shared-common
  - package
  - prod
  - group-platform
  - shared
  - jest-unit-tests
fileGroups:
  src:
    - '**/*.ts'
    - '!target/**/*'
tasks:
  jest:
    command: node
    args:
      - '--no-experimental-require-module'
      - $workspaceRoot/scripts/jest
      - '--config'
      - $projectRoot/jest.config.js
    options:
      runFromWorkspaceRoot: true
    inputs:
      - '@group(src)'
  jestCI:
    command: node
    args:
      - '--no-experimental-require-module'
      - $workspaceRoot/scripts/jest
      - '--config'
      - $projectRoot/jest.config.js
    options:
      runFromWorkspaceRoot: true
    inputs:
      - '@group(src)'
