$schema: https://moonrepo.dev/schemas/project.json
id: '@kbn/inference-langchain'
type: unknown
owners:
  defaultOwner: '@elastic/appex-ai-infra'
toolchain:
  default: node
language: typescript
project:
  name: '@kbn/inference-langchain'
  description: Moon project for @kbn/inference-langchain
  channel: ''
  owner: '@elastic/appex-ai-infra'
  metadata:
    sourceRoot: x-pack/platform/packages/shared/ai-infra/inference-langchain
dependsOn:
  - '@kbn/inference-common'
  - '@kbn/zod'
  - '@kbn/logging'
  - '@kbn/logging-mocks'
tags:
  - platform
  - shared-common
  - prod
fileGroups:
  src:
    - '**/*.ts'
    - '!target/**/*'
tasks:
  jest:
    args:
      - '--config'
      - $projectRoot/jest.config.js
    inputs:
      - '@group(src)'
  jestCI:
    args:
      - '--config'
      - $projectRoot/jest.config.js
    inputs:
      - '@group(src)'
