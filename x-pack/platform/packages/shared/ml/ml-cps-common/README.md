# @kbn/ml-cps-common

Cross-project search (CPS) helpers for ML anomaly detection jobs that are usable from both
browser and server code, such as resolving the effective `project_routing` of a job's datafeed.
