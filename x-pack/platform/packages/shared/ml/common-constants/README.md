# @kbn/ml-common-constants

This package contains runtime constants shared by the ML plugin across both server and browser. These include plugin identifiers and route paths, the plugin config schema and feature flag helpers, anomaly-detection and jobs-health rule type identifiers, and case attachment type identifiers.
