/*
 * Copyright Elasticsearch B.V. and/or licensed to Elasticsearch B.V. under one
 * or more contributor license agreements. Licensed under the Elastic License
 * 2.0; you may not use this file except in compliance with the Elastic License
 * 2.0.
 */

// Verbatim code-defined prompts from deductive-ai/deductive at a60f80265f5ebe5c4ce9be02b0a158be00e2d58c.

export const PLAN_EXTRACTION_SYSTEM_PROMPT =
  'You are tasked with extracting a reusable investigation decision trace from a completed investigation.\n\nThis trace should NOT include specific findings or outcomes from this investigation, but rather capture the methodology that was effective so it can be reapplied to similar problems in the future.\n\nOutput the plan in markdown format.\n\n## CRITICAL: Keep the Graph Small\n\nThe decision trace MUST be compact, generic, and reusable. **Aim for 5-12 nodes total.** A good trace captures the *pattern* of investigation, not every individual step.\n\n### Abstraction Rules\n\n- **One Evidence node per distinct operation type**, not per API call.   3 Prometheus label-value queries = one `Query Prometheus labels` node.   But querying Prometheus metrics and querying Loki logs are different operations.\n- **Labels must name the data source AND operation.**   GOOD: `Query Prometheus labels`, `Search Slack messages`, `List Grafana alert rules`   BAD: `Gather Context`, `Process Data`, `Get Info`, `Fetch resource by ID`\n- **One Symptom node per question category**, not per question.\n- **Merge similar decisions.** "Did API return data?" and "Did query succeed?"   are the same decision node.\n- **Strip specific values from graph labels** — no metric names, PR numbers, channel names, timestamps, or one-off IDs in node labels.\n- **Preserve key query entities in evidence metadata** — metric names, Prometheus labels,\n  Elasticsearch/Lucene keywords, log indexes, Kubernetes namespaces/resources, service names, filters, and similar\n  concrete query details should appear in `evidence_gatherer_metadata` for the corresponding E-node. Preserve the\n  exact reusable query entities from the investigation/tool calls whenever they are available.\n\n## Decision Trace Node Types\n\nThe decision trace is a directed graph with exactly **4 node types**:\n\n1. **Symptom / Question** — Describes the observable symptom, alert condition, or user question that initiated the investigation (e.g. "High error rate on /api/checkout", "Pod restarts exceeding threshold", "Why is checkout latency high?"). These are the starting points of the trace.\n\n2. **Evidence Gatherer** — A step that gathers evidence from a data source. Group related queries of the same type into one node (e.g. multiple Prometheus label queries = one node). But keep different operation types separate (querying metrics vs searching logs vs listing resources).\n\n3. **Decision** — A reasoning step that uses the gathered evidence to draw a conclusion, choose a branch, or form a hypothesis. Decision nodes should state the question being answered and have 2+ outgoing edges with condition labels describing the possible outcomes.\n\n4. **End** — A terminal node representing the conclusion of an investigation path (e.g. root cause identified, issue resolved, escalated). End nodes have no outgoing edges.\n\n## Mermaid Decision Trace Format\n\nThe decision trace MUST follow this exact format for parsing:\n\n1. **Start with**: `flowchart TD` (top-down flowchart)\n\n2. **Node ID format**: Use short alphanumeric IDs prefixed by node type: `S` for Symptom/Question, `E` for Evidence Gatherer, `D` for Decision, `X` for End (e.g. S1, S2, E1, E2, D1, D2, X1, X2).\n\n3. **Node shapes by type** (MUST use these consistently):\n   - `([label])` — **Symptom / Question nodes**: Observable symptoms, alert conditions, or user questions\n   - `[label]` — **Evidence Gatherer nodes**: A single data-gathering step\n   - `{{label}}` — **Decision nodes**: Reasoning steps with branching outcomes\n   - `((label))` — **End nodes**: Terminal conclusions (root cause found, resolved, escalated)\n\n4. **Labels** — Keep node labels short (2-6 words). Put exact reusable query entities from the    investigation/tool calls (metric names, labels, log keywords, indexes, filters, namespaces, resources,    and service names) in `evidence_gatherer_metadata`.\n\n5. **Edge format**:\n   - Simple: `S1 --> E1`\n   - With condition label: `D1 -->|error_rate > 5%| E3` or `D1 -->|error_rate normal| E4`\n   - Decision node outgoing edges MUST have condition labels\n   - Do NOT include parentheses `()` or brackets `[]` in edge labels; use commas or hyphens instead\n\n6. **Rules**:\n   - Each node must have a unique ID\n   - Define each node (with its shape and label) EXACTLY ONCE. After a node is defined, refer to it by its ID only (e.g. `E3 --> D2` not `E3 --> D2{{label again}}`).\n   - Symptom/Question nodes (`([...])`) are entry points — they have no incoming edges\n   - Decision nodes (`{{...}}`) must have 2+ outgoing edges with condition labels\n   - End nodes (`((...))`) are terminal — they have no outgoing edges\n   - Keep labels concise (2-6 words); exact reusable query entities belong in `evidence_gatherer_metadata`\n   - Avoid special characters in labels that conflict with Mermaid syntax. In particular, do not use parentheses `()` inside labels — rephrase instead (e.g. "Which apps drove load increase" not "Which application(s) drove the load increase").\n   - Mark edges on the path actually taken during the investigation by prefixing the edge label with `✅` (e.g. `-->|✅ error_code starts with DB|`). Edges NOT taken should have no prefix. For simple edges (no condition label) on the taken path, add `-->|✅|`.\n   - Do NOT use `linkStyle`, `style`, `classDef`, subgraphs, or any other Mermaid styling/directives. Only `flowchart TD`, nodes, and edges.\n   - The trace should reflect the actual conversation flow: Symptoms/Questions lead to Evidence Gathering, which feeds into Decisions, which may lead to further Evidence Gathering or terminate at an End node.\n\n## Example Mermaid Diagram\n\nThe diagram must start directly with `flowchart TD` — no code fences, no triple backticks:\n\nflowchart TD\n    S1([High error rate on API]) -->|✅| E1[Query Loki error logs]\n    S2([Increased P99 latency]) --> E2[Query Prometheus latency metrics]\n    E1 -->|✅| D1{{Database-related errors?}}\n    E2 --> D1\n    D1 -->|✅ yes| E3[Query Prometheus DB pool metrics]\n    D1 -->|no| E4[Search Tempo traces for failures]\n    E3 -->|✅| D2{{Connection pool exhausted?}}\n    D2 -->|✅ yes| E5[Check GitHub recent deploys]\n    D2 -->|no| E5\n    E4 --> D3{{Memory issue?}}\n    D3 -->|high memory usage| E7[Check memory allocation trends]\n    D3 -->|memory normal| E8[Query upstream dependencies]\n    E5 -->|✅| D4{{Leak found in recent deploy?}}\n    D4 -->|✅ Yes| E9[Identify leaking code path]\n    D4 -->|No| E6\n    E9 -->|✅| X1((Connection leak in deploy))\n    E6 --> X2((Slow query performance))\n    E7 --> X3((Memory pressure from allocation growth))\n    E8 --> D5{{Is upstream degraded?}}\n    D5 -->|unhealthy| E10[Check upstream service logs]\n    D5 -->|all healthy| E11[Collect thread dump and heap snapshot]\n    E10 --> X4((Upstream service degradation))\n    E11 --> X5((Escalate with diagnostics))\n\n## Evidence Gatherer Metadata Format\n\nFor EACH evidence gatherer node (E1, E2, ...) in your decision tree, provide a string in the format `<node_id>: <description>` where description is a natural-language summary of the toolset/skill used, what data it gathers, and from which data sources. Include the exact reusable query entities needed to recreate the query, such as Prometheus metric names and labels, Elasticsearch/Lucene keywords, log indexes, Kubernetes namespaces/resources, service names, filters, and relevant time-window guidance. Copy these entities from the investigation/tool calls when available.\n\nExamples:\n- "E1: DatadogToolkit - Query container.cpu.usage and container.memory.usage grouped by pod and namespace for the alert time window"\n- "E2: ElasticsearchToolkit - Search application logs in the checkout-* index for service=checkout and keywords InvalidQueryException or timeout"\n- "E3: KubernetesToolkit - Check pod restart counts and warning events in namespace checkout-prod for pods matching checkout-*"\n';

export const BASE_TREE_TEMPLATE =
  "\n**CRITICAL — You MUST evolve the existing tree below, not replace it.**\n\nAn existing decision graph already exists. Your job is to merge the new conversation's methodology into it while keeping the graph SMALL.\n\nRules (in priority order):\n  1. **REUSE before creating.** Before adding ANY new node, scan every existing      node label. If an existing node covers the same concept (same API call,      same check, same decision — even with different wording), REUSE that node ID.      Only create a new node when NO existing node is even approximately equivalent.\n  2. **Keep ALL existing node IDs.** Do not remove, rename, or renumber them.\n  3. **Connect, don't duplicate.** If the conversation follows a path already      in the tree, just add the ✅ mark to the existing edges — do not create      parallel duplicate paths.\n  4. **Merge similar symptom/question nodes.** If the new conversation's entry      point is a variation of an existing symptom node, add an edge from the      existing symptom node instead of creating a new one.\n  5. **Target: fewer than 15 total nodes.** If the tree is approaching this      limit, aggressively merge similar nodes rather than adding new ones.      A 15-node graph that covers 80% of patterns is better than a 50-node      graph that covers 100%.\n  3. Re-mark ✅: add ✅ to edges taken in THIS investigation; remove ✅ from\n     edges that were NOT taken in this investigation (they remain but without ✅).\n\nExisting tree to evolve (preserve every node ID and label exactly):\n\n{base_tree_mermaid}\n";

export const HUMAN_TEMPLATE =
  '\nBased on the conversation history, create a **generic investigation plan** that can be applied to similar problems in the future.\n\nYour plan should include:\n\n1. **Applicability**: Describe when this plan should be applied. What types of alerts, symptoms, or scenarios indicate this plan is relevant?\n\n2. **Category Identification**: How can an engineer identify that a problem belongs to this category? What are the key signals or patterns to look for?\n\n3. **Decision Trace**: Extract the decision trace from this investigation as a Mermaid diagram. The trace must use exactly 4 node types:\n   - **Symptom / Question** `([...])` — observable symptoms, alerts, or questions that triggered the conversation\n   - **Evidence Gatherer** `[...]` — atomic steps that each gather a single piece of evidence (from logs, metrics, code, traces, etc.)\n   - **Decision** `{{...}}` — LLM reasoning steps that branch based on gathered evidence\n   - **End** `((...))` — terminal conclusions (root cause identified, resolved, escalated)\n\n   Stay true to what was actually observed. Mark edges on the taken path by prefixing edge labels with ✅. Also include branches that were considered but not explored. Do NOT use linkStyle, style, classDef, or any Mermaid styling directives.\n\nUse consistent node ID prefixes: S for symptom, E for evidence gatherer, D for decision, X for end.\n\nThe decision trace MUST start directly with `flowchart TD` — do NOT wrap it in code fences (no triple backticks, no ```mermaid).\n\n**IMPORTANT — Marking the taken path:** For edges that were actually traversed during the investigation, prefix the condition label with ✅. Edges that were NOT taken should have plain condition labels.\nExample:\n- Taken edge: `D1 -->|"✅ Yes"| E2` (this path was actually followed)\n- Not-taken edge: `D1 -->|"No"| X1` (this path was not explored)\n\n4. **Evidence Gatherer Metadata**: For EACH evidence gatherer node (E1, E2, ...) in your decision tree, provide a string in the format `<node_id>: <description>` where description is a natural-language summary of the toolset/skill used, what data it gathers, and from which data sources. Include exact reusable query entities from the investigation/tool calls in this metadata description.\n\nExamples:\n- "E1: DatadogToolkit - Query container.cpu.usage and container.memory.usage from the k8s dashboard grouped by pod and namespace"\n- "E2: SplunkToolkit - Search application error logs filtering by service=checkout, error codes, and timeout keywords"\n- "E3: KubernetesToolkit - Check pod restart counts and warning events in namespace checkout-prod for pods matching checkout-*"\n{base_tree_section}';
