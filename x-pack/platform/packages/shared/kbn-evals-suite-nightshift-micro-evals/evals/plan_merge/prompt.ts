/*
 * Copyright Elasticsearch B.V. and/or licensed to Elasticsearch B.V. under one
 * or more contributor license agreements. Licensed under the Elastic License
 * 2.0; you may not use this file except in compliance with the Elastic License
 * 2.0.
 */

// Verbatim code-defined prompts from deductive-ai/deductive at a60f80265f5ebe5c4ce9be02b0a158be00e2d58c.

export const PLAN_MERGE_SYSTEM_PROMPT =
  'You are an expert at maintaining investigation plans. The per-call user message will invoke you in one of two modes:\n\n- **Merge mode** — you are given two investigation plans (Plan A and Plan B) and must produce a single unified plan that captures all investigation paths from both.\n- **Reinforcement mode** — you are given Plan A and a separate Causal Analysis describing a confirmed root cause, and must update Plan A\'s decision tree to mark the verified causal path and apply any structural edits called out in the analysis.\n\nThe user message for each mode spells out the exact output contract for that mode (what must / must not appear in `decision_tree_mermaid`). Follow that contract exactly.\n\nMode-independent invariants:\n- Return a valid `InvestigationPlan` whose `decision_tree_mermaid` is a syntactically valid Mermaid flowchart.\n- Do NOT use `style`, `linkStyle`, `classDef`, or any other Mermaid styling directives.\n- Every evidence_gatherer node in the returned tree must have a corresponding entry in `evidence_gatherer_metadata` in the format `"<node_id>: <description>"`. Include exact reusable query entities from the investigation/tool calls, such as metric names, labels, log indexes, keywords, service names, namespaces, resources, and filters in that metadata description (e.g. `"E1: DatadogToolkit - Query container.cpu.usage grouped by pod and namespace"`).\n- `applicability`, `category_identification`, and `keywords` must be coherent and free of duplication.\n\nHere the base tree is Plan A and the inputs to merge are Plan A together with Plan B (merge mode) or the Causal Analysis (reinforcement mode).\nDecision-tree merge discipline (avoid near-duplicate growth):\n- The result must be the union of the inputs, deduplicated where they overlap: overlapping subtrees are collapsed onto shared nodes and only genuinely unique paths are added. It is a structural near-superset of the base tree, never a parallel rewrite.\n- Preserve the base tree\'s node IDs, shapes, and naming conventions; reuse them for any node you introduce.\n- Match before adding: for each step, decision, or outcome, first find the existing node whose meaning best matches it - even if worded differently or reached via different concrete values - and reuse that node\'s id. Only when no existing node matches do you ADD a new Evidence (E*), Decision (D*), or Terminal (X*) node using the same conventions.\n- Concrete payload specifics are evidence values, never node identity: timestamps, B/C/Value numbers, FIRING counts, Slack event_ts/message_id, channel ids, per-cluster name sets. A node that differs from an existing one only by such a value is the same node; reuse it. Preserve exact reusable query entities from the investigation/tool calls, such as metric names, labels, log indexes, keywords, namespaces, resources, services, and filters, in `evidence_gatherer_metadata`.\n';

export const REINFORCEMENT_GUIDANCE =
  '\n<reinforcement_guidance>\nThe Causal Analysis above is a natural-language description of the confirmed root cause\nfrom human feedback. Apply it to Plan A\'s decision tree in this exact order:\n\n**Step A — Identify the confirmed root cause terminal.**\nRead Plan A\'s terminal nodes (X* nodes). Find the one whose description best matches the\nconfirmed root cause. If no existing terminal matches, ADD new Evidence (E*), Decision (D*)\nand Terminal (X*) nodes using the same naming convention and mermaid node shapes as Plan A.\nThe new terminal description must reflect the confirmed root cause.\n\n**Step B — Mark the causal path with ✅. (This step is mandatory.)**\nTrace the path from the root symptom (S*) to the confirmed terminal (existing or newly added).\nPrefix EVERY edge condition on that path with ✅ (e.g. `-->|✅ Yes - deploy found|`).\nThis step MUST produce at least one ✅-marked edge. If you complete Step A, you MUST complete\nStep B before writing the output.\n\n**Step C — Strip all other ✅ marks.**\nEvery edge NOT on the confirmed causal path must have a plain condition label — no ✅ prefix.\n\n**Step D — Apply structural edits (if any).**\nIf the Causal Analysis identifies a node or heuristic as incorrect or misleading\n(e.g. "the lunch-peak check is wrong", "remove this node"), apply that structural edit —\nremove or replace the misleading node — while preserving all paths unrelated to the edit.\nStructural edits do NOT remove the ✅ marks placed in Step B.\n\n**Step E — Preserve all unrelated paths.**\nAll branches in Plan A not touched by the causal correction must appear unchanged.\n\nDo NOT use `style`, `linkStyle`, `classDef`, or any Mermaid styling directives.\n</reinforcement_guidance>\n';

export const HUMAN_TEMPLATE =
  "\n## Plan A (existing decision tree to update)\n\n**Applicability:**\n\n\n**Category Identification:**\n\n\n**Decision Tree:**\n{initial_tree}\n\n**Evidence Gatherer Metadata:**\n(none)\n\n**Keywords:**\n\n\n---\n\n## Causal Analysis (confirmed root cause from human feedback)\n\n{causal_summary}\n\n---\n\n{reinforcement_guidance}\n\nReturn an updated version of Plan A's decision tree with the causal path correctly marked.\nThe decision tree should begin with ```mermaid and be a valid Mermaid flowchart.\n";
