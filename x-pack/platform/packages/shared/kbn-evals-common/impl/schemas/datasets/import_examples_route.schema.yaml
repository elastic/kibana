openapi: 3.0.0
info:
  title: Import Evaluation Dataset Examples API endpoint
  version: '1'
paths:
  /internal/evals/datasets/{datasetId}/examples/_import:
    post:
      x-codegen-enabled: true
      x-labels: [ess, serverless]
      operationId: ImportEvaluationDatasetExamples
      description: Import examples into an evaluation dataset
      summary: Import evaluation dataset examples
      tags:
        - Evals API
      parameters:
        - name: datasetId
          in: path
          required: true
          schema:
            type: string
            maxLength: 1024
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                examples:
                  type: array
                  # Keep in sync with MAX_EXAMPLES_PER_DATASET in constants.ts
                  maxItems: 10000
                  items:
                    $ref: '#/components/schemas/ImportExamplesPayload'
              required:
                - examples
      responses:
        '200':
          description: Successful response
          content:
            application/json:
              schema:
                type: object
                properties:
                  added:
                    type: integer
                  skipped_duplicates:
                    type: integer
                required:
                  - added
                  - skipped_duplicates
        '404':
          description: Dataset not found
          content:
            application/json:
              schema:
                type: object
                properties:
                  statusCode:
                    type: number
                  error:
                    type: string
                  message:
                    type: string

components:
  schemas:
    ImportExamplesPayload:
      type: object
      properties:
        input:
          type: object
          additionalProperties: true
        output:
          type: object
          additionalProperties: true
        metadata:
          type: object
          additionalProperties: true
