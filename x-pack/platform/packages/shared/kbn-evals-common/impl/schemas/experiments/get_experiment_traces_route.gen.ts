/*
 * Copyright Elasticsearch B.V. and/or licensed to Elasticsearch B.V. under one
 * or more contributor license agreements. Licensed under the Elastic License
 * 2.0; you may not use this file except in compliance with the Elastic License
 * 2.0.
 */

/*
 * NOTICE: Do not edit this file manually.
 * This file is automatically generated by the OpenAPI Generator, @kbn/openapi-generator.
 *
 * info:
 *   title: Get Evaluation Experiment Traces API endpoint
 *   version: 1
 */

import { z, lazySchema } from '@kbn/zod/v4';

import { TraceSpan } from '../common_attributes.gen';

/**
 * One resolved trace: its reference (trace id, role, and the evaluator name for evaluator traces) plus the spans fetched from traces-*. Spans are empty when the trace has aged out of traces-*.
 */
export const ExperimentTrace = lazySchema(() =>
  z.object({
    trace_id: z.string().max(256),
    role: z.enum(['task', 'evaluator']),
    evaluator_name: z.string().max(256).optional(),
    spans: z.array(TraceSpan),
    total_spans: z.number().int(),
    duration_ms: z.number(),
  })
);
export type ExperimentTrace = z.infer<typeof ExperimentTrace>;

export const GetEvaluationExperimentTracesRequestQuery = lazySchema(() =>
  z.object({
    /**
     * Return only task traces or only evaluator traces. When omitted, traces of both roles are returned, task traces first.
     */
    role: z.enum(['task', 'evaluator']).optional(),
    /**
     * Restrict evaluator traces to this evaluator name. Only valid together with role=evaluator.
     */
    evaluator: z.string().max(256).optional(),
    page: z.coerce.number().int().min(1).optional().default(1),
    /**
     * Traces per page. Bounded tighter than the runs endpoint because each trace carries all of its spans.
     */
    per_page: z.coerce.number().int().min(1).max(50).optional().default(10),
  })
);
export type GetEvaluationExperimentTracesRequestQuery = z.infer<
  typeof GetEvaluationExperimentTracesRequestQuery
>;
export type GetEvaluationExperimentTracesRequestQueryInput = z.input<
  typeof GetEvaluationExperimentTracesRequestQuery
>;

export const GetEvaluationExperimentTracesRequestParams = lazySchema(() =>
  z.object({
    experimentId: z.string().max(1024),
  })
);
export type GetEvaluationExperimentTracesRequestParams = z.infer<
  typeof GetEvaluationExperimentTracesRequestParams
>;
export type GetEvaluationExperimentTracesRequestParamsInput = z.input<
  typeof GetEvaluationExperimentTracesRequestParams
>;

export const GetEvaluationExperimentTracesResponse = lazySchema(() =>
  z.object({
    experiment_id: z.string().max(1024),
    traces: z.array(ExperimentTrace).max(50),
    /**
     * Total distinct traces the experiment's score documents reference, capped at 10000 per role (the same ceiling the score retrieval endpoints apply to score documents).
     */
    total: z.number().int(),
    page: z.number().int(),
    per_page: z.number().int(),
  })
);
export type GetEvaluationExperimentTracesResponse = z.infer<
  typeof GetEvaluationExperimentTracesResponse
>;
