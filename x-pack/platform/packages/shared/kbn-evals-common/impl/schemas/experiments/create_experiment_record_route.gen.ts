/*
 * Copyright Elasticsearch B.V. and/or licensed to Elasticsearch B.V. under one
 * or more contributor license agreements. Licensed under the Elastic License
 * 2.0; you may not use this file except in compliance with the Elastic License
 * 2.0.
 */

/*
 * NOTICE: Do not edit this file manually.
 * This file is automatically generated by the OpenAPI Generator, @kbn/openapi-generator.
 *
 * info:
 *   title: Create Evaluation Experiment Record API endpoint
 *   version: 1
 */

import { z, lazySchema } from '@kbn/zod/v4';

import {
  ExperimentRecordProtocol,
  ExperimentRecordProvenance,
  EvaluationExperimentRecord,
} from '../common_attributes.gen';

export const CreateEvaluationExperimentRecordRequestParams = lazySchema(() =>
  z.object({
    experimentId: z.string().max(1024),
  })
);
export type CreateEvaluationExperimentRecordRequestParams = z.infer<
  typeof CreateEvaluationExperimentRecordRequestParams
>;
export type CreateEvaluationExperimentRecordRequestParamsInput = z.input<
  typeof CreateEvaluationExperimentRecordRequestParams
>;

export const CreateEvaluationExperimentRecordRequestBody = lazySchema(() =>
  z.object({
    name: z.string().min(1).max(256),
    description: z.string().max(2048).optional(),
    protocol: ExperimentRecordProtocol,
    /**
     * Initial lifecycle status. `pending` is for queued runs (e.g. workflow executions) that have not started executing yet; runs that begin immediately are created as `running`.
     */
    status: z.enum(['pending', 'running']).optional().default('running'),
    /**
     * When execution began. Defaults to the creation time for records created as `running`.
     */
    started_at: z.string().max(64).optional(),
    provenance: ExperimentRecordProvenance.optional(),
    /**
     * Additional spaces to assign the record to, mirroring the space assignment of the run's ingested scores. The caller's active space is always included, so the record can still be finalized from the space that created it.
     */
    space_ids: z.array(z.string().max(256)).max(100).optional(),
  })
);
export type CreateEvaluationExperimentRecordRequestBody = z.infer<
  typeof CreateEvaluationExperimentRecordRequestBody
>;
export type CreateEvaluationExperimentRecordRequestBodyInput = z.input<
  typeof CreateEvaluationExperimentRecordRequestBody
>;

export const CreateEvaluationExperimentRecordResponse = lazySchema(
  () => EvaluationExperimentRecord
);
export type CreateEvaluationExperimentRecordResponse = z.infer<
  typeof CreateEvaluationExperimentRecordResponse
>;
