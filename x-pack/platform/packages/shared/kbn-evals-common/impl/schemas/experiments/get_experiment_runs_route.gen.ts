/*
 * Copyright Elasticsearch B.V. and/or licensed to Elasticsearch B.V. under one
 * or more contributor license agreements. Licensed under the Elastic License
 * 2.0; you may not use this file except in compliance with the Elastic License
 * 2.0.
 */

/*
 * NOTICE: Do not edit this file manually.
 * This file is automatically generated by the OpenAPI Generator, @kbn/openapi-generator.
 *
 * info:
 *   title: Get Evaluation Experiment Runs API endpoint
 *   version: 1
 */

import { z, lazySchema } from '@kbn/zod/v4';

import { ExampleInfo, TaskInfo, EvaluatorInfo } from '../common_attributes.gen';

/**
 * One task execution: an example run once at a given repetition, with every evaluator's result on that output. The example (with its full input) and the task (with its full output, model, repetition index, and trace reference) are shared by the run; each evaluator entry carries that evaluator's score, label, explanation, metadata, and trace reference. A model is never attributed to a `code` evaluator, which invokes none.
 */
export const ExperimentRun = lazySchema(() =>
  z.object({
    example: ExampleInfo,
    task: TaskInfo,
    evaluators: z.array(EvaluatorInfo).max(1000),
  })
);
export type ExperimentRun = z.infer<typeof ExperimentRun>;

export const GetEvaluationExperimentRunsRequestQuery = lazySchema(() =>
  z.object({
    /**
     * Filter by suite ID
     */
    suite_id: z.string().max(256).optional(),
    /**
     * Filter by task model ID
     */
    model_id: z.string().max(256).optional(),
    /**
     * When provided, returns the runs of all experiments in this execution instead of the single experiment from the path param
     */
    execution_id: z.string().max(1024).optional(),
    page: z.coerce.number().int().min(1).optional().default(1),
    /**
     * Runs per page. Bounded because each run carries its full task output and example input.
     */
    per_page: z.coerce.number().int().min(1).max(100).optional().default(20),
  })
);
export type GetEvaluationExperimentRunsRequestQuery = z.infer<
  typeof GetEvaluationExperimentRunsRequestQuery
>;
export type GetEvaluationExperimentRunsRequestQueryInput = z.input<
  typeof GetEvaluationExperimentRunsRequestQuery
>;

export const GetEvaluationExperimentRunsRequestParams = lazySchema(() =>
  z.object({
    experimentId: z.string().max(1024),
  })
);
export type GetEvaluationExperimentRunsRequestParams = z.infer<
  typeof GetEvaluationExperimentRunsRequestParams
>;
export type GetEvaluationExperimentRunsRequestParamsInput = z.input<
  typeof GetEvaluationExperimentRunsRequestParams
>;

export const GetEvaluationExperimentRunsResponse = lazySchema(() =>
  z.object({
    experiment_id: z.string().max(1024),
    runs: z.array(ExperimentRun).max(100),
    /**
     * Total distinct runs the experiment produced, capped at 10000 (the same ceiling the score retrieval endpoints apply to score documents).
     */
    total: z.number().int(),
    page: z.number().int(),
    per_page: z.number().int(),
  })
);
export type GetEvaluationExperimentRunsResponse = z.infer<
  typeof GetEvaluationExperimentRunsResponse
>;
