import React from 'react';
export interface AlertEpisodeTimelineRelativeTimestampProps {
    timestamp: string;
}
/** Renders a relative time (e.g. "5 minutes ago") with the full timestamp on hover. */
export declare const AlertEpisodeTimelineRelativeTimestamp: ({ timestamp, }: AlertEpisodeTimelineRelativeTimestampProps) => React.JSX.Element;
