/*
 * Copyright Elasticsearch B.V. and/or licensed to Elasticsearch B.V. under one
 * or more contributor license agreements. Licensed under the Elastic License
 * 2.0; you may not use this file except in compliance with the Elastic License
 * 2.0.
 */

describe('Query rule detail', () => {
  describe('existing query ruleset', () => {
    it('should render the query ruleset detail page', () => {
      // render the component with mock data
      // check the page title is correct
      // check if header has data button
      // check if header has save button
      // make sure save button is greyed out
      // check if add rule button is present
      // check drag and drop table headers are correct
      // check if we have correct amount of rows
      // check if the rows are limited to 3 rules
      // check if the rows show correct setting pin/exclude
      // check if the rows show correct document count for pin/exclude
    });
  });
  describe('create query ruleset flow', () => {
    // TODO: refactor this test
    it('should render the create query ruleset page', () => {
      // Test implementation goes here
    });
  });
});
