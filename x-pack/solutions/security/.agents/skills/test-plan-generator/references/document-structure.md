# Test Plan Document Structure

This file defines the required sections of a test plan, their order, and the content expected in each. Follow this template exactly when writing a test plan in Step 3.

---

## Template

```markdown
# Test Plan: [Feature Name]

## Overview

[One paragraph explaining what this test plan covers and the scope of validation — what feature is being tested, against which issue, and what the expected outcome is.]

## Feature Background

[Why this feature exists and what problem it solves. Summarise the motivation from the issue or parent epic. Keep it concise — 2–4 sentences.]

## Scope

**In scope:**
- [What is explicitly covered by this test plan]

**Out of scope:**
- [What is explicitly excluded — deferred work, related features not touched by this issue, etc.]

## Terminology

[Optional — omit this section entirely if no special terminology is needed. Include only if the issue or sub-issues use domain-specific terms that a tester would need to understand the scenarios.]

| Term | Definition |
|---|---|
| [Term] | [Plain-language definition] |

## Assumptions

[Baseline conditions that apply to all scenarios unless a scenario states otherwise.]

- **License level:** [e.g. Enterprise, Basic]
- **User role:** [e.g. Editor, Viewer — the role used for all scenarios unless stated otherwise]
- **Data setup:** [e.g. at least one active alert, a pre-configured rule, empty index]
- **Deployment type:** [e.g. self-managed, serverless, ECH]

## Acceptance Criteria

[Numbered list, taken directly from the issue. Do not paraphrase — copy the criteria as stated, or note if they were inferred.]

1. [Acceptance criterion from the issue]
2. [Acceptance criterion from the issue]

## Known Limitations

[Constraints or deferred functionality. Include anything that affects completeness of this test plan.]

- ⚠️ [Limitation or assumption that could not be confirmed]

## Test Scenarios

[Organised by feature area. Each area is wrapped in a collapsible block so the comment remains readable regardless of plan size. Write scenarios in priority order within each area: P0 first, then P1, then P2. See `references/output-formats.md` for the required scenario format and automation coverage rules.]

<details>
<summary><strong>[Feature area name]</strong> — N scenarios (P0: n, P1: n, P2: n)</summary>

[Scenarios for this area, P0 first]

</details>

---

<details>
<summary><strong>[Feature area name]</strong> — N scenarios (P0: n, P1: n, P2: n)</summary>

[Scenarios for this area, P0 first]

</details>

---

## Test Coverage Summary

**Total scenarios:** [N]

| Feature area | Scenarios | P0 | P1 | P2 | Automated | Manual only |
|---|---|---|---|---|---|---|
| [Feature area] | [N] | [n] | [n] | [n] | [n (type)] | [n] |
| **Total** | **[N]** | **[n]** | **[n]** | **[n]** | **[n]** | **[n]** |

**Automated coverage notes:**
- [Test framework and location — e.g. "Unit tests (Jest) in `x-pack/solutions/security/plugins/…`"]
- [Key test files — e.g. "`alerts.test.tsx`, `rule_form.test.ts`"]
- [Any gaps — e.g. "No e2e (Cypress/Scout) or integration tests exist for this feature yet"]

## Test Execution Notes

[Explicit execution order by priority. List each scenario by name under its priority level.]

**P0 — run first, block release if failing:**
- [Scenario title]

**P1 — run before release, high-impact regressions:**
- [Scenario title]

**P2 — run as capacity allows, limited blast radius:**
- [Scenario title]

---

*🤖 Generated by [model-identifier] on [YYYY-MM-DD]*
```

---

## Notes

- **Terminology** is optional — omit the section entirely if the issue uses no domain-specific terms.
- **RBAC / Permission Testing Matrix** — only include if the issue explicitly mentions roles, permissions, or access control. Do not add speculatively.
- If a section cannot be filled in due to missing information, apply the Core rule: stop and ask the user rather than leaving placeholder text in the draft.

## Test Coverage Summary — filling in the table

The `Automated` and `Manual only` columns are derived from the test coverage catalog built in Step 1:

- **Automated** — count the scenarios in this area that have at least one matching test (unit, integration, API integration, or e2e) in the test coverage catalog. In parentheses, note the test type(s) present — e.g. `2 (unit)`, `3 (unit, e2e)`, `1 (unit, partial)`. Use `partial` when a test exists but covers only a subset of the scenario's behaviour.
- **Manual only** — count the scenarios in this area with no matching tests at all (`No existing tests found covering this scenario`). These are the scenarios that require manual execution and represent the testing gap.
- The **Total** row must sum each column correctly. Double-check before saving.
- **Automated coverage notes** must include: the test framework and directory path, the specific key test files, and a plain statement of what is not yet covered (no e2e, no integration, etc.). Do not leave this as a generic placeholder — derive it from the test coverage catalog.