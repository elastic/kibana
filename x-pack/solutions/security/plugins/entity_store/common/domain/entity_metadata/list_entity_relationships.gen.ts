/*
 * Copyright Elasticsearch B.V. and/or licensed to Elasticsearch B.V. under one
 * or more contributor license agreements. Licensed under the Elastic License
 * 2.0; you may not use this file except in compliance with the Elastic License
 * 2.0.
 */

/*
 * NOTICE: Do not edit this file manually.
 * This file is automatically generated by the OpenAPI Generator, @kbn/openapi-generator.
 *
 * info:
 *   title: List Entity Relationships Schema
 *   version: 1
 */

import { z, lazySchema } from '@kbn/zod/v4';

export const ListEntityRelationshipsRequestQuery = lazySchema(() =>
  z.object({
    page: z.coerce.number().int().min(1).optional().describe('Page number to return (1-indexed).'),
    per_page: z.coerce
      .number()
      .int()
      .min(1)
      .optional()
      .describe('Number of records per page.'),
    sort_field: z.string().max(1000).optional().describe('Field to sort results by.'),
    sort_order: z.enum(['asc', 'desc']).optional().describe('Sort order.'),
    kind: z
      .enum([
        'accesses_frequently',
        'accesses_infrequently',
        'communicates_with',
        'administers',
        'depends_on',
        'owns',
        'supervises',
      ])
      .optional()
      .describe('Relationship kind to filter on.'),
    target: z.string().max(1000).optional().describe('Target EUID to filter on.'),
    from: z.string().max(100).datetime().optional().describe('ISO-8601 lower bound for @timestamp.'),
    to: z.string().max(100).datetime().optional().describe('ISO-8601 upper bound for @timestamp.'),
  })
);
export type ListEntityRelationshipsRequestQuery = z.infer<
  typeof ListEntityRelationshipsRequestQuery
>;
export type ListEntityRelationshipsRequestQueryInput = z.input<
  typeof ListEntityRelationshipsRequestQuery
>;

export const ListEntityRelationshipsRequestParams = lazySchema(() =>
  z.object({
    /**
     * The EUID of the entity to list relationships for.
     */
    entityId: z.string().min(1).max(1000),
  })
);
export type ListEntityRelationshipsRequestParams = z.infer<
  typeof ListEntityRelationshipsRequestParams
>;
export type ListEntityRelationshipsRequestParamsInput = z.input<
  typeof ListEntityRelationshipsRequestParams
>;

export const ListEntityRelationshipsResponse = lazySchema(() =>
  z.object({
    records: z.array(z.object({})),
    page: z.number().int().min(1),
    per_page: z.number().int().min(1),
    total: z.number().int().min(0),
  })
);
export type ListEntityRelationshipsResponse = z.infer<typeof ListEntityRelationshipsResponse>;
