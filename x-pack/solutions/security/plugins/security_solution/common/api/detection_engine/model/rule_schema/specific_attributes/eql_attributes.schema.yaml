openapi: 3.0.0
info:
  title: EQL Rule Attributes
  version: 'not applicable'
paths: {}
components:
  x-codegen-enabled: true
  schemas:
    EventCategoryOverride:
      type: string
    TimestampField:
      type: string
      description: Contains the event timestamp used for sorting a sequence of events. This is different from timestamp_override, which is used for querying events within a range. Defaults to the @timestamp ECS field.
    TiebreakerField:
      type: string
      description: Sets a secondary field for sorting events
