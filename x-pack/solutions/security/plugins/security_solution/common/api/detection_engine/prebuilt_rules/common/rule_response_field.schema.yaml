openapi: 3.0.0
info:
  title: Rule Response Field
  version: '1'
paths: {}
components:
  schemas:
    RuleResponseField:
      type: string
      description: |
        Top-level key from detection rule API responses (`RuleResponse`).
      enum:
        - actions
        - alias_purpose
        - alias_target_id
        - alert_suppression
        - anomaly_threshold
        - author
        - building_block_type
        - concurrent_searches
        - created_at
        - created_by
        - data_view_id
        - description
        - enabled
        - event_category_override
        - exceptions_list
        - execution_summary
        - false_positives
        - filters
        - from
        - history_window_start
        - id
        - immutable
        - index
        - investigation_fields
        - interval
        - items_per_search
        - language
        - license
        - machine_learning_job_id
        - max_signals
        - meta
        - name
        - namespace
        - new_terms_fields
        - note
        - outcome
        - output_index
        - query
        - references
        - related_integrations
        - required_fields
        - response_actions
        - revision
        - risk_score
        - risk_score_mapping
        - rule_id
        - rule_name_override
        - rule_source
        - saved_id
        - severity
        - severity_mapping
        - setup
        - tags
        - threat
        - threat_filters
        - threat_index
        - threat_indicator_path
        - threat_language
        - threat_mapping
        - threat_query
        - threshold
        - throttle
        - tiebreaker_field
        - timeline_id
        - timeline_title
        - timestamp_field
        - timestamp_override
        - timestamp_override_fallback_disabled
        - to
        - type
        - updated_at
        - updated_by
        - version
