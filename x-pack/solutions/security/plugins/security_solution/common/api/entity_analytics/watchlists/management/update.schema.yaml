# Update Watchlist API Schema
# Modeled after create.schema.yaml
openapi: 3.0.0
info:
  title: Watchlist Management API
  version: "2026-01-28"

paths:
  /api/entity_analytics/watchlists/management/{name}:
    put:
      x-labels: [ess, serverless]
      x-codegen-enabled: true
      operationId: UpdateWatchlist
      summary: Update an existing watchlist
      parameters:
        - name: name
          in: path
          required: true
          schema:
            type: string
          description: The name of the watchlist to update
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - name
                - description
                - riskModifier
              properties:
                name:
                  type: string
                  description: Unique name of the watchlist
                description:
                  type: string
                  description: Description of the watchlist
                riskModifier:
                  type: number
                  description: Risk score modifier associated with the watchlist
                managed:
                  type: boolean
                  description: Indicates if the watchlist is managed by the system
      responses:
        "200":
          description: Watchlist updated successfully
          content:
            application/json:
              schema:
                $ref: "./common.schema.yaml#/components/schemas/WatchlistObject"
