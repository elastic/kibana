openapi: 3.0.0
info:
  title: Memory Dump Schema
  version: '2023-10-31'
paths:
  /api/endpoint/action/{action_id}:
    post:
      summary: Generate a memory dump from the host machine
      operationId: EndpointGetActionsDetails
      description: Generates memory dumps on the targeted host.
      x-codegen-enabled: false
      x-labels: [ ess, serverless ]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/MemoryDumpRouteRequestBody'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MemoryDumpRouteResponse'

components:
  x-codegen-enabled: false
  schemas:
    MemoryDumpRouteRequestBody:
      properties:
#        TODO:PT implement schema
    MemoryDumpRouteResponse:
      properties:


