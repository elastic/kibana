/*
 * Copyright Elasticsearch B.V. and/or licensed to Elasticsearch B.V. under one
 * or more contributor license agreements. Licensed under the Elastic License
 * 2.0; you may not use this file except in compliance with the Elastic License
 * 2.0.
 */

import type { SavedObjectsType } from '@kbn/core/server';
import { SAVED_VIEW_SO_TYPE } from '../../../common/threat_intelligence/hub';

/**
 * `threat-intelligence-saved-view` saved-object type.
 *
 * Stores the dashboard filter state (regions, categories, time range) as a
 * named pointer. The id is generated by the saved-objects client; the object
 * uses `namespaceType: 'multiple-isolated'`, meaning each space holds its own
 * independent copy — the object is NOT automatically shared across spaces and
 * cannot be shared via the standard Spaces UI (that requires `'multiple'`).
 *
 * The type is HTTP-visible (no `hiddenFromHttpApis: true`) but writes still
 * go through the plugin's internal routes so we can enforce shape + size
 * limits before saving.
 */
export const SavedViewType: SavedObjectsType = {
  name: SAVED_VIEW_SO_TYPE,
  hidden: false,
  namespaceType: 'multiple-isolated',
  mappings: {
    dynamic: false,
    properties: {
      name: { type: 'keyword' },
      description: { type: 'text' },
      filters: {
        properties: {
          regions: { type: 'keyword' },
          categories: { type: 'keyword' },
          time_range: {
            properties: {
              from: { type: 'date' },
              to: { type: 'date' },
            },
          },
        },
      },
      created_at: { type: 'date' },
      updated_at: { type: 'date' },
      owner: { type: 'keyword' },
    },
  },
  management: {
    importableAndExportable: true,
    visibleInManagement: true,
    defaultSearchField: 'name',
    getTitle: (so) => {
      const name = (so.attributes as { name?: string }).name;
      return name ? `Threat Intelligence view: ${name}` : 'Threat Intelligence saved view';
    },
  },
};
