# This file is generated by the @kbn/moon package. Any manual edits will be erased!
#  To extend this, write your extensions/overrides to 'moon.extend.yml'
#  then regenerate this file with: 'node scripts/regenerate_moon_projects --update --filter @kbn/security-solution-plugin'

$schema: https://moonrepo.dev/schemas/project.json
id: '@kbn/security-solution-plugin'
type: unknown
owners:
  defaultOwner: '@elastic/security-solution'
toolchain:
  default: node
language: typescript
project:
  name: '@kbn/security-solution-plugin'
  description: Moon project for @kbn/security-solution-plugin
  channel: ''
  owner: '@elastic/security-solution'
  metadata:
    sourceRoot: x-pack/solutions/security/plugins/security_solution
tags:
  - security
  - plugin
  - prod
fileGroups:
  src:
    - common/**/*
    - public/**/*
    - server/**/*
    - scripts/**/*
    - test/scout/**/*
    - common/**/*.json
    - server/**/*.json
    - scripts/**/*.json
    - public/**/*.json
    - emotion.d.ts
    - '!target/**/*'
    - '!**/cypress/**'
    - '!public/management/cypress.config.ts'
tasks:
  jest:
    description: Run jest tests for the project
  print-stats:
    script: node -p "v8.getHeapStatistics().heap_size_limit / 1024 / 1024"
    options:
      runFromWorkspaceRoot: true
      cache: false
    env:
      NODE_OPTIONS: '--max-old-space-size=9216'
  typecheck:
    command:
      - tsc
      - '--build'
      - $projectSource/tsconfig.moon.json
      - '--pretty'
    inputs:
      - '@group(src)'
      - tsconfig.json
      - tsconfig.*.json
    outputs:
      - target
    deps:
      - kibana:generateAllTypecheckConfigs
      - ^:typecheck
      - ~:print-stats
    options:
      runFromWorkspaceRoot: true
      mergeOutputs: replace
      mergeArgs: replace
      mergeInputs: replace
    env:
      NODE_OPTIONS: '--max-old-space-size=9216'
