/*
 * Copyright Elasticsearch B.V. and/or licensed to Elasticsearch B.V. under one
 * or more contributor license agreements. Licensed under the Elastic License
 * 2.0; you may not use this file except in compliance with the Elastic License
 * 2.0.
 */

/*
 * NOTICE: Do not edit this file manually.
 * This file is automatically generated by the OpenAPI Generator, @kbn/openapi-generator.
 *
 * info:
 *   title: AlertZero Floor Watch Worker-specific settings
 *   version: 1
 */

import { z, lazySchema } from '@kbn/zod/v4';

/**
 * Agent Builder agent the Attack Discovery Worker runs with. Carried from the Worker through the runner to each per-discovery review, where it opens the investigation conversation. Max length matches Agent Builder's agentIdMaxLength.
 */
export const AttackDiscoveryAgentId = lazySchema(() => z.string().min(1).max(64));
export type AttackDiscoveryAgentId = z.infer<typeof AttackDiscoveryAgentId>;

/**
 * Complete Worker-specific settings for the Attack Discovery Worker, owned by Floor Watch. Sent whole on write. Every field is optional and the object itself may be omitted: the Worker declares no default extras, so a Worker that has never had an agent picked stores no `extras` key at all and the review opens its conversation with the default agent.
 */
export const AttackDiscoveryWorkerExtras = lazySchema(() =>
  z
    .object({
      agentId: AttackDiscoveryAgentId.optional(),
    })
    .strict()
);
export type AttackDiscoveryWorkerExtras = z.infer<typeof AttackDiscoveryWorkerExtras>;
