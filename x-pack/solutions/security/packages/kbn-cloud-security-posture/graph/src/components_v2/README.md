# Graph prototype v.2

Isolated copy of the graph visualizer UI (nodes, edges, controls, investigation chrome).

- Edit files in this folder for **v.2** only.
- **v.1** lives in `src/components`.
- Mock / fetch data is shared: both versions import `src/hooks/use_fetch_graph_data`.
