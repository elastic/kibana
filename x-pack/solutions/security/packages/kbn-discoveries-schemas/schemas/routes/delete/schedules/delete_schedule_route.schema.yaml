openapi: 3.0.0
info:
  title: Delete Attack Discovery Schedule Internal API endpoint
  version: '2023-10-31'
paths:
  /internal/attack_discovery/schedules/{id}:
    delete:
      x-codegen-enabled: true
      x-labels: [ess, serverless]
      operationId: DeleteAttackDiscoverySchedule
      x-state: Technical Preview
      summary: Delete an attack discovery schedule
      description: >-
        Internal API that permanently deletes an attack discovery schedule and all
        associated configuration.
      parameters:
        - name: id
          in: path
          required: true
          description: The unique identifier of the schedule to delete
          schema:
            $ref: '../../../common_attributes.schema.yaml#/components/schemas/NonEmptyString'
      responses:
        '200':
          description: Schedule deleted successfully
          content:
            application/json:
              schema:
                type: object
                required:
                  - id
                properties:
                  id:
                    $ref: '../../../common_attributes.schema.yaml#/components/schemas/NonEmptyString'
                    description: The unique identifier of the deleted schedule
        '400':
          description: Bad request
          content:
            application/json:
              schema:
                type: object
                required:
                  - status_code
                  - error
                  - message
                properties:
                  status_code:
                    type: number
                  error:
                    type: string
                  message:
                    type: string
components:
  x-codegen-enabled: true
