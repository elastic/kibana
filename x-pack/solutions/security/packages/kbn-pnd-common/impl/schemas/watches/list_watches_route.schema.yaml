openapi: 3.0.0
info:
  title: List PND Watches API endpoint
  version: '1'
paths:
  /internal/pnd/watches:
    get:
      x-codegen-enabled: true
      x-labels: [ess, serverless]
      operationId: ListWatches
      description: List Security Watch catalog entries projected from managed workflows
      summary: List watches
      tags:
        - PND API
      responses:
        '200':
          description: Successful response
          content:
            application/json:
              schema:
                type: object
                properties:
                  watches:
                    type: array
                    items:
                      $ref: '../components/watch.schema.yaml#/components/schemas/Watch'
                required:
                  - watches
