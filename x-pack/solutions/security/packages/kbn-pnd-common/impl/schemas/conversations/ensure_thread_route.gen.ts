/*
 * Copyright Elasticsearch B.V. and/or licensed to Elasticsearch B.V. under one
 * or more contributor license agreements. Licensed under the Elastic License
 * 2.0; you may not use this file except in compliance with the Elastic License
 * 2.0.
 */

/*
 * NOTICE: Do not edit this file manually.
 * This file is automatically generated by the OpenAPI Generator, @kbn/openapi-generator.
 *
 * info:
 *   title: Ensure PND Thread API endpoint
 *   version: 1
 */

import { z, lazySchema } from '@kbn/zod/v4';
import { isNonEmptyString } from '@kbn/zod-helpers/v4';

import { PndGateId } from '../components/gate.gen';

/**
* ⛔ **Exactly these two fields, forever.** The seed message is built server-side from the proposal row the server already has (gate, Attack Discovery title, proposed change, rationale). Do **not** add `prompt`, `message`, `title` or any other caller-supplied text: this route is called by a workflow step and reachable by any holder of `pnd_threads_write`, so caller-supplied prompt text would be a prompt-injection and token-burn vector straight into an LLM turn (D5). Unknown properties are dropped rather than rejected, matching `_apply`'s precedent, so an injected field can never reach `/converse` — and a watch is never 400ed for sending one.

*/
export const EnsureThreadRequestBody = lazySchema(() =>
  z.object({
    /**
      * Attack Discovery 2.0 alert id the thread is derived from. Required and non-empty: `deriveThreadConversationId` fails closed on a blank or whitespace-only id, so a body carrying one could only ever derive `undefined`.

      */
    correlationId: z.string().max(1024).superRefine(isNonEmptyString),
    gateId: PndGateId,
  })
);
export type EnsureThreadRequestBody = z.infer<typeof EnsureThreadRequestBody>;
export type EnsureThreadRequestBodyInput = z.input<typeof EnsureThreadRequestBody>;

export const EnsureThreadResponse = lazySchema(() =>
  z.object({
    /**
      * Deterministic UUIDv5 thread id, `uuidv5(`${correlationId}:${gateId}`, PND_THREAD_NAMESPACE)`. Byte-for-byte the `threadConversationId` already carried on the paired proposal row.

      */
    threadConversationId: z.string().max(1024),
    /**
      * True when this call materialised the conversation, false when it already existed. The idempotency signal a retried workflow step reports with — a second `true` for one `(correlationId, gateId)` pair means D6's pre-read / in-flight map / post-failure re-read has a hole.

      */
    created: z.boolean(),
  })
);
export type EnsureThreadResponse = z.infer<typeof EnsureThreadResponse>;
