/*
 * Copyright Elasticsearch B.V. and/or licensed to Elasticsearch B.V. under one
 * or more contributor license agreements. Licensed under the Elastic License
 * 2.0; you may not use this file except in compliance with the Elastic License
 * 2.0.
 */

/*
 * NOTICE: Do not edit this file manually.
 * This file is automatically generated by the OpenAPI Generator, @kbn/openapi-generator.
 *
 * info:
 *   title: Derive PND Conversations API endpoint
 *   version: 1
 */

import { z, lazySchema } from '@kbn/zod/v4';

export const DeriveConversationIdsRequestQuery = lazySchema(() =>
  z.object({
    /**
     * Attack Discovery 2.0 alert id
     */
    correlationId: z.string().max(1024),
  })
);
export type DeriveConversationIdsRequestQuery = z.infer<typeof DeriveConversationIdsRequestQuery>;
export type DeriveConversationIdsRequestQueryInput = z.input<
  typeof DeriveConversationIdsRequestQuery
>;

export const DeriveConversationIdsResponse = lazySchema(() =>
  z.object({
    investigationConversationId: z.string().max(1024),
    incidentConversationId: z.string().max(1024),
    /**
      * Phase-4 conversation id, derived from the third fixed UUIDv5 namespace. Optional so the widened response can land before the route fills it in.

      */
    tuningConversationId: z.string().max(1024).optional(),
    /**
     * Rendered Attack Discovery markdown seeded into the conversation
     */
    attackDiscoveryMarkdown: z.string().max(100000),
    /**
      * Attack Discovery title. The watch titles the investigation, incident and tuning conversations from it alone: the kind each one renders with is re-derived from the namespace that minted its id, never read back out of the title.

      */
    attackDiscoveryTitle: z.string().max(200).optional(),
    /**
      * Agent Builder agent id for the phase-2 conversation. Returned over the wire rather than hardcoded in the YAML so agent existence and agent-id availability succeed or degrade together (ADR-011): when derive_ids exhausts its retries and `continue: true` fires, `agent-id` renders empty and the step falls back to the default agent instead of hard-failing on an agent that was never ensured.

      */
    investigationAgentId: z.string().max(1024).optional(),
    /**
     * Agent Builder agent id for the phase-3 conversation
     */
    incidentAgentId: z.string().max(1024).optional(),
    /**
     * Agent Builder agent id for the phase-4 conversation
     */
    tuningAgentId: z.string().max(1024).optional(),
    /**
      * Mirror of `xpack.pnd.demo.forceIncident` (default false). The Watch Floor ORs it with the model's real `isIncident` verdict, and the UI badges a run as demo mode when it is on. Absent renders empty, KQL yields false, and the condition falls through to the real verdict — so a broken demo switch can never silently force-escalate.

      */
    demoForceIncident: z.boolean().optional(),
  })
);
export type DeriveConversationIdsResponse = z.infer<typeof DeriveConversationIdsResponse>;
