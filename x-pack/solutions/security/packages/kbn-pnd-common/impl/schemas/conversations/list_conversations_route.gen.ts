/*
 * Copyright Elasticsearch B.V. and/or licensed to Elasticsearch B.V. under one
 * or more contributor license agreements. Licensed under the Elastic License
 * 2.0; you may not use this file except in compliance with the Elastic License
 * 2.0.
 */

/*
 * NOTICE: Do not edit this file manually.
 * This file is automatically generated by the OpenAPI Generator, @kbn/openapi-generator.
 *
 * info:
 *   title: List PND Conversations API endpoint
 *   version: 1
 */

import { z, lazySchema } from '@kbn/zod/v4';

import { PndConversation } from '../components/conversation.gen';

export const ListConversationsRequestQuery = lazySchema(() =>
  z.object({
    /**
      * Restrict to one conversation kind. When `incident` or `investigation`, the page of that kind is the group list; nested children of those parents (threads, and for incidents also tuning plus the originating investigation) are appended so the client can nest without a second unbounded read. `total` still counts only the requested kind.

      */
    kind: z.enum(['investigation', 'incident', 'tuning', 'thread']).optional(),
    /**
      * 1-based page of the requested kind. Omit together with perPage to return the full matching set. Has no default — callers that need the whole projection (lifecycle, queue) send neither.

      */
    page: z.coerce.number().int().min(1).optional(),
    /**
      * Page size of the requested kind, bounded to prevent unbounded-input reads. Omit together with page to return the full matching set.

      */
    perPage: z.coerce.number().int().min(1).max(100).optional(),
  })
);
export type ListConversationsRequestQuery = z.infer<typeof ListConversationsRequestQuery>;
export type ListConversationsRequestQueryInput = z.input<typeof ListConversationsRequestQuery>;

export const ListConversationsResponse = lazySchema(() =>
  z.object({
    conversations: z.array(PndConversation).max(10000),
    total: z.number().int().min(0),
  })
);
export type ListConversationsResponse = z.infer<typeof ListConversationsResponse>;
