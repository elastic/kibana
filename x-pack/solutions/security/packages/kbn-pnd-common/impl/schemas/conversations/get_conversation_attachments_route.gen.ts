/*
 * Copyright Elasticsearch B.V. and/or licensed to Elasticsearch B.V. under one
 * or more contributor license agreements. Licensed under the Elastic License
 * 2.0; you may not use this file except in compliance with the Elastic License
 * 2.0.
 */

/*
 * NOTICE: Do not edit this file manually.
 * This file is automatically generated by the OpenAPI Generator, @kbn/openapi-generator.
 *
 * info:
 *   title: Get PND Conversation attachments API endpoint
 *   version: 1
 */

import { z, lazySchema } from '@kbn/zod/v4';
import { isNonEmptyString } from '@kbn/zod-helpers/v4';

import { PndConversationAttachment } from '../components/attachment.gen';

export const GetConversationAttachmentsRequestQuery = lazySchema(() =>
  z.object({
    /**
      * Attack Discovery 2.0 alert id the conversation must be derivable from (security finding **S11**). Required on every PND conversation route: the handler asserts `conversationId` is a member of the set derived from this alert id, which is what keeps these routes from becoming a generic Agent Builder CRUD proxy reachable by any PND-privileged user.

      */
    correlationId: z.string().max(1024).superRefine(isNonEmptyString),
  })
);
export type GetConversationAttachmentsRequestQuery = z.infer<
  typeof GetConversationAttachmentsRequestQuery
>;
export type GetConversationAttachmentsRequestQueryInput = z.input<
  typeof GetConversationAttachmentsRequestQuery
>;

export const GetConversationAttachmentsRequestParams = lazySchema(() =>
  z.object({
    /**
     * Agent Builder conversation id
     */
    conversationId: z.string().max(1024),
  })
);
export type GetConversationAttachmentsRequestParams = z.infer<
  typeof GetConversationAttachmentsRequestParams
>;
export type GetConversationAttachmentsRequestParamsInput = z.input<
  typeof GetConversationAttachmentsRequestParams
>;

export const GetConversationAttachmentsResponse = lazySchema(() =>
  z.object({
    /**
      * Active attachments only, in Agent Builder's own order. Soft-deleted ones are excluded — PND never passes `include_deleted`.

      */
    attachments: z.array(PndConversationAttachment).max(100),
    total: z.number().int().min(0),
  })
);
export type GetConversationAttachmentsResponse = z.infer<typeof GetConversationAttachmentsResponse>;
