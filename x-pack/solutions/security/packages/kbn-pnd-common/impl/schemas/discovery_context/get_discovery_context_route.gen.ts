/*
 * Copyright Elasticsearch B.V. and/or licensed to Elasticsearch B.V. under one
 * or more contributor license agreements. Licensed under the Elastic License
 * 2.0; you may not use this file except in compliance with the Elastic License
 * 2.0.
 */

/*
 * NOTICE: Do not edit this file manually.
 * This file is automatically generated by the OpenAPI Generator, @kbn/openapi-generator.
 *
 * info:
 *   title: Get PND Discovery Context API endpoint
 *   version: 1
 */

import { z, lazySchema } from '@kbn/zod/v4';
import { ArrayFromString } from '@kbn/zod-helpers/v4';

import { PndDiscoveryContext } from '../components/discovery_context.gen';

export const GetDiscoveryContextRequestQuery = lazySchema(() =>
  z.object({
    /**
      * Attack Discovery 2.0 alert ids to enrich. Each id becomes an `ids` filter clause in a single aggregation, so the count is capped at `PND_DISCOVERY_CONTEXT_MAX_ALERT_IDS` (200) — enforced by the route, which returns a 400 above it, because `maxItems` cannot be expressed here. `@kbn/openapi-generator` renders a bounded query array as `ArrayFromString(...).max(n)`, and `ArrayFromString` returns a `z.preprocess` pipe that has no `.max`, so the generated codec throws at parse time. `kbn-inbox-common` hit the same wall on `list_history_route.schema.yaml` and resolved it the same way.

      */
    correlationIds: ArrayFromString(z.string().max(1024)),
  })
);
export type GetDiscoveryContextRequestQuery = z.infer<typeof GetDiscoveryContextRequestQuery>;
export type GetDiscoveryContextRequestQueryInput = z.input<typeof GetDiscoveryContextRequestQuery>;

export const GetDiscoveryContextResponse = lazySchema(() =>
  z.object({
    /**
      * One entry per readable, resolved discovery. Shorter than the request when an id is unreadable or has no constituent alerts; order is not significant, so a client keys on `correlationId`.

      */
    contexts: z.array(PndDiscoveryContext).max(200),
  })
);
export type GetDiscoveryContextResponse = z.infer<typeof GetDiscoveryContextResponse>;
