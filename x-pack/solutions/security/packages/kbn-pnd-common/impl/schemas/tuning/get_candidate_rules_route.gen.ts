/*
 * Copyright Elasticsearch B.V. and/or licensed to Elasticsearch B.V. under one
 * or more contributor license agreements. Licensed under the Elastic License
 * 2.0; you may not use this file except in compliance with the Elastic License
 * 2.0.
 */

/*
 * NOTICE: Do not edit this file manually.
 * This file is automatically generated by the OpenAPI Generator, @kbn/openapi-generator.
 *
 * info:
 *   title: Get PND Tuning Candidate Rules API endpoint
 *   version: 1
 */

import { z, lazySchema } from '@kbn/zod/v4';

import { PndCandidateRule } from '../components/tuning.gen';

export const GetCandidateRulesRequestQuery = lazySchema(() =>
  z.object({
    /**
      * The Attack Discovery 2.0 alert whose constituent alerts name the candidate rules. A single id rather than an array: the menu is per-draft, and one draft tunes the rules behind one discovery.

      */
    correlationId: z.string().max(1024),
    /**
      * Optional filter, matched against **either** `id` or `rule_id`. Present when the discovery-context surface already carries a `ruleRef` for the gate, in which case the draft is refining one known rule rather than choosing among them. An unmatched `ruleRef` yields `rules: []` rather than the unfiltered menu, so a stale ref cannot silently widen the draft's choice back to every rule.

      */
    ruleRef: z.string().max(1024).optional(),
  })
);
export type GetCandidateRulesRequestQuery = z.infer<typeof GetCandidateRulesRequestQuery>;
export type GetCandidateRulesRequestQueryInput = z.input<typeof GetCandidateRulesRequestQuery>;

export const GetCandidateRulesResponse = lazySchema(() =>
  z.object({
    /**
      * The distinct rules behind the discovery's alerts, capped at `PND_TUNING_CANDIDATE_RULES_MAX` (20). May be empty: the discovery resolved, but its alerts name no rule the caller can read, or `ruleRef` matched none.

      */
    rules: z.array(PndCandidateRule).max(20),
  })
);
export type GetCandidateRulesResponse = z.infer<typeof GetCandidateRulesResponse>;
