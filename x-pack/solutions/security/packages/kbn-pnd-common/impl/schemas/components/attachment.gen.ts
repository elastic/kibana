/*
 * Copyright Elasticsearch B.V. and/or licensed to Elasticsearch B.V. under one
 * or more contributor license agreements. Licensed under the Elastic License
 * 2.0; you may not use this file except in compliance with the Elastic License
 * 2.0.
 */

/*
 * NOTICE: Do not edit this file manually.
 * This file is automatically generated by the OpenAPI Generator, @kbn/openapi-generator.
 *
 * info:
 *   title: PND Conversation attachment component schemas
 *   version: 1
 */

import { z, lazySchema } from '@kbn/zod/v4';

/**
  * One Agent Builder conversation attachment, projected into PND's own shape (D10).

A **deliberately narrow projection**, not a passthrough of Agent Builder's `VersionedAttachment`. PND creates three `type: 'text'` attachments on a thread when `_ensure` materialises it (the Attack Discovery markdown, the proposed rule change and the backtest comparison) and reads them back through an S11-guarded proxy over `GET /api/agent_builder/conversations/{id}/attachments`. Re-declaring Agent Builder's versioning model in a Security-owned contract would create a second source of truth that drifts the moment the platform adds a field, so only what the Attachments tab renders is projected: identity, type, description, and the **current** version's text.

Every field is bounded, because the proxy forwards whatever Agent Builder returns.

  */
export const PndConversationAttachment = lazySchema(() =>
  z.object({
    /**
     * Agent Builder attachment id
     */
    id: z.string().max(1024),
    /**
      * Attachment type. Agent Builder declares this as an open `schema.string()` whose built-ins are `text`, `esql`, `screen_context` and `connector`, and contributors may add more — so this stays an open, bounded string rather than a closed enum PND does not own. PND itself only ever creates `text`.

      */
    type: z.string().max(256),
    /**
     * Human-readable description, e.g. "Attack Discovery"
     */
    description: z.string().max(1024).optional(),
    /**
      * Text content of the attachment's **current** version — Agent Builder's `versions[current_version].data.content` for a `text` attachment. Absent for any type whose data is not text, so the Attachments tab can list an attachment it cannot render inline rather than dropping it.

      */
    content: z.string().max(100000).optional(),
    /**
     * Agent Builder's `current_version` (version numbers start at 1)
     */
    version: z.number().int().min(1).optional(),
    /**
     * ISO 8601 timestamp of the current version (`created_at`)
     */
    createdAt: z.string().max(64).optional(),
  })
);
export type PndConversationAttachment = z.infer<typeof PndConversationAttachment>;
