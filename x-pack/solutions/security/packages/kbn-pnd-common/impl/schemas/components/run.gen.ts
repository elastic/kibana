/*
 * Copyright Elasticsearch B.V. and/or licensed to Elasticsearch B.V. under one
 * or more contributor license agreements. Licensed under the Elastic License
 * 2.0; you may not use this file except in compliance with the Elastic License
 * 2.0.
 */

/*
 * NOTICE: Do not edit this file manually.
 * This file is automatically generated by the OpenAPI Generator, @kbn/openapi-generator.
 *
 * info:
 *   title: PND Run component schemas
 *   version: 1
 */

import { z, lazySchema } from '@kbn/zod/v4';

/**
  * Closed lifecycle status for a PND orchestrator run. This is intentionally a closed enum (mirroring Attack Discovery 2.0's terminal-state branching) and is why WatchRecentRun.status (an open string) cannot be reused for a run row.

  */
export const PndRunStatus = lazySchema(() =>
  z.enum(['running', 'waiting_for_input', 'succeeded', 'failed', 'cancelled', 'timed_out'])
);
export type PndRunStatus = z.infer<typeof PndRunStatus>;
export type PndRunStatusEnum = typeof PndRunStatus.enum;
export const PndRunStatusEnum = PndRunStatus.enum;

export const PndRun = lazySchema(() =>
  z.object({
    executionId: z.string().max(1024),
    workflowId: z.string().max(1024),
    workflowRunId: z.string().max(1024),
    watchId: z.string().max(1024),
    /**
     * Attack Discovery 2.0 alert id that triggered this run
     */
    correlationId: z.string().max(1024),
    /**
     * ISO 8601 timestamp
     */
    startedAt: z.string().max(64),
    /**
     * ISO 8601 timestamp; absent while the run is non-terminal
     */
    endedAt: z.string().max(64).optional(),
    status: PndRunStatus,
    /**
     * Best-effort headline for run cards and tables
     */
    summary: z.string().max(4096),
    /**
     * Trigger id or actor that started the run
     */
    triggeredBy: z.string().max(1024).optional(),
    /**
     * Number of HITL gates currently awaiting input
     */
    pendingGateCount: z.number().int().min(0),
    /**
     * Failure or cancellation reason when terminal and not successful
     */
    reason: z.string().max(4096).optional(),
    /**
      * Path to this run's execution details, **relative to the Workflows app**: `/{workflowId}?tab=executions&executionId={workflowRunId}`, plus `&stepExecutionId={id}` when one step is the interesting one (plan F1). It is NOT an `/app/pnd` path, and it carries no app mount and no `/s/{space}` prefix — resolve it with `getUrlForApp('workflows', { path })` / `navigateToApp('workflows', { path })`, which add both, and never by concatenating `/app/workflows` by hand. Built by `buildRunDeepLink` (`pnd/server/routes/get/runs/helpers/build_run_deep_link`), which is authoritative for the shape.

      */
    deepLinkPath: z.string().max(2048),
  })
);
export type PndRun = z.infer<typeof PndRun>;
