/*
 * Copyright Elasticsearch B.V. and/or licensed to Elasticsearch B.V. under one
 * or more contributor license agreements. Licensed under the Elastic License
 * 2.0; you may not use this file except in compliance with the Elastic License
 * 2.0.
 */

/*
 * NOTICE: Do not edit this file manually.
 * This file is automatically generated by the OpenAPI Generator, @kbn/openapi-generator.
 *
 * info:
 *   title: PND Discovery context component schemas
 *   version: 1
 */

import { z, lazySchema } from '@kbn/zod/v4';

/**
  * One entity observed across an Attack Discovery's constituent detection alerts, from a `terms` aggregation over the alerts index (D2). Attack Discovery 2.0 itself has no structured entity field (G2), so the constituent alerts reached through `alert_ids` are the only structured source.

  */
export const PndDiscoveryContextEntity = lazySchema(() =>
  z.object({
    /**
     * Number of constituent alerts carrying this value
     */
    count: z.number().int().min(0),
    /**
     * ECS field the value was aggregated on, e.g. `host.name`
     */
    field: z.string().max(256),
    /**
     * The aggregated term, e.g. `host-a`
     */
    value: z.string().max(1024),
  })
);
export type PndDiscoveryContextEntity = z.infer<typeof PndDiscoveryContextEntity>;

/**
  * The blast radius and the normalized risk score for one Attack Discovery, derived together in a single aggregation so both surfaces share one react-query key (D10).

  */
export const PndDiscoveryContext = lazySchema(() =>
  z.object({
    correlationId: z.string().max(1024),
    /**
      * Merged entity terms, highest count first. Bounded because the aggregation behind it is: four `terms` sub-aggregations at `size: 20` can emit at most 80 today, and the cap leaves headroom for a fifth field without becoming unbounded.

      */
    entities: z.array(PndDiscoveryContextEntity).max(100),
    /**
      * MAX of the constituent detection alerts' `kibana.alert.risk_score`, which is naturally 0-100 (D5) — deliberately NOT the Attack Discovery's own `risk_score`, an unbounded *sum* that reaches four digits in production. Absent — never zero — for an uncorrelated run that has no constituent alerts, so a surface can tell "no score is possible" from "the score is zero".

      */
    riskScore: z.number().min(0).max(100).optional(),
  })
);
export type PndDiscoveryContext = z.infer<typeof PndDiscoveryContext>;
