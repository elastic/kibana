/*
 * Copyright Elasticsearch B.V. and/or licensed to Elasticsearch B.V. under one
 * or more contributor license agreements. Licensed under the Elastic License
 * 2.0; you may not use this file except in compliance with the Elastic License
 * 2.0.
 */

/*
 * NOTICE: Do not edit this file manually.
 * This file is automatically generated by the OpenAPI Generator, @kbn/openapi-generator.
 *
 * info:
 *   title: PND Gate component schemas
 *   version: 1
 */

import { z, lazySchema } from '@kbn/zod/v4';

/**
  * A registered HITL gate id — the `gateId` half of the `(correlationId, gateId)` key a `[Thread]` conversation is derived from (D1), and the same key `dedupeProposals` dedupes proposals on, which is why "one row per Proposal" and "one thread per Proposal" are the same guarantee.

A **closed enum matching `PND_GATE_IDS`**, never a free string, because `deriveThreadConversationId` fails closed on anything outside `PND_GATE_REGISTRY`: no thread id can exist for an unregistered gate, so no contract may claim one does. In particular the four `waitForInput` **step** ids (`await_open_investigation` …) are deliberately *not* members — they are a different vocabulary, and passing one where a gate id belongs derives `undefined`.

Shared by every surface that names a gate rather than copied per route: `PndConversation.gateId` (present only on the `thread` kind) and `EnsureThreadRequestBody.gateId`. `route_contracts.test.ts` pins the member list against `PND_GATE_IDS` itself, so the two cannot drift.

`PndProposalRow.gateId` is deliberately left as the free string it already was: widening a *response* field to a closed enum would make an older parked row unparseable rather than merely unrecognised.

  */
export const PndGateId = lazySchema(() =>
  z.enum(['open_investigation', 'promote_incident', 'incident_contained', 'apply_tuning'])
);
export type PndGateId = z.infer<typeof PndGateId>;
export type PndGateIdEnum = typeof PndGateId.enum;
export const PndGateIdEnum = PndGateId.enum;
