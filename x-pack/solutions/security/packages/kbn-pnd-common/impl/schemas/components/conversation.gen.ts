/*
 * Copyright Elasticsearch B.V. and/or licensed to Elasticsearch B.V. under one
 * or more contributor license agreements. Licensed under the Elastic License
 * 2.0; you may not use this file except in compliance with the Elastic License
 * 2.0.
 */

/*
 * NOTICE: Do not edit this file manually.
 * This file is automatically generated by the OpenAPI Generator, @kbn/openapi-generator.
 *
 * info:
 *   title: PND Conversation component schemas
 *   version: 1
 */

import { z, lazySchema } from '@kbn/zod/v4';

import { PndGateId } from './gate.gen';

/**
  * How a derived conversation relates to its parent. The camelCase field `parentConversationRelation` maps 1:1 onto Agent Builder PR #284458's `parent_conversation_relation`. That PR only carries `subagent`; PND needs these two members instead — the requirements artifact kibana-tjil.17 sends to workchat-eng.

`thread` — a HITL proposal thread hanging under a container (investigation or incident), recovered from `PND_GATE_REGISTRY.parentKind`.

`worker` — a specialist conversation hanging under the investigation (the tuning conversation today; a Deep Watch conversation minted under `PND_WORKER_NAMESPACE` when that watch writes to its own id).

Parentage is a read-time fold, stored nowhere: Agent Builder documents do not carry these fields, and PND does not write them.

  */
export const PndConversationRelation = lazySchema(() => z.enum(['thread', 'worker']));
export type PndConversationRelation = z.infer<typeof PndConversationRelation>;
export type PndConversationRelationEnum = typeof PndConversationRelation.enum;
export const PndConversationRelationEnum = PndConversationRelation.enum;

export const PndConversation = lazySchema(() =>
  z.object({
    /**
     * Deterministic UUIDv5 conversation id (see deriveConversationIds)
     */
    id: z.string().max(1024),
    /**
      * Which derived Agent Builder conversation this is. The first three are keyed on the Attack Discovery alert id alone, one per alert, and name the phase of the four-phase lifecycle they drive: the investigation (phase 2), the incident (phase 3) or the detection tuning (phase 4). `thread` is the fourth namespace (D1 / ADR-012): the conversation paired 1:1 with a single HITL proposal, keyed on `(correlationId, gateId)`, so an alert has one per registered gate rather than one in total. Inlined rather than shared as a component so it does not collide with the pre-existing PndConversationKind type, which models the not-found `undefined` case and is deliberately **not** widened with `thread` (it also names which of the three installed agents answers a thread, and there is no fourth agent).

      */
    kind: z.enum(['investigation', 'incident', 'tuning', 'thread']),
    gateId: PndGateId.optional(),
    /**
      * Id of the parent conversation, recovered on read. CamelCase maps 1:1 onto Agent Builder PR #284458's `parent_conversation_id`. Absent on container kinds. Never persisted — `parentOf` folds it from `correlationId`, `kind`, `gateId` and `PND_GATE_REGISTRY.parentKind`.

      */
    parentConversationId: z.string().max(1024).optional(),
    parentConversationRelation: PndConversationRelation.optional(),
    /**
      * Id of the originating investigation conversation, recovered on read when `kind` is `incident`. Absent on every other kind. Never persisted — `promotedFrom` folds it from `correlationId`. The investigation does not carry a reverse list of incidents.

      */
    promotedFrom: z.string().max(1024).optional(),
    title: z.string().max(1024),
    /**
      * Correlation id this conversation was derived from (Aug-19 decision 5). On the Attack Discovery path this is the producer alert id; it also keys an incident imported from elsewhere, which has no attack discovery.

      */
    correlationId: z.string().max(1024),
    /**
     * ISO 8601 timestamp
     */
    createdAt: z.string().max(64),
    /**
     * ISO 8601 timestamp
     */
    updatedAt: z.string().max(64),
  })
);
export type PndConversation = z.infer<typeof PndConversation>;
