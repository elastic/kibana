/*
 * Copyright Elasticsearch B.V. and/or licensed to Elasticsearch B.V. under one
 * or more contributor license agreements. Licensed under the Elastic License
 * 2.0; you may not use this file except in compliance with the Elastic License
 * 2.0.
 */

/*
 * NOTICE: Do not edit this file manually.
 * This file is automatically generated by the OpenAPI Generator, @kbn/openapi-generator.
 *
 * info:
 *   title: PND Execution projection component schemas
 *   version: 1
 */

import { z, lazySchema } from '@kbn/zod/v4';

/**
  * Closed status for one of the 14 four-phase catalog rows, aggregated across the Watch Floor and Post-Incident Watch executions.
An answered gate — whether a human or the autonomy auto-approver resumed it — is `completed`. There is one representation of an answered gate: the `waitForInput` step execution itself. Every gate always parks, so a skipped-wrapper marker is not a status the projection can emit.
`upstream` is a row whose work happens before PND is invoked — a catalog row with `liveness: 'upstream'`, performed by Attack Discovery or by existing Elastic Security rather than by a PND orchestrator step. It never carries a `stepExecutionId`, because no PND step execution realizes it, and it must never read `completed` or `not_started`: the first would claim a PND step ran, the second would suggest one is about to. It is resolved from the catalog alone rather than from any execution. It replaces `stubbed`, which kibana-phf4.12 retired with the lifecycle stub, and keeps `skipped` meaning only a genuine engine skip or cancel.

  */
export const PndPhaseStepStatus = lazySchema(() =>
  z.enum([
    'not_started',
    'running',
    'waiting_for_input',
    'completed',
    'failed',
    'skipped',
    'upstream',
  ])
);
export type PndPhaseStepStatus = z.infer<typeof PndPhaseStepStatus>;
export type PndPhaseStepStatusEnum = typeof PndPhaseStepStatus.enum;
export const PndPhaseStepStatusEnum = PndPhaseStepStatus.enum;

/**
  * Projection of a single phase-catalog step (keyed by phaseStepId) onto the orchestrator executions that realize it. Only phaseStepId and status are always present; the correlation fields are filled once a step has run.

  */
export const PndPhaseStepProjection = lazySchema(() =>
  z.object({
    /**
     * Phase-catalog step id (see PHASE_CATALOG)
     */
    phaseStepId: z.string().max(1024),
    status: PndPhaseStepStatus,
    workflowId: z.string().max(1024).optional(),
    workflowRunId: z.string().max(1024).optional(),
    stepExecutionId: z.string().max(1024).optional(),
    /**
     * ISO 8601 timestamp
     */
    startedAt: z.string().max(64).optional(),
    /**
     * ISO 8601 timestamp
     */
    finishedAt: z.string().max(64).optional(),
    /**
      * Path to this row's execution details, **relative to the Workflows app**: `/{workflowId}?tab=executions&executionId={workflowRunId}&stepExecutionId={id}` (plan F1 — without the third param every row of a response shares one execution-level link). It is NOT an `/app/pnd` path, and it carries no app mount and no `/s/{space}` prefix — resolve it with `getUrlForApp('workflows', { path })` / `navigateToApp('workflows', { path })`, which add both, and never by concatenating `/app/workflows` by hand. Built by `buildRunDeepLink` (`pnd/server/routes/get/runs/helpers/build_run_deep_link`), which is authoritative for the shape and omits the reserved `__overview` / `trigger` step ids.

      */
    deepLinkPath: z.string().max(2048).optional(),
  })
);
export type PndPhaseStepProjection = z.infer<typeof PndPhaseStepProjection>;
