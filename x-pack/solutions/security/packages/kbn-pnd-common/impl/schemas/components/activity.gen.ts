/*
 * Copyright Elasticsearch B.V. and/or licensed to Elasticsearch B.V. under one
 * or more contributor license agreements. Licensed under the Elastic License
 * 2.0; you may not use this file except in compliance with the Elastic License
 * 2.0.
 */

/*
 * NOTICE: Do not edit this file manually.
 * This file is automatically generated by the OpenAPI Generator, @kbn/openapi-generator.
 *
 * info:
 *   title: PND Proposal activity component schemas
 *   version: 1
 */

import { z, lazySchema } from '@kbn/zod/v4';

/**
  * Gates opened in one bucket, per recommended action. Every action is required so a tile can read `counts[action]` without a lookup that misses, and so a quiet hour renders as a zero rather than as a gap in the sparkline. Keyed on the four `RECOMMENDED_ACTIONS`; the server joins `stepId` to `gate.recommendedAction` through PND_GATE_REGISTRY in JS, because category is not a mapped Elasticsearch field (G4).

  */
export const PndActivityCounts = lazySchema(() =>
  z.object({
    contain: z.number().int().min(0),
    escalate: z.number().int().min(0),
    investigate: z.number().int().min(0),
    tune: z.number().int().min(0),
  })
);
export type PndActivityCounts = z.infer<typeof PndActivityCounts>;

/**
 * One hour of the 24h sparkline series.
 */
export const PndActivityBucket = lazySchema(() =>
  z.object({
    counts: PndActivityCounts,
    /**
      * Epoch milliseconds at the start of the hour, UTC — the `date_histogram` bucket key, passed straight to `@elastic/charts` as a time-scaled x value.

      */
    time: z.number().int().min(0),
  })
);
export type PndActivityBucket = z.infer<typeof PndActivityBucket>;
