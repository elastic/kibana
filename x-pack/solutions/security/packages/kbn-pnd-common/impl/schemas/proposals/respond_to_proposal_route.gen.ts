/*
 * Copyright Elasticsearch B.V. and/or licensed to Elasticsearch B.V. under one
 * or more contributor license agreements. Licensed under the Elastic License
 * 2.0; you may not use this file except in compliance with the Elastic License
 * 2.0.
 */

/*
 * NOTICE: Do not edit this file manually.
 * This file is automatically generated by the OpenAPI Generator, @kbn/openapi-generator.
 *
 * info:
 *   title: Respond to PND Proposal API endpoint
 *   version: 1
 */

import { z, lazySchema } from '@kbn/zod/v4';
import { isNonEmptyString } from '@kbn/zod-helpers/v4';

export const RespondToProposalRequestParams = lazySchema(() =>
  z.object({
    /**
     * Opaque id of the pending gate being responded to
     */
    sourceId: z.string().max(1024),
  })
);
export type RespondToProposalRequestParams = z.infer<typeof RespondToProposalRequestParams>;
export type RespondToProposalRequestParamsInput = z.input<typeof RespondToProposalRequestParams>;

export const RespondToProposalRequestBody = lazySchema(() =>
  z.object({
    input: z
      .object({
        /**
      * The human's decision (security finding D2). Required and closed: a body carrying only a rationale used to proceed as an **approval**, and so did a capitalized "Dismiss", while the orchestrator YAMLs only ever match `decision : "dismiss"`. That was fail-open on a consequential path.

      */
        decision: z.enum(['approve', 'dismiss']),
        /**
      * Required, non-empty human rationale for the decision. Mandatory for a dismissal too — there is no rationale-free path through a gate.

      */
        rationale: z.string().max(2000).superRefine(isNonEmptyString),
        /**
      * The recommended actions the analyst toggled on, echoed back for the containment gate. Only the gate's own workflow reads them; a gate whose schema does not declare the key has it stripped by the engine on resume.

      */
        approved_actions: z.array(z.record(z.string(), z.unknown())).max(50).optional(),
      })
      .strict(),
  })
);
export type RespondToProposalRequestBody = z.infer<typeof RespondToProposalRequestBody>;
export type RespondToProposalRequestBodyInput = z.input<typeof RespondToProposalRequestBody>;

export const RespondToProposalResponse = lazySchema(() =>
  z.object({
    sourceId: z.string().max(1024),
    /**
     * True when the paused workflow step was resumed
     */
    resumed: z.boolean(),
  })
);
export type RespondToProposalResponse = z.infer<typeof RespondToProposalResponse>;
