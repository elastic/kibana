/*
 * Copyright Elasticsearch B.V. and/or licensed to Elasticsearch B.V. under one
 * or more contributor license agreements. Licensed under the Elastic License
 * 2.0; you may not use this file except in compliance with the Elastic License
 * 2.0.
 */

/*
 * NOTICE: Do not edit this file manually.
 * This file is automatically generated by the OpenAPI Generator, @kbn/openapi-generator.
 *
 * info:
 *   title: PND Attack Discovery FP/TP analysis failure schema
 *   version: 1
 */

import { z, lazySchema } from '@kbn/zod/v4';

export const FpTpFailureError = lazySchema(() =>
  z
    .object({
      /**
       * Machine-readable failure code (never a classification)
       */
      code: z.string().min(1).max(128),
      message: z.string().min(1).max(4000),
      retryable: z.boolean(),
    })
    .strict()
);
export type FpTpFailureError = z.infer<typeof FpTpFailureError>;

/**
  * Operational failure. Must not include a classification field — unavailable data sources, permissions, tool failures, and timeouts never surface as false_positive.

  */
export const FpTpFailure = lazySchema(() =>
  z
    .object({
      status: z.literal('failed'),
      attack_discovery_id: z.string().min(1).max(1024),
      investigation_id: z.string().min(1).max(1024),
      error: FpTpFailureError,
    })
    .strict()
);
export type FpTpFailure = z.infer<typeof FpTpFailure>;
