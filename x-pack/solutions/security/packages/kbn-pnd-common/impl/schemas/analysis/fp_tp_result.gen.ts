/*
 * Copyright Elasticsearch B.V. and/or licensed to Elasticsearch B.V. under one
 * or more contributor license agreements. Licensed under the Elastic License
 * 2.0; you may not use this file except in compliance with the Elastic License
 * 2.0.
 */

/*
 * NOTICE: Do not edit this file manually.
 * This file is automatically generated by the OpenAPI Generator, @kbn/openapi-generator.
 *
 * info:
 *   title: PND Attack Discovery FP/TP analysis result schemas
 *   version: 1
 */

import { z, lazySchema } from '@kbn/zod/v4';

/**
 * Completed analysis classification. Distinct from operational failure.
 */
export const FpTpClassification = lazySchema(() =>
  z.enum(['true_positive', 'false_positive', 'inconclusive'])
);
export type FpTpClassification = z.infer<typeof FpTpClassification>;
export type FpTpClassificationEnum = typeof FpTpClassification.enum;
export const FpTpClassificationEnum = FpTpClassification.enum;

/**
 * How a corroborating evidence item relates to the classification.
 */
export const FpTpEvidenceResult = lazySchema(() => z.enum(['supports', 'contradicts', 'neutral']));
export type FpTpEvidenceResult = z.infer<typeof FpTpEvidenceResult>;
export type FpTpEvidenceResultEnum = typeof FpTpEvidenceResult.enum;
export const FpTpEvidenceResultEnum = FpTpEvidenceResult.enum;

/**
 * Outcome of a configured corroboration check.
 */
export const FpTpCheckStatus = lazySchema(() => z.enum(['completed', 'skipped', 'failed']));
export type FpTpCheckStatus = z.infer<typeof FpTpCheckStatus>;
export type FpTpCheckStatusEnum = typeof FpTpCheckStatus.enum;
export const FpTpCheckStatusEnum = FpTpCheckStatus.enum;

export const FpTpCorroboratingEvidence = lazySchema(() =>
  z
    .object({
      id: z.string().min(1).max(128),
      /**
       * Evidence source (e.g. attack_discovery, related_alerts)
       */
      source: z.string().min(1).max(256),
      result: FpTpEvidenceResult,
      details: z.string().max(4000).optional(),
    })
    .strict()
);
export type FpTpCorroboratingEvidence = z.infer<typeof FpTpCorroboratingEvidence>;

export const FpTpCheck = lazySchema(() =>
  z
    .object({
      id: z.string().min(1).max(128),
      name: z.string().max(256).optional(),
      status: FpTpCheckStatus,
      /**
       * Why the check completed, was skipped, or failed.
       */
      details: z.string().max(4000).optional(),
    })
    .strict()
);
export type FpTpCheck = z.infer<typeof FpTpCheck>;

export const FpTpAnalysisMetadata = lazySchema(() =>
  z
    .object({
      workflow_id: z.string().min(1).max(256),
      workflow_execution_id: z.string().min(1).max(256),
      configuration_version: z.string().min(1).max(64),
      /**
       * ISO 8601 timestamp when analysis finished
       */
      completed_at: z.string().min(1).max(64),
    })
    .strict()
);
export type FpTpAnalysisMetadata = z.infer<typeof FpTpAnalysisMetadata>;

export const FpTpResult = lazySchema(() =>
  z
    .object({
      schema_version: z.literal('1'),
      attack_discovery_id: z.string().min(1).max(1024),
      investigation_id: z.string().min(1).max(1024),
      classification: FpTpClassification,
      summary: z.string().min(1).max(8000),
      reasoning: z.string().min(1).max(16000),
      corroborating_evidence: z.array(FpTpCorroboratingEvidence).max(50),
      caveats: z.array(z.string().max(2000)).max(20),
      checks: z.array(FpTpCheck).max(20),
      analysis_metadata: FpTpAnalysisMetadata,
    })
    .strict()
);
export type FpTpResult = z.infer<typeof FpTpResult>;
