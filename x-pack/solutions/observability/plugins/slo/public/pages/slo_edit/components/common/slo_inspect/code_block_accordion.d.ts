import type { ReactNode } from 'react';
import React from 'react';
export declare function CodeBlockAccordion({ id, label, json, extraAction, children, }: {
    id: string;
    label: string;
    json?: any;
    extraAction?: ReactNode;
    children?: ReactNode;
}): React.JSX.Element;
