import type { QuerySchema } from '@kbn/slo-schema';
import React from 'react';
export declare function DisplayQuery({ query, index }: {
    query?: QuerySchema;
    index: string;
}): React.JSX.Element;
