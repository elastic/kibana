import type { ReactNode } from 'react';
import React from 'react';
export interface Props {
    title: ReactNode;
    subtitle: ReactNode;
}
export declare function DefinitionItem({ title, subtitle }: Props): React.JSX.Element;
