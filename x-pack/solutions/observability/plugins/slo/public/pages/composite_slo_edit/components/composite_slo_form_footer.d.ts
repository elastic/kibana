import React from 'react';
interface Props {
    compositeSloId?: string;
    isEditMode: boolean;
}
export declare function CompositeSloFormFooter({ compositeSloId, isEditMode }: Props): React.JSX.Element;
export {};
