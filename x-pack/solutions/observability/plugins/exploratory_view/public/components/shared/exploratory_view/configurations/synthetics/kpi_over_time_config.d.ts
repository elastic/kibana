import type { ConfigProps, SeriesConfig } from '../../types';
export declare const isStepLevelMetric: (metric?: string) => boolean;
export declare function getSyntheticsKPIConfig({ dataView }: ConfigProps): SeriesConfig;
