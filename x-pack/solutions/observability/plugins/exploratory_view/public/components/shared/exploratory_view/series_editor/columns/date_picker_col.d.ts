import React from 'react';
import type { SeriesUrl } from '../../types';
interface Props {
    seriesId: number;
    series: SeriesUrl;
}
export declare function DatePickerCol({ seriesId, series }: Props): React.JSX.Element | null;
export {};
