import React from 'react';
import type { SeriesConfig, SeriesUrl } from '../../types';
export declare function ReportDefinitionCol({ seriesId, series, seriesConfig, }: {
    seriesId: number;
    series: SeriesUrl;
    seriesConfig: SeriesConfig;
}): React.JSX.Element;
