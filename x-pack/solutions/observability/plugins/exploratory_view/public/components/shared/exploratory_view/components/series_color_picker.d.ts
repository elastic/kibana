import React from 'react';
import type { SeriesUrl } from '../types';
export declare function SeriesColorPicker({ seriesId, series }: {
    seriesId: number;
    series: SeriesUrl;
}): React.JSX.Element;
