import React from 'react';
import type { SeriesUrl } from '../../types';
interface Props {
    seriesId: number;
    series: SeriesUrl;
}
export declare function SeriesDatePicker({ series, seriesId }: Props): React.JSX.Element;
export {};
