import React from 'react';
interface Props {
    value: number;
    append?: string;
    prepend?: string;
}
export declare function Label({ value, prepend, append }: Props): React.JSX.Element;
export {};
