import type { CoreSetup, Logger } from '@kbn/core/server';
export declare function registerAnnotationAPIs({ core, index, logger, }: {
    core: CoreSetup;
    index: string;
    logger: Logger;
}): void;
