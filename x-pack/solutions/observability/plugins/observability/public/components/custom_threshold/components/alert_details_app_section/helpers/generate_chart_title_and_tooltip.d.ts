import type { MetricExpression } from '../../../types';
export declare const generateChartTitleAndTooltip: (criterion: MetricExpression, chartTitleLimit?: number) => {
    tooltip: string;
    title: string;
};
