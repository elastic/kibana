import React from 'react';
export declare function ColorPaletteFlexItem({ hexCode, inFocus, percentage, tooltip, position, }: {
    hexCode: string;
    position: number;
    inFocus: boolean;
    percentage: number;
    tooltip: string;
}): React.JSX.Element;
