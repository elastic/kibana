import React from 'react';
interface Props {
    apmServerUrl?: string;
    secretToken?: string;
}
export declare function OpenTelemetryInstructions({ apmServerUrl, secretToken }: Props): React.JSX.Element;
export {};
