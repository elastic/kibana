import React from 'react';
interface Props {
    canReadAlerts: boolean;
    canSaveAlerts: boolean;
    canReadMlJobs: boolean;
}
export declare function AlertingPopoverAndFlyout({ canSaveAlerts, canReadAlerts, canReadMlJobs }: Props): React.JSX.Element;
export {};
