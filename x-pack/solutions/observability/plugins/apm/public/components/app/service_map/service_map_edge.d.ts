import React from 'react';
import { type EdgeProps } from '@xyflow/react';
export declare const ServiceMapEdge: React.MemoExoticComponent<({ id, sourceX, sourceY, targetX, targetY, sourcePosition, targetPosition, style, markerEnd, markerStart, }: EdgeProps) => React.JSX.Element>;
