import React from 'react';
export declare function MobileTreemap({ kuery, serviceName, start, end, environment, }: {
    kuery: string;
    serviceName: string;
    start: string;
    end: string;
    environment: string;
}): React.JSX.Element;
