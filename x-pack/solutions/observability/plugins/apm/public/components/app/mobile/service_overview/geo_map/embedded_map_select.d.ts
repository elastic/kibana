import React from 'react';
import { MapTypes } from '../../../../../../common/mobile/constants';
export declare function EmbeddedMapSelect({ selectedMap, onChange, }: {
    selectedMap: MapTypes;
    onChange: (value: MapTypes) => void;
}): React.JSX.Element;
