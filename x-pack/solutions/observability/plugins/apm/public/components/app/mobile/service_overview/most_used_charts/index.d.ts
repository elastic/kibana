import React from 'react';
export declare function MostUsedCharts({ start, end, kuery, environment, transactionType, serviceName, }: {
    start: string;
    end: string;
    kuery: string;
    environment: string;
    transactionType?: string;
    serviceName: string;
}): React.JSX.Element;
