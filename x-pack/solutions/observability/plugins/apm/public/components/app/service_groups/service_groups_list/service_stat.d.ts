import type { PropsWithChildren } from 'react';
import React from 'react';
interface Props {
    loading: boolean;
    grow?: boolean;
}
export declare function ServiceStat({ loading, grow, children }: PropsWithChildren<Props>): React.JSX.Element;
export {};
