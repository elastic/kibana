import React from 'react';
interface Props {
    items: string[];
    multipleItemsMessage?: string;
}
export declare function ItemsBadge({ items, multipleItemsMessage, }: Props): React.JSX.Element;
export {};
