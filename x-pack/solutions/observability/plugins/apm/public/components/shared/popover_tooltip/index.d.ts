import React from 'react';
interface PopoverTooltipProps {
    ariaLabel?: string;
    iconType?: string;
    children: React.ReactNode;
}
export declare function PopoverTooltip({ ariaLabel, iconType, children, }: PopoverTooltipProps): React.JSX.Element;
export {};
