import React from 'react';
import type { Transaction } from '../../../../../typings/es_schemas/ui/transaction';
interface Props {
    transaction: Transaction;
}
export declare function TransactionMetadata({ transaction }: Props): React.JSX.Element;
export {};
