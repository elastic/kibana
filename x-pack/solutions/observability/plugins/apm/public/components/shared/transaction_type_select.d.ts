import React from 'react';
import type { CSSObject } from '@emotion/react';
export declare function TransactionTypeSelect({ compressed, hideLabel, fullWidth, cssOverride, }: {
    compressed?: boolean;
    hideLabel?: boolean;
    fullWidth?: boolean;
    cssOverride?: CSSObject;
}): React.JSX.Element;
