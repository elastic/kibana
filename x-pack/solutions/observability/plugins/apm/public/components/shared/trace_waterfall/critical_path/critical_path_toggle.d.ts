import React from 'react';
interface Props {
    checked: boolean;
    onChange: (checked: boolean) => void;
}
export declare function CriticalPathToggle({ checked, onChange }: Props): React.JSX.Element;
export {};
