type: object
required:
  - service
  - settings
properties:
  agent_name:
    type: string
    description: Agent name
  service:
    $ref: 'service_object.yaml'
  settings:
    $ref: 'settings_object.yaml'
