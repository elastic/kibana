import React from 'react';
interface Props {
    value: React.ReactNode;
    tooltipContent: string;
}
export declare function CellTooltip({ value, tooltipContent }: Props): React.JSX.Element;
export {};
