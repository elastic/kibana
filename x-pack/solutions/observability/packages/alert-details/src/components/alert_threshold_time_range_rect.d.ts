import React from 'react';
interface Props {
    color: string;
    id: string;
    threshold: number;
}
export declare function AlertThresholdTimeRangeRect({ color, id, threshold }: Props): React.JSX.Element;
export {};
