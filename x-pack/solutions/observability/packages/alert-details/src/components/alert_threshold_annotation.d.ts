import React from 'react';
interface Props {
    id: string;
    threshold: number;
    color: string;
}
export declare function AlertThresholdAnnotation({ threshold, color, id }: Props): React.JSX.Element;
export {};
