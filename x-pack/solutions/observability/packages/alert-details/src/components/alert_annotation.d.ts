import React from 'react';
interface Props {
    alertStart: number;
    color: string;
    dateFormat: string;
    id: string;
}
export declare function AlertAnnotation({ alertStart, color, dateFormat, id }: Props): React.JSX.Element;
export {};
