# @kbn/apm-embeddable-common

Shared constants for APM embeddable panels.

Extracted so that consumers (e.g. the SLO plugin) can reference APM
embeddable IDs without taking a direct dependency on the APM plugin.
