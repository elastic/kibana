server:
  port: ${PORT}
  host: ${HOST}

kibana:
  hosts:
    - ${KIBANA_URL}/health/ok
    - ${KIBANA_URL}/health/redirect
    - ${KIBANA_URL}/health/unauthorized

logging:
  root:
    appenders: ['console']
    level: 'all'
