server:
  port: ${PORT}
  host: ${HOST}

kibana:
  hosts:
    - ${KIBANA_URL}/api/status/ok
    - ${KIBANA_URL}/api/status/redirect
    - ${KIBANA_URL}/api/status/unauthorized

logging:
  root:
    appenders: ['console']
    level: 'all'
