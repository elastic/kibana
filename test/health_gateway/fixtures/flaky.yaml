server:
  port: ${PORT}
  host: ${HOST}

kibana:
  hosts:
    - ${KIBANA_URL}/api/status/ok
    - ${KIBANA_URL}/api/status/flaky?session=${SESSION}

logging:
  root:
    appenders: ['console']
    level: 'all'
