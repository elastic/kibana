server:
  port: ${PORT}
  host: ${HOST}

kibana:
  hosts:
    - ${KIBANA_URL}/health/ok
    - ${KIBANA_URL}/health/flaky?session=${SESSION}

logging:
  root:
    appenders: ['console']
    level: 'all'
