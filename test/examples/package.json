{
  "author": "Elastic",
  "name": "@kbn/serverless-observability",
  "version": "1.0.0",
  "private": true,
  "license": "Elastic License 2.0",
  "scripts": {
    "cypress:open": "NODE_OPTIONS=--openssl-legacy-provider node ../../node_modules/.bin/cypress open --e2e --config-file ./cypress.config.ts",
    "cypress:run": "NODE_OPTIONS=--openssl-legacy-provider node ../../node_modules/.bin/cypress run --e2e --browser chrome --config-file ./cypress.config.ts"
  }
}