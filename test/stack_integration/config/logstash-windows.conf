input { generator { message => "hello" } }

filter {
  sleep { time => 10 }
}

output {
  elasticsearch {
    hosts => ["localhost:9200"]
    user => "elastic"
    password => "changeit"
    #ssl => true
    #cacert => "config\ca.crt"
  }
}
