$schema: https://moonrepo.dev/schemas/project.json
id: '@kbn/capture-oas-snapshot-cli'
type: unknown
owners:
  defaultOwner: '@elastic/kibana-core'
toolchain:
  default: node
language: typescript
project:
  name: '@kbn/capture-oas-snapshot-cli'
  description: Moon project for @kbn/capture-oas-snapshot-cli
  channel: ''
  owner: '@elastic/kibana-core'
  metadata:
    sourceRoot: packages/kbn-capture-oas-snapshot-cli
tags:
  - common
  - shared-common
  - dev
fileGroups:
  src:
    - '**/*.ts'
    - '!target/**/*'
tasks:
  jest:
    args:
      - '--config'
      - $projectRoot/jest.config.js
    inputs:
      - '@group(src)'
  jestCI:
    args:
      - '--config'
      - $projectRoot/jest.config.js
    inputs:
      - '@group(src)'
