{
  "name": "@elastic/datemath",
  "version": "5.0.2",
  "description": "elasticsearch datemath parser, used in kibana",
  "license": "Apache-2.0",
  "main": "target/index.js",
  "typings": "scr/index.d.ts",
  "scripts": {
    "build": "babel src --out-dir target --copy-files --extensions \".ts\"",
    "kbn:bootstrap": "yarn build --quiet",
    "kbn:watch": "yarn build --watch"
  },
  "devDependencies": {
    "@babel/cli": "^7.2.3",
    "@babel/core": "^7.2.2",
    "@babel/preset-typescript": "^7.1.0",
    "moment": "^2.13.0"
  },
  "dependencies": {
    "tslib": "^1.9.3"
  },
  "peerDependencies": {
    "moment": "^2.13.0"
  }
}
