openapi: 3.0.0
info:
  title: oneof_body_tightened
  version: '1'
paths:
  /api/test:
    post:
      requestBody:
        content:
          application/json:
            schema:
              oneOf:
                - $ref: '#/components/schemas/A'
                - $ref: '#/components/schemas/B'
      responses:
        '200':
          description: ok
components:
  schemas:
    A:
      type: object
      additionalProperties: false
      properties:
        a:
          type: string
    B:
      type: object
      properties:
        b:
          type: string
