openapi: 3.0.0
info:
  title: component_body_tightened
  version: '1'
paths:
  /api/test:
    post:
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/Body'
      responses:
        '200':
          description: ok
components:
  schemas:
    Body:
      type: object
      additionalProperties: false
      properties:
        a:
          type: string
