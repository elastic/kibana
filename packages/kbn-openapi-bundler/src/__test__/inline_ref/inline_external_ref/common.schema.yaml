openapi: 3.0.3
info:
  title: Test endpoint
  version: 'not applicable'
paths: {}

components:
  schemas:
    TestSchema1:
      x-inline: true
      type: object
      properties:
        field1:
          type: string
          enum: [value1]
        field2:
          type: integer
          minimum: 1

    TestSchema2:
      x-inline: false
      type: object
      properties:
        field1:
          type: string
          enum: [value1]
        field2:
          type: integer
          minimum: 1

    TestSchema3:
      type: object
      properties:
        field1:
          type: string
          enum: [value1]
        field2:
          type: integer
          minimum: 1
