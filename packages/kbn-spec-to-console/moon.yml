# This file is generated by the @kbn/moon package. Any manual edits will be erased!
#  To extend this, write your extensions/overrides to 'moon.extend.yml'
#  then regenerate this file with: 'node scripts/regenerate_moon_projects.js --update --filter @kbn/spec-to-console'

$schema: https://moonrepo.dev/schemas/project.json
id: '@kbn/spec-to-console'
type: unknown
owners:
  defaultOwner: '@elastic/kibana-management'
toolchain:
  default: node
language: javascript
project:
  name: '@kbn/spec-to-console'
  description: Moon project for @kbn/spec-to-console
  channel: ''
  owner: '@elastic/kibana-management'
  metadata:
    sourceRoot: packages/kbn-spec-to-console
tags:
  - shared-common
  - package
  - dev
  - group-undefined
  - jest-unit-tests
tasks:
  jest:
    args:
      - '--config'
      - $projectRoot/jest.config.js
    inputs:
      - '@group(src)'
  jestCI:
    args:
      - '--config'
      - $projectRoot/jest.config.js
    inputs:
      - '@group(src)'
