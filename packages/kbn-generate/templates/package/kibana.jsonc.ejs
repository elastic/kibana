{
  "type": "shared-common",
  "id": <%- json(pkg.id) %>,
  "owner": <%- json(pkg.owner) %>,<% if (pkg.dev) { %>,
  "devOnly": true<% } else { %>
  "group": <%- json(pkg.group) %>,
  "visibility": <%- json(pkg.visibility) %>,
    <% } %>
}
