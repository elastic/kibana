import { <%= connector.className %> } from './<%= connector.name %>';

describe('<%= connector.className %>', () => {
  it('should be defined', () => {
    expect(<%= connector.className %>).toBeDefined();
  });
});
