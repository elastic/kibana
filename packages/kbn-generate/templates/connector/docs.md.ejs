---
navigation_title: "<%= connector.displayName %>"
applies_to:
  stack: preview <%= connector.version %>
  serverless: preview
---

# <%= connector.displayName %> connector [<%= connector.kebabName %>-action-type]

The <%= connector.displayName %> connector communicates with the <%= connector.displayName %> API.

## Create connectors in {{kib}} [define-<%= connector.kebabName %>-ui]

You can create connectors in **{{stack-manage-app}} > {{connectors-ui}}**. For example:

### Connector configuration [<%= connector.kebabName %>-connector-configuration]

<%= connector.displayName %> connectors have the following configuration properties:

<!-- TODO: Add action descriptions here -->
<!-- Example:
API Key
:   The <%= connector.displayName %> API key for authentication.
-->


## Get API credentials [<%= connector.kebabName %>-api-credentials]

To use the <%= connector.displayName %> connector, you need to:

<!-- TODO: Add instructions here -->
