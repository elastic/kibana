/*
 * Copyright Elasticsearch B.V. and/or licensed to Elasticsearch B.V. under one
 * or more contributor license agreements. Licensed under the Elastic License
 * 2.0; you may not use this file except in compliance with the Elastic License
 * 2.0.
 */

import type { HttpFetchOptions } from '@kbn/core-http-browser';
{{#each files}}
import { {{methods}} } from '{{file}}';
{{/each}}

interface HttpClient {
  fetch<T>(url: string, options: HttpFetchOptions): Promise<T>;
}

export class ApiClient<TClient extends HttpClient> {

  private client: TClient;

  constructor(client: TClient) {
    this.client = client;
  }

  {{#each files}}
  public async {{methods}}(params, signal) {
    return {{methods}}(this.client, params, signal);
  }

  {{/each}}

}