# @kbn/check-kibana-settings-cli

CLI tool that can collect and list all of the Kibana settings.
It compares the list of settings with those allowed/denied in the `cloud` repository, showing the missing ones.
