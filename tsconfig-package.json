{
  // This config is intended for us in packages

  "extends": "./tsconfig",
  "compilerOptions": {
    // The target must be something that runs natively in Kibana
    "target": "es2015",
    "declaration": true
  }
}
