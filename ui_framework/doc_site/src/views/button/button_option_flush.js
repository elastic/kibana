import React from 'react';

import {
  KuiButtonOption,
} from '../../../../components';

export default () => (
  <div>
    <KuiButtonOption flush="left">
      Flush left
    </KuiButtonOption>

    <KuiButtonOption flush="right">
      Flush right
    </KuiButtonOption>
  </div>
);
