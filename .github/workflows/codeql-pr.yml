name: "CodeQL PR"

on:
  pull_request:
    branches: [ "main" ]
    types: [ opened, ready_for_review ]

jobs:
  analyze:
    name: Analyze (${{ matrix.language }})
    runs-on: kibana
    # Hardcoded list of users will be removed once testing is complete
    if: |
      contains(fromJSON('["elena-shostak", "SiddharthMantri", "azasypkin", "kc13greiner", "jeramysoucy", "rgodfrey-elastic"]'), github.event.pull_request.user.login) ||
      (contains(github.event.pull_request.labels.*.name, 'ci:codeql') &&
      contains(fromJSON('["MEMBER", "COLLABORATOR", "CONTRIBUTOR"]'), github.event.pull_request.author_association) &&
      github.event.pull_request.user.type != 'Bot' &&
      github.event.pull_request.draft == false)
    permissions:
      security-events: write
      packages: read
      actions: read
      contents: read

    strategy:
      fail-fast: false
      matrix:
        language: [ 'javascript' ]
    steps:
    - name: Checkout repository
      uses: actions/checkout@08c6903cd8c0fde910a37f88322edcfb5dd907a8 # v5.0.0

    - name: Initialize CodeQL
      uses: github/codeql-action/init@f443b600d91635bebf5b0d9ebc620189c0d6fba5 # v4.30.8
      with:
        languages: ${{ matrix.language }}
        config-file: ./.github/codeql/codeql-config.yml
    - name: Perform CodeQL Analysis
      uses: github/codeql-action/analyze@f443b600d91635bebf5b0d9ebc620189c0d6fba5 # v4.30.8
      env:
        CODEQL_EXTRACTOR_JAVASCRIPT_OPTION_SKIP_TYPES: true
      with:
        category: "/language:${{matrix.language}}"
