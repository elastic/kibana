# PR title

Show only OpenTelemetry for Kubernetes quickstart

# PR description

Updates the Kubernetes Add Data category to show OpenTelemetry as the only featured quickstart. Keeps Elastic Agent Kubernetes available by direct URL and updates the Scout and Ensemble-backed tests so EA coverage no longer depends on the removed quickstart card.
