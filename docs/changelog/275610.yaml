prs:
- https://github.com/elastic/kibana/pull/275610
type: enhancement
products:
- product: cloud-serverless
  target: 2026-07-14
areas:
- Kibana platform
title: "Add certificate identity field to cross-cluster API keys"
description: "Cross-cluster API keys can now be created and updated with a certificate identity (strong identity verification) in Kibana's API key management, restricting a key to connections authenticated by a specific TLS certificate."
