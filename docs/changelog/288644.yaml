prs:
- https://github.com/elastic/kibana/pull/288644
type: bug-fix
products:
- product: cloud-serverless
- product: kibana
areas:
- Alerting and reporting
title: Fix alerting rules keeping an API key that was queued for invalidation
description: Fixes an alerting rule keeping an API key that was already queued for invalidation. API key rotation and rule enabling now replace the stored rule in full, so credentials removed during rotation no longer remain on the rule.
