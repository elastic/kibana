prs:
- https://github.com/elastic/kibana/pull/288499
issues:
- https://github.com/elastic/kibana/issues/288351
type: bug-fix
products:
- product: cloud-serverless
- product: kibana
title: Fix Threat Hunting leads not being saved on serverless
description: "Fixes Threat Hunting Leads reporting no entities on serverless even when the entity store held data. Lead generation ran correctly, but the leads index could not be created because serverless rejects the `index.auto_expand_replicas` setting."
