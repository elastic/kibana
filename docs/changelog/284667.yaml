prs:
- https://github.com/elastic/kibana/pull/284667
issues:
- https://github.com/elastic/security-team/issues/18364
type: enhancement
products:
- product: cloud-serverless
  target: 2026-08-25
- product: kibana
title: 'Add "Get Blacklist" and IPv6 support to the AbuseIPDB connector'
description: 'Adds a "Get Blacklist" action to the AbuseIPDB connector, returns `lastReportedAt` on IP checks, and accepts IPv4 and IPv6 addresses.'
