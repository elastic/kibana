prs:
- https://github.com/elastic/kibana/pull/282772
type: bug-fix
products:
- product: cloud-serverless
  target: 2026-08-21
- product: kibana
areas:
- Alerting and reporting
title: Fix Cases custom field values falling out of sync with field definitions
description: "Fixes legacy Cases custom fields and field-library values getting out of sync on write. Linked fields now stay paired in both directions, and conflicting values in the same request return an error instead of silently keeping one side."
