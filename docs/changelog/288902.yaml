prs:
- https://github.com/elastic/kibana/pull/288902
issues:
- https://github.com/elastic/kibana/issues/217503
type: bug-fix
products:
- product: cloud-serverless
- product: kibana
title: Fix stale endpoint transforms left in Elasticsearch after package upgrades
description: Fixes Fleet removing only one stale transform version per package upgrade, and adds a reconciliation step that deletes orphaned endpoint transforms that no saved object references.
