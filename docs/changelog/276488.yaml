prs:
- https://github.com/elastic/kibana/pull/276488
type: bug-fix
products:
- product: cloud-serverless
- product: kibana
title: "Fix the `security.alerts` tool returning alerts from other spaces"
description: "Scopes the Agent Builder `security.alerts` tool to the current Kibana space. The tool no longer accepts an `index` argument, and it returns zero alerts when the current space has no alerts index."
