prs:
  - https://github.com/elastic/kibana/pull/290705
issues:
  - https://github.com/elastic/security-team/issues/19288
type: feature
products:
  - product: cloud-serverless
  - product: kibana
title: Add AlertZero action catalog API and agent builder tool
description: Adds the internal AlertZero action catalog API (`/internal/alertzero/actions`) for category-scoped discovery of installed action workflows, plus the `security.alertzero.actions.list` agent builder tool that surfaces the catalog to AI agents at runtime.
