prs:
- https://github.com/elastic/kibana/pull/285178
issues:
- https://github.com/elastic/kibana/issues/283864
type: bug-fix
products:
- product: cloud-serverless
- product: kibana
areas:
- Kibana platform
title: Fix detection rules silently dropping exceptions for large IP range lists
description: 'Fixes detection rule exception items that use `ip_range` value lists with more than 200 dash-notation entries so those exceptions are evaluated and can suppress matching alerts.'
