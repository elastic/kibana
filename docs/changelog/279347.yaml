prs:
- https://github.com/elastic/kibana/pull/279347
type: enhancement
products:
- product: cloud-serverless
  target: 2026-07-22
- product: kibana
areas:
- AI and machine Learning
title: Reduce duplicate anomaly detection alerts by preferring `event.ingested`
description: When `event.ingested` is present, rule queries use the previous run window
  on that field instead of overlapping timestamp lookbacks, while results without
  `event.ingested` keep the existing behavior.
