prs:
- https://github.com/elastic/kibana/pull/275711
type: breaking-change
products:
- product: cloud-serverless
  target: 2026-07-22
- product: kibana
title: Update endpoint metadata APIs to enforce field length limits
description: Endpoint metadata, policy response, protection note, and script listing
  APIs now validate request field lengths.
impact: Requests that exceed the new field-length limits are rejected.
action: Update endpoint metadata, policy response, protection note, and script listing
  API clients to keep request fields within the documented limits.
subtype: api
