prs:
- https://github.com/elastic/kibana/pull/285163
issues:
- https://github.com/elastic/response-ops-team/issues/707
type: bug-fix
products:
- product: cloud-serverless
  target: 2026-08-21
- product: kibana
areas:
- Alerting and reporting
title: Fix scheduled task authentication failures for serverless API keys
description: "Fixes scheduled tasks failing authentication when stored serverless identity API keys used a different encoding than Elasticsearch expects. Task Manager now normalizes both stored shapes before using the key."
