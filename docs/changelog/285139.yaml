prs:
- https://github.com/elastic/kibana/pull/285139
issues:
- https://github.com/elastic/kibana/issues/268111
- https://github.com/elastic/kibana/issues/272474
type: bug-fix
products:
- product: cloud-serverless
- product: kibana
title: Fix 500 error in Cloud Security graph when querying with pinned nodes
description: Fixes a `null_pointer_exception` returned by the Cloud Security graph API when `pinnedIds` was non-empty, caused by a nullable column being passed directly into an ES|QL predicate.
