prs:
- https://github.com/elastic/kibana/pull/279346
type: feature
products:
- product: cloud-serverless
  target: 2026-07-22
- product: kibana
areas:
- Alerting and reporting
title: Add autocomplete and validation to the Cases template YAML editor
description: Adds field-type snippets, `$ref` autocomplete, and inline validation
  for misspelled metadata keys, invalid `show_when` and `required_when` references,
  and control-specific validation rules.
