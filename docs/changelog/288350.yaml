prs:
- https://github.com/elastic/kibana/pull/288350
issues:
- https://github.com/elastic/kibana/issues/283081
type: bug-fix
products:
- product: cloud-serverless
- product: kibana
areas:
- Kibana platform
title: Fix settings pages failing to load when a stored setting has an invalid value
description: Kibana now skips a stored UI setting whose value has an unsupported type and renders the remaining settings, instead of failing the whole page.
