prs:
- https://github.com/elastic/kibana/pull/282815
type: bug-fix
products:
- product: cloud-serverless
  target: 2026-08-12
- product: kibana
areas:
- Dashboards and visualizations
title: "Fix duration `format` schema"
description: "Fixed Lens as-code `duration` format round-trip for `decimals` and `compact` when `to` is not `auto-approximate`."
