steps:
  - label: ":pipeline::grey_question::seedling: Trigger Kibana Tests for ${ENVIRONMENT}"
    command: "make run-environment-tests"
    agent:
      image: "docker.elastic.co/ci-agent-images/quality-gate-seedling:0.0.1"
