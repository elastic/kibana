steps:
  - command: .buildkite/scripts/re-use.sh
    label: 'Test 1'
    agents:
      queue: n2-4-spot
    key: test-1
    timeout_in_minutes: 60
    retry:
      automatic:
        - exit_status: '-1'
          limit: 3

  - wait: ~
    continue_on_failure: true

  - command: .buildkite/scripts/re-use.sh
    label: 'Test 2'
    agents:
      queue: n2-4-spot
    key: test-2
    timeout_in_minutes: 60
    retry:
      automatic:
        - exit_status: '-1'
          limit: 3

  - wait: ~
    continue_on_failure: true

  - command: .buildkite/scripts/re-use.sh
    label: 'Test 3'
    agents:
      queue: n2-4-spot
    key: test-3
    timeout_in_minutes: 60
    retry:
      automatic:
        - exit_status: '-1'
          limit: 3
