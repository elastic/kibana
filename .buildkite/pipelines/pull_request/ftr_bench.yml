steps:
  - command: .buildkite/scripts/steps/ftr_bench.sh
    label: 'FTR Bench'
    agents:
      machineType: n2-highmem-32
      diskSizeGb: 100
    key: ftr_bench
    timeout_in_minutes: 300
    soft_fail: true
    depends_on:
      - build
      # - quick_checks
      # - checks
      # - linting
      # - linting_with_types
      # - check_types
      # - check_oas_snapshot
