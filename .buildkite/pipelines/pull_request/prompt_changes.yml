steps:
  - command: ts-node .buildkite/scripts/steps/checks/prompt_changes_detector.ts
    label: 'Check Prompt Changes'
    agents:
      machineType: n2-standard-2
      preemptible: true
      diskSizeGb: 100
    timeout_in_minutes: 10
    retry:
      automatic:
        - exit_status: '-1'
          limit: 3
