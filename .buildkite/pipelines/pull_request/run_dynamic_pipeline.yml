steps:
  - label: Run Dynamic Pipeline
    command: |
      .buildkite/scripts/pipelines/pull_request/pipeline.sh | buildkite-agent pipeline upload
    plugins:
      - elastic/es-partial-checkout: ~
