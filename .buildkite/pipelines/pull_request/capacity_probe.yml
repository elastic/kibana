steps:
  - command: |
      set -euo pipefail
      ZONE="$(curl -fsS -H 'Metadata-Flavor: Google' http://metadata.google.internal/computeMetadata/v1/instance/zone | awk -F/ '{print $4}')"
      echo "probe succeeded in ${ZONE}"
      nproc
      df -h /
    label: 'Capacity Probe us-central1-b (%n)'
    agents:
      machineType: c4d-standard-16
      diskType: hyperdisk-balanced
      zones: us-central1-a
      diskSizeGb: 200
    timeout_in_minutes: 10
    parallelism: 10

  - command: |
      set -euo pipefail
      ZONE="$(curl -fsS -H 'Metadata-Flavor: Google' http://metadata.google.internal/computeMetadata/v1/instance/zone | awk -F/ '{print $4}')"
      echo "probe succeeded in ${ZONE}"
      nproc
      df -h /
    label: 'Capacity Probe us-east1-b (%n)'
    agents:
      machineType: c4d-standard-16
      diskType: hyperdisk-balanced
      zones: us-central1-b
      diskSizeGb: 200
    timeout_in_minutes: 10
    parallelism: 10

  - command: |
      set -euo pipefail
      ZONE="$(curl -fsS -H 'Metadata-Flavor: Google' http://metadata.google.internal/computeMetadata/v1/instance/zone | awk -F/ '{print $4}')"
      echo "probe succeeded in ${ZONE}"
      nproc
      df -h /
    label: 'Capacity Probe us-west1-b (%n)'
    agents:
      machineType: c4d-standard-16
      diskType: hyperdisk-balanced
      zones: us-central1-c
      diskSizeGb: 200
    timeout_in_minutes: 10
    parallelism: 10