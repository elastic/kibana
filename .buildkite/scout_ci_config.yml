# Define which plugins should be run or skipped in Scout CI pipeline
ui_tests:
  enabled:
    - security_solution
  disabled:
    - apm
    - discover_enhanced
    - maps
    - observability_onboarding
