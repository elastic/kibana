puppeteer_skip_download=true

# pnpm 11 only reads auth/registry settings from .npmrc. Install behavior
# (node-linker, hoisting, peers) and allowBuilds are authored in
# pnpm-workspace.yaml; `kbn_pm bootstrap` regenerates its packages: list and the
# fenced overrides block from package.json#resolutions.
