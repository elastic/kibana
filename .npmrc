puppeteer_skip_download=true

# pnpm 11 only reads auth/registry settings from .npmrc. Install behavior
# (node-linker, hoisting, peers), overrides and allowBuilds are authored in
# pnpm-workspace.yaml; `kbn_pm bootstrap` regenerates only its packages: list.
