puppeteer_skip_download=true

# pnpm 11 only reads auth/registry settings from .npmrc. Install behavior
# (node-linker, hoisting, peers) and overrides live in pnpm-workspace.yaml,
# generated from package.json "pnpm" by `kbn_pm bootstrap`.
