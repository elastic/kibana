# THIS FILE IS AUTO-GENERATED — DO NOT EDIT BY HAND
# Generated by: node oas_docs/scripts/generate_required_field_fixes.js
#
# Fixes two categories of wrongly-required fields in the OAS bundles:
#   1. x-oas-optional — schema.maybe() emits x-oas-optional: true on the resolved
#      type schema, but the OAS generator still lists the field in required.
#   2. default-value — schema.object/etc. with defaultValue emits default: on the
#      resolved schema (the field is optional at runtime), but the OAS generator
#      still lists it in required.
# Each fix uses remove + update because the Bump overlay engine appends arrays
# on 'update' alone rather than replacing them.
#
# 9 schema(s) affected.

overlay: 1.0.0
info:
  title: Required-field fixes (auto-generated)
  version: 0.0.1
actions:
  - target: "$.components.schemas['Kibana_HTTP_APIs_security_query_roles_body'].required"
    description: "Remove incorrect 'required' property from optional fields (x-oas-optional or has default): sort, filters"
    remove: true
  - target: "$.components.schemas['Kibana_HTTP_APIs_security_query_roles_body']"
    description: "Restore required array without optional fields"
    update:
      required: []

  - target: "$.components.schemas['Security_Detections_API_EqlRule'].allOf[0].required"
    description: "Remove wrongly-required fields (x-oas-optional or has default): max_signals"
    remove: true
  - target: "$.components.schemas['Security_Detections_API_EqlRule'].allOf[0]"
    description: "Restore required array without optional fields"
    update:
      required:
        - "name"
        - "description"
        - "risk_score"
        - "severity"
        - "version"
        - "tags"
        - "enabled"
        - "risk_score_mapping"
        - "severity_mapping"
        - "interval"
        - "from"
        - "to"
        - "actions"
        - "exceptions_list"
        - "author"
        - "false_positives"
        - "references"
        - "threat"
        - "setup"
        - "related_integrations"
        - "required_fields"

  - target: "$.components.schemas['Security_Detections_API_EsqlRule'].allOf[0].required"
    description: "Remove wrongly-required fields (x-oas-optional or has default): max_signals"
    remove: true
  - target: "$.components.schemas['Security_Detections_API_EsqlRule'].allOf[0]"
    description: "Restore required array without optional fields"
    update:
      required:
        - "name"
        - "description"
        - "risk_score"
        - "severity"
        - "version"
        - "tags"
        - "enabled"
        - "risk_score_mapping"
        - "severity_mapping"
        - "interval"
        - "from"
        - "to"
        - "actions"
        - "exceptions_list"
        - "author"
        - "false_positives"
        - "references"
        - "threat"
        - "setup"
        - "related_integrations"
        - "required_fields"

  - target: "$.components.schemas['Security_Detections_API_MachineLearningRule'].allOf[0].required"
    description: "Remove wrongly-required fields (x-oas-optional or has default): max_signals"
    remove: true
  - target: "$.components.schemas['Security_Detections_API_MachineLearningRule'].allOf[0]"
    description: "Restore required array without optional fields"
    update:
      required:
        - "name"
        - "description"
        - "risk_score"
        - "severity"
        - "version"
        - "tags"
        - "enabled"
        - "risk_score_mapping"
        - "severity_mapping"
        - "interval"
        - "from"
        - "to"
        - "actions"
        - "exceptions_list"
        - "author"
        - "false_positives"
        - "references"
        - "threat"
        - "setup"
        - "related_integrations"
        - "required_fields"

  - target: "$.components.schemas['Security_Detections_API_NewTermsRule'].allOf[0].required"
    description: "Remove wrongly-required fields (x-oas-optional or has default): max_signals"
    remove: true
  - target: "$.components.schemas['Security_Detections_API_NewTermsRule'].allOf[0]"
    description: "Restore required array without optional fields"
    update:
      required:
        - "name"
        - "description"
        - "risk_score"
        - "severity"
        - "version"
        - "tags"
        - "enabled"
        - "risk_score_mapping"
        - "severity_mapping"
        - "interval"
        - "from"
        - "to"
        - "actions"
        - "exceptions_list"
        - "author"
        - "false_positives"
        - "references"
        - "threat"
        - "setup"
        - "related_integrations"
        - "required_fields"

  - target: "$.components.schemas['Security_Detections_API_QueryRule'].allOf[0].required"
    description: "Remove wrongly-required fields (x-oas-optional or has default): max_signals"
    remove: true
  - target: "$.components.schemas['Security_Detections_API_QueryRule'].allOf[0]"
    description: "Restore required array without optional fields"
    update:
      required:
        - "name"
        - "description"
        - "risk_score"
        - "severity"
        - "version"
        - "tags"
        - "enabled"
        - "risk_score_mapping"
        - "severity_mapping"
        - "interval"
        - "from"
        - "to"
        - "actions"
        - "exceptions_list"
        - "author"
        - "false_positives"
        - "references"
        - "threat"
        - "setup"
        - "related_integrations"
        - "required_fields"

  - target: "$.components.schemas['Security_Detections_API_SavedQueryRule'].allOf[0].required"
    description: "Remove wrongly-required fields (x-oas-optional or has default): max_signals"
    remove: true
  - target: "$.components.schemas['Security_Detections_API_SavedQueryRule'].allOf[0]"
    description: "Restore required array without optional fields"
    update:
      required:
        - "name"
        - "description"
        - "risk_score"
        - "severity"
        - "version"
        - "tags"
        - "enabled"
        - "risk_score_mapping"
        - "severity_mapping"
        - "interval"
        - "from"
        - "to"
        - "actions"
        - "exceptions_list"
        - "author"
        - "false_positives"
        - "references"
        - "threat"
        - "setup"
        - "related_integrations"
        - "required_fields"

  - target: "$.components.schemas['Security_Detections_API_ThreatMatchRule'].allOf[0].required"
    description: "Remove wrongly-required fields (x-oas-optional or has default): max_signals"
    remove: true
  - target: "$.components.schemas['Security_Detections_API_ThreatMatchRule'].allOf[0]"
    description: "Restore required array without optional fields"
    update:
      required:
        - "name"
        - "description"
        - "risk_score"
        - "severity"
        - "version"
        - "tags"
        - "enabled"
        - "risk_score_mapping"
        - "severity_mapping"
        - "interval"
        - "from"
        - "to"
        - "actions"
        - "exceptions_list"
        - "author"
        - "false_positives"
        - "references"
        - "threat"
        - "setup"
        - "related_integrations"
        - "required_fields"

  - target: "$.components.schemas['Security_Detections_API_ThresholdRule'].allOf[0].required"
    description: "Remove wrongly-required fields (x-oas-optional or has default): max_signals"
    remove: true
  - target: "$.components.schemas['Security_Detections_API_ThresholdRule'].allOf[0]"
    description: "Restore required array without optional fields"
    update:
      required:
        - "name"
        - "description"
        - "risk_score"
        - "severity"
        - "version"
        - "tags"
        - "enabled"
        - "risk_score_mapping"
        - "severity_mapping"
        - "interval"
        - "from"
        - "to"
        - "actions"
        - "exceptions_list"
        - "author"
        - "false_positives"
        - "references"
        - "threat"
        - "setup"
        - "related_integrations"
        - "required_fields"

