lang: Curl
# label:
source: |
  curl -X POST "localhost:5601/api/actions/connector" -H 'kbn-xsrf: true' -H 'Content-Type: application/json' -d'
  {
    "name": "my-connector",
    "connector_type_id": ".index",
    "config": {
      "index": "test-index"
    }
  }
  '
